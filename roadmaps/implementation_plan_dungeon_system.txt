DUNGEON SYSTEM IMPLEMENTATION PLAN (Roadmap & Status)

Legend:
Done = Fully implemented & tests pass
Partial = Some components implemented (details in parentheses)
X = Not started yet
Optional = Stretch / future wave

Scope Overview:
Deliver a data‑driven, procedural dungeon framework integrating layout generation, biome & thematic layering, enemy difficulty (relative level differential), objective orchestration, environmental hazards, puzzle & traversal mechanics, dynamic events, loot & crafting hooks, infinite player progression pacing, analytics, and deterministic replay. Constraints: single character, infinite leveling (no classes; universal skill access), infinite inventory stack, existing systems (loot, affixes, enemy difficulty, crafting, vendor, skill maze). Goals: High encounter variety, readable progression of challenge, systemic synergy (materials, recipes, vendors, mastery), minimal content authoring overhead, robust anti-exploit & performance guarantees.

STATUS DELTA (Latest):
- Phase 9 mutators (new scaffolding):
	- Implemented mutator registry and JSON loader (9.1 Done): RogueMutatorDesc{id, weight, reward_multiplier, effect_dsl}, registry add/find/get, and loaders from JSON text/file. Deterministic, minimal parser; caps and duplicate-id guards in place.
	- Deterministic selection helper (9.2 Partial): headless roll K choose N using worldgen micro-RNG with weighted picks (no UI yet). Stable manifest CSV utility and sorting for replay determinism.
	- Phase 9.3 Done: Stacking rules and incompatibility matrix with per-group caps; loader supports `group`, selection enforces `incompatible` pairs and `group_cap` via a backtracking compatible-subset search (greedy fallback retained). Run-summary callback registration/emission added for post-run analytics; depth profile export writes a JSON `depth_target_level_table` and ensures parent directories exist.
	- Tests added: `test_dungeon_phase9_1_mutator_loader`, `test_dungeon_phase9_2_selection`, and `test_dungeon_phase9_3_stacking_and_export` (verifies incompatibility/group-cap enforcement and JSON export exists).
	- Build & Tests: Debug (SDL2) all‑green (617/617) with -j12.
- Phase 8 loot & reward orchestration updates (current):
	- Chest distribution (overlay 10–13) and adjacent upgrade marker (14) remain in place; overlay writes guarded by overlay_magic.
	- EV‑normalized tier sampler finalized with a debug sampling API for tests; bounds, depth monotonicity, and bump behavior validated.
	- Upgrade‑possible heuristic integrated: worldgen now gates the upgrade guarantee on a conservative equipment snapshot; includes a test override API.
	- Smart‑drop coupling completed: the guaranteed upgrade chest now carries planned_def_index/planned_rarity derived from the inventory snapshot with a robust fallback across categories.
	- New tests: `test_dungeon_phase8_5_ev_sanity`, `test_dungeon_phase8_3_upgrade_guarantee`, and `test_dungeon_phase8_3_upgrade_coupling_integration`, alongside `test_dungeon_phase8_loot_and_materials`.
	- Build & Tests: Debug (SDL2) all‑green (614/614) with -j12.
- Phase 8.2 weighting and 8.3 upgrade hook completion:
	- EV‑normalized, depth‑aware reward tier weighting uses a deterministic basis‑point sampler around the clamped base tier. `bump_count` (off‑main‑path + ELITE/PUZZLE flags) shifts probability mass low→high per bump with clamps; tiers remain bounded [0..3].
	- Upgrade hook marks the selected placement in out_array (is_upgrade=1) and places an adjacent overlay 14 marker without clobbering the tier overlays. Gated by `rogue_dungeon_upgrade_possible()`; tests exercise forced yes/no via override. Planned contents are now computed and exposed via planned_def_index/planned_rarity on the marked chest.
	- Build & Tests re‑run after integration: all‑green (614/614) in Debug (SDL2) with -j12.
- Phase 7 hazards & traps landed and validation stabilized:
	- Hazard & Trap subsystem implemented: JSON trap defs (trigger_type, telegraph_ms, damage profile, cooldown), damage scales with depth ΔL and avoidance stats; overlap resolver caps simultaneous high-DPS hazards; disarm interaction with skill-based success probability and deterministic micro-RNG; unit `test_dungeon_phase7_traps` covers telegraph compliance, scaling monotonicity, overlap cap, and disarm odds.
	- Puzzle/traversal: soft-lock watchdog integrated into worldgen flow; minor scaffolding tightened.
	- Validation manager instrumentation: begin/done traces for all validators and cross-rules added; Entities validator now enforces skip-texture mode during headless validation to avoid renderer I/O and divide-by-zero in sheet slicing; restored previous flag on exit.
	- Build & Tests: Debug (SDL2) full suite all-green (611/611) with -j12.
- Phase 2 groundwork landed alongside Phase 1 kernel:
	- Phase 1 kernel slice: 1.1–1.4 implemented end‑to‑end.
		• Mini‑DSL parser: L(n) linear, H(k) hub, B(n,b) branching; deterministic manual parser.
		• Constraint solver: loop_percent, max_deadends, min/max degree enforced.
		• Critical path targeting heuristic wired into generator.
		• Public params/API: RogueDungeonGenParams, rogue_dungeon_generate_graph_ex, rogue_dungeon_generate_from_grammar.
		• Tests: determinism + constraints + CPL targeting added.
	- Phase 2 progress:
		• Tile stamping engine scaffolding (2.2 Partial): ASCII → transforms (rot/reflection) → exits → stamp into RogueTileMap with bounds checks.
		• Room template JSON loader (2.1 Done): minimal, dependency‑free parser supporting id/cls/biome_tags/encounter_slots/hazard_slots/puzzle_slot/grid/exits/doors (type+key_id)/deco; key‑order independent.
		• Door placement & locking metadata (2.3 Done): door types normal/locked/secret with key_id preserved across transforms and stamping.
		• Environmental deco overlay (2.4 Done): overlay_deco buffer and kind→code mapping, transform-aware stamping, renderer overlay pass, and navigation blocking semantics. Safety hardened via RogueTileMap.overlay_magic invariant to guard overlay usage in tests and runtime. Unit tests for deco layering and JSON key-order fuzz pass.
		• Unit tests (2.5 Done): bounds correctness, exit alignment, rotation determinism, and JSON loader coverage incl. doors/deco/transform alignment (test_dungeon_phase2_room_templates, test_dungeon_phase2_room_templates_json).
		• Integration: module compiled into rogue_core; deterministic behavior verified.
 - Phase 3 biome & theme layering landed:
	- Deterministic biome selection per depth bands with seed governance (3.1 Done).
	- Theme profile injection (fog density, ambient color, ambient SFX id) derived from biome tags (3.2 Done).
	- Hazard palette computed with depth scaling and tag adjustments (movement penalties, DoT, trap weighting) (3.3 Done).
	- Rare biome substitution with depth-scaled probability envelope (3.4 Done).
	- Tests added (3.5 Done): determinism, value ranges, and depth-scaling behavior covered in test_dungeon_phase3_theme; added distribution envelope and rare-biome probability bounds tests in test_dungeon_phase3_5_biome_distribution.
	- Build & Tests: Debug (SDL2) and full suite pass 100% (606/606) with -j12.

- Phase 4 encounter planning (expanded):
	- Implemented per-room encounter planner with BFS room depths, ΔL- and critical-path-weighted difficulty budgets, elite/miniboss spacing constraints, modifier smoothing for affix/mod clusters, and rare nemesis injection under deterministic micro-RNG.
	- Unit tests validate plan counts, budget sanity, type distribution, nemesis frequency, and ΔL mapping precision (test_dungeon_phase4_encounters, test_dungeon_phase4_delta_mapping).
	- Stability: persistence integration bridge previously flaky in one full run re-validated; full suite now consistently green.

 - Phase 5 objectives (scripting, gates, substitution) (Done):
	 - Objective scripting input with `rogue_dungeon_set_objective_script(const char*)`; parser supports tokens: ACTIVATE, CLEAR, PUZZLE, ?PUZZLE (optional), BOSS, and GATE(KEYSTONE).
	 - Gate mechanics: GATE(KEYSTONE) expands into COLLECT(KEYSTONE) + ACTIVATE(GATE) with RogueDungeonObjectiveParamFlags marking KEYSTONE/GATE steps. Builder preserves deterministic shallow/mid/far room selection and prevents duplicate room reuse.
	 - Dynamic substitution: required PUZZLE becomes CLEAR when infeasible; optional ?PUZZLE is dropped silently. Terminal BOSS enforced.
	 - Tests: baseline objectives determinism (existing), plus `test_dungeon_phase5_objective_script_and_gates.c` and `test_dungeon_phase5_dynamic_substitution.c` for flagging and substitution behavior.
	 - Build & Tests: Full suite green (608/608) under Debug (SDL2) with -j12.

FOUNDATIONAL ASSUMPTIONS / CURRENT STATE
- Tile / biome configuration files (assets/tiles.cfg, assets/biomes/*) (Partial)
- Enemy definitions & relative level differential model (Planned / Partial)
- Loot tables & affix system (Done)
- Skill maze & progression (Planned / Partial)
- Crafting material registry (Planned)
- Persistence + hash chain (Done)
- Basic world generation (Partial)

PHASE 0. Audit & Core Taxonomy
0.1 Catalog existing biome assets & tiles; map to dungeon theme tags (Partial) (biome→theme tag utility in code; full asset catalog pending)
0.2 Define dungeon archetypes: Linear, Branching, Looping, Hub, Gauntlet, Puzzle, Arena (Done)
0.3 Establish difficulty tier matrix (depth vs target player level delta) (Done) (initial monotonic depth→ΔL mapping; tunable)
0.4 Reserve ID ranges for room templates, event types, objective types (Done)
0.5 Tests: taxonomy uniqueness, template id collisions (Done) (unit: test_dungeon_phase0_taxonomy)

PHASE 1. Layout Generation Kernel
1.1 Graph grammar DSL (room_node, connector, constraint tags) (Done) (mini‑DSL L(n), H(k), B(n,b) parser implemented)
1.2 Layout generator (seed -> abstract graph) with pluggable archetype heuristics (Done) (seed→graph with archetype hint heuristics)
1.3 Constraint solver: enforce min/max branch factor, loop quota, dead-end limit (Done)
1.4 Critical path length target (objective pacing) (Done) (heuristic targeting wired)
1.5 Tests: deterministic reproduction, constraint satisfaction, path length distribution (Partial) (determinism + constraints + CPL targeting done; distribution coverage pending)
1.6 Kernel helpers: degree stats, critical path computation, deterministic graph hash (Done) (unit: test_dungeon_phase1_kernel)

PHASE 2. Room Template & Tile Assembly
2.1 Room template file (template_id, size_class, biome_tags, encounter_slots, hazard_slots, puzzle_slot, exit_points) (Done)
	(JSON loader implemented with key‑order independence; supports explicit doors (type/key_id) and deco markers; unit test added.)
2.2 Tile stamping engine (assemble template -> tile grid; handle rotations/reflections) (Partial) (ASCII parse, rotations/reflections, exit extraction, stamp to RogueTileMap with bounds checks)
2.3 Portal / door placement & locking metadata (Done) (door types normal/locked/secret; key_id; transform-safe; stamped tile kinds validated)
2.4 Environmental deco layering (aesthetic & minor collision) (Done) (overlay_deco buffer + transform-aware stamping, renderer overlay pass, navigation blocking; safety via overlay_magic guard; tests added and green)
2.5 Tests: template bounds correctness, exit alignment, rotation determinism (Done) (unit: test_dungeon_phase2_room_templates)

PHASE 3. Biome & Theme Layering
3.1 Biome selection policy per floor/segment (weighted by desired difficulty & scarcity analytics) (Done)
3.2 Thematic modifier injection (fog density, lighting color, ambient SFX) (Done)
3.3 Biome hazard palette (trap types, environmental DoT, movement penalty tiles) (Done)
3.4 Rare biome event substitution (chance to swap standard segment with rare theme) (Done)
3.5 Tests: biome distribution bounds, hazard palette completeness, rare event probability (Done) (added distribution envelope + rare-biome probability bounds; determinism/range/depth-scaling covered)

PHASE 4. Encounter Seeding & Enemy Composition
4.1 Encounter slot definition (slot_type: combat, elite_pack, mini_boss, puzzle_guard) (Done)
4.2 Difficulty budget allocation along critical path vs optional branches (Done) (ΔL- and critical-path-weighted budgets; depth/room depth scaling retained)
4.3 Relative level delta mapping per depth (tie to enemy scaling phase model) (Partial) (monotonic mapping implemented; precision tests added; further calibration pending)
4.4 Elite & modifier distribution smoothing (no clustering beyond thresholds) (Done) (spacing constraints + modifier smoothing)
4.5 Nemesis injection hook (chance / triggered conditions) (Done)
4.6 Tests: budget conservation, modifier spacing invariants, ΔL mapping correctness (Done) (counts/budgets/type distribution/nemesis frequency, plus ΔL precision in test_dungeon_phase4_delta_mapping)

PHASE 5. Objectives & Progression Gates
5.1 Objective taxonomy: Clear, Collect, Activate, Escort, Survive, PuzzleComplete, Boss (Partial) (core chain builder implemented with terminal BOSS; taxonomy struct defined)
5.2 Objective chain scripting file (sequence, optional branches, fail conditions) (Done)
5.3 Gate mechanics (sealed door, barrier rune, keystone item) (Done)
5.4 Dynamic objective substitution (adaptive if player overpowered: insert optional challenge) (Done)
5.5 Tests: chain determinism, gate unlock integrity, substitution constraints (Done)

PHASE 6. Puzzle & Traversal Subsystem
6.1 Puzzle template file (logic_type, param set, min_skill_requirements) (Partial) (minimal JSON loader added; logic_type/param0/min_skill_req parsed)
6.2 Traversal mechanics (moving platforms, timed doors, jump glyphs, secret passages) (Partial) (marker placement scaffold: jump glyph in puzzle rooms, timed door in treasure rooms; stamped as overlay codes for tests)
6.3 Assist mode toggles (longer timers, highlight interactables) (Optional) (Partial) (toggles struct defined; no runtime effects yet)
6.4 Fail reset & soft-lock prevention watchdog (Done) (connectivity watchdog implemented over graph)
6.5 Tests: solvability, time limit variance, soft-lock detection (Partial) (unit test covers JSON load, marker placement, and watchdog)

PHASE 7. Hazard & Trap System
7.1 Trap definition file (trigger_type, telegraph_duration, damage_profile, cooldown) (Done)
7.2 Scaling trap damage with relative level differential & player avoidance stats (Done)
7.3 Overlap resolver (cap active simultaneous high-damage hazards) (Done)
7.4 Disarm interaction & skill-based success probability (Done)
7.5 Tests: telegraph compliance, damage scaling monotonicity, disarm probability accuracy (Done)

PHASE 8. Loot & Reward Orchestration
8.1 Chest distribution algorithm (reserve for dead-end & milestone rooms) (Done)
8.2 Reward tier weighting by depth, optional branch completion, challenge objectives (Done) (EV‑normalized, depth‑aware weighted sampler using integer basis points; bump_count (off‑main‑path + ELITE/PUZZLE) shifts probability mass low→high with clamps; deterministic micro‑RNG; tiers capped at 3)
8.3 Smart drop injection (ensure at least one upgrade opportunity per run unless top-tier equipped) (Done)
	(Adjacent upgrade marker + is_upgrade output hook, upgrade‑possible heuristic + test override, and planned contents coupling completed. The marked chest exposes planned_def_index and planned_rarity chosen from a strictly-better candidate w.r.t. the snapshot, with rarity clamped [1..3] and nudged by chest tier. Robust fallback flips weapon/armor categories when the primary pool is empty.)
8.4 Material & crafting recipe seeding (rare nodes inside dungeons) (Done)
8.5 Tests: EV consistency vs difficulty, upgrade guarantee logic, chest placement constraints (Done) (added `test_dungeon_phase8_5_ev_sanity`, `test_dungeon_phase8_3_upgrade_guarantee`, and `test_dungeon_phase8_3_upgrade_coupling_integration`; placement and marker adjacency covered in `test_dungeon_phase8_loot_and_materials`)

PHASE 9. Run Modifiers & Mutators
9.1 Mutator definition file (id, weight, effect DSL, reward_multiplier) (Done)
9.2 Pre-run mutator selection UI (roll K choose N) (Partial) (deterministic headless selection helper implemented; UI pending)
9.3 Stacking rules & incompatibility matrix (Done)
9.4 Dynamic mid-run mutator event (optional challenge offer) (X)
9.5 Tests: reward scaling correctness, incompatibility enforcement, deterministic selection (Partial) (incompatibility/group-cap enforcement covered; reward scaling checks pending)

PHASE 10. Scaling & Adaptive Difficulty Integration
10.1 Map dungeon depth -> target enemy relative ΔL band (X)
10.2 Performance-based on-the-fly tweaks (if player dominance sustained: optional side-elite spawn) (X)
10.3 Cap adaptive stacking: relative model first, mutators second, adaptive last (ordering doc) (X)
10.4 Boss difficulty calibration vs player TTK metrics (X)
10.5 Tests: ordering invariants, adaptive envelope clamps, TTK variance bounds (X)

PHASE 11. Time & Score Systems
11.1 Run timer & segment splits (X)
11.2 Scoring formula (time efficiency, damage taken, optional objectives, mutator intensity) (X)
11.3 Rank thresholds & reward bundles (X)
11.4 Anti-speed exploit (detect path skipping anomalies) (X)
11.5 Tests: score reproducibility, exploit detection false positive rate (X)

PHASE 12. Persistence & Replay
12.1 Run seed & config snapshot (layout graph, mutators, objectives) (X)
12.2 Journal (enter_room, encounter_start, objective_events) hash chain (X)
12.3 Replay harness (seed + journal -> deterministic state) (X)
12.4 Partial resume (disconnect safety) (X)
12.5 Tests: replay equivalence, resume integrity (X)

PHASE 13. Performance & Memory
13.1 Streaming room activation (load active + neighbors; unload far) (X)
13.2 SoA for room runtime metadata (visited, cleared, hazard_flags) (X)
13.3 Object pool for traps, projectiles, particle VFX (X)
13.4 Micro-bench: 100 rooms generated < target ms; active tick budget < frame slice (X)
13.5 Memory targets: layout + runtime meta < 256 KB typical run (X)
13.6 Tests: generation latency P95, memory ceiling, GC/pool churn rate (X)

PHASE 14. Analytics & Telemetry
14.1 Layout shape metrics (branch factor, loop count, depth) (X)
14.2 TTK & death heatmaps per room archetype (X)
14.3 Puzzle failure & retry counts (X)
14.4 Mutator adoption rates & impact on score (X)
14.5 Loot upgrade success rate per run (X)
14.6 Drift alerts (TTK or upgrade rate deviates) (X)
14.7 Tests: analytic aggregation correctness, drift false positive rate (X)

PHASE 15. Anti-Exploit & Integrity
15.1 Room skip detection (teleport / clip anomalies) (X)
15.2 Chest prediction / reroll abuse guard (limit retries on layout regen) (X)
15.3 AFK farm detection (minimal movement + periodic spawn kills) (X)
15.4 Journal tamper detection (hash mismatch) (X)
15.5 Tests: exploit scenario simulation, false positive bounds (X)

PHASE 16. UI / UX
16.1 Procedural map panel (fog-of-war, discovered rooms, objective markers) (X)
16.2 Mutator selection UI & mid-run offer dialog (X)
16.3 Reward summary screen (score breakdown, upgrade diff highlights) (X)
16.4 Puzzle assist toggles & accessibility overlays (X)
16.5 Danger indicators (trap density, elite presence) (X)
16.6 Tests: map reveal determinism, summary correctness snapshot (X)

PHASE 17. Tooling & Dev Experience
17.1 Layout debugger (render abstract graph with budgets) (X)
17.2 Seed explorer CLI (generate N seeds stats table) (X)
17.3 Mutator impact simulator (expected difficulty delta & reward scaling) (X)
17.4 TTK simulation harness integration (enemy difficulty model tie-in) (X)
17.5 Template coverage analyzer (unused templates report) (X)
17.6 Tests: deterministic tool outputs, harness reproducibility (X)

PHASE 18. Future / Optional Extensions
18.1 Multi-floor meta progression (carry-over mutator stacking) (Optional)
18.2 Rogue events (choose-one narrative modifiers) (Optional)
18.3 Dynamic live modifiers (global weekend mutator) (Optional)
18.4 Co-op synchronization hooks (future) (Optional)
18.5 Procedural puzzle generator (beyond templates) (Optional)

DEPENDENCIES & INTEGRATIONS
- Uses enemy difficulty relative ΔL model (depth mapping).
- Integrates with loot & crafting for chest & material spawns.
- Pulls progression (skill unlocks) for puzzle gating / traversal requirements.
- Vendor special offer triggers can reference dungeon score ranks.
- Persistence journal & RNG governance reuse existing infrastructure.

RISKS & MITIGATIONS
- Repetitive layouts -> Graph grammar diversity + template coverage analyzer (17.5)
- Overtuned difficulty spikes -> Depth-based ΔL smoothing + analytics drift alerts (14.6)
- Performance stalls on generation -> Streaming & preallocation (13.x)
- Puzzle soft-locks -> Reset watchdog & solvability tests (6.4/6.5)
- Exploit path skipping -> Skip detection & scoring validation (11.4,15.1)
- Reward inflation -> EV normalization & upgrade guarantee cap (8.x)

KEY METRICS (Targets)
- Layout generation time: < 40 ms P95 (standard size)
- Active tick cost (dungeon systems): < 1.5 ms/frame P95
- Same-level path TTK variance (combat rooms): CV < 0.28
- Upgrade opportunity realization: >= 85% runs produce ≥1 potential equip upgrade
- Puzzle soft-lock incidence: < 0.1% runs
- Mutator selection diversity: top mutator pick share < 45%
- Replay determinism: 100% identical layout + objectives over 100 seeds

ROLL-OUT STRATEGY
1. Phases 0–1 taxonomy + abstract layout kernel.
2. Phases 2–3 templates & biome layering for visual/thematic grounding.
3. Phases 4–5 encounter seeding + objectives gating core loop.
4. Phases 6–7 puzzle & hazard depth for interaction variety.
5. Phases 8–9 reward orchestration + mutators to deepen replay.
6. Phases 10–11 scaling and scoring systems.
7. Performance, persistence, analytics (12–14) for stability.
8. Anti-exploit, UI, tooling (15–17) finalize & refine.
9. Optional extensions (18) post stabilization.

TEST PRIORITIZATION
Early: 0,1,2,3,4,5
Mid: 6,7,8,9,10,11,12,13
Late: 14,15,16,17,18

NOTES
- Deterministic ordering critical: sort room IDs & mutators before hashing.
- Keep all numeric curve parameters externalized for live tuning.
- Use heatmaps & coverage reports early to prevent template underuse.
- Provide dev toggles for: force archetype, force mutators, lock seed.
- Integrate with enemy simulation harness to precompute target ΔL table per depth.
\nCROSS-SYSTEM SYNERGIES (Added)
Ordering (Run Scaling): Progression Power Table -> Depth Target Level Mapping -> Enemy Difficulty ΔL Application -> Mutator Effects -> Adaptive Encounter Tweaks -> Reward Orchestration (loot/material/vendor hooks) -> Score Calculation.

Inputs Consumed:
 - Progression: effective_level curve & power_index percentiles shape depth_target_level table generation; puzzle gating reads maze node tags (mobility, insight, elemental_affinity) for room accessibility.
 - Enemy Difficulty: Provides ΔL->multiplier table; dungeon precomputes depth_difficulty_profile referencing that table to assign encounter budgets.
 - Loot & Equipment: Upgrade detection heuristic (dungeon Phase 8.3) queries equipment power snapshot & progression attribute vector to ensure at least one meaningful upgrade chest; interacts with loot smart drop API.
 - Crafting & Gathering: Material scarcity signals bias chest material nodes & rare room events; rare biome substitution (Phase 3.4) can inject scarce resource pockets.
 - Vendor System: High score rank or optional challenge completion emits vendor_offer_event modifiers (e.g., discount token, special blueprint weighting).
 - Skill System: Traversal/puzzle templates specify required skill tags; skill layer supplies capability flags to gating logic.
 - World Boss: Boss defeat events unlock new mutator pools or rare biome variants; depth generator includes world_boss_epoch_id in seed to rotate content.

Outputs Produced:
 - depth_target_level_table.json (export) for analytics & balancing; consumed by enemy difficulty regression tests.
 - run_reward_profile (expected materials, upgrade probability) ingested by economy telemetry & crafting balancing.
 - mutator_selection_manifest (ordered) used by replay + integrity hash chain.

Integrity / Determinism:
 - Run hash includes (layout_graph_hash, depth_target_level_table_hash, mutator_ids_sorted, objective_chain_hash, enemy_curve_version); ensures cross-system replay comparability.
 - Ordering rule test: Layout generation with fixed seed must produce identical sequence irrespective of current equipment or progression state (equipment/progression only influence smart drop contents, not layout graph). CI to assert invariance.

Risk Mitigation Cross-System:
 - Avoid power creep feedback loop (frequent upgrades speeding progression -> higher ΔL trivialization): enforce upgrade guarantee ceiling and feed realized upgrade count into enemy adaptive tuning epoch after run ends (not mid-run).
 - Prevent exploit of mutator reward stacking with vendor special offers: vendor hooks only triggered once per unique mutator_id per run.

Open Tasks:
 - Add gating manifest listing all puzzle/skill tags used in current seed for fast audit.
 - Add gating manifest listing all puzzle/skill tags used in current seed for fast audit.
 - Provide additional consumers for run_summary callbacks in vendor/crafting modules (registration API exists; wire listeners).

Change Impact: Documentation only; follow-up commits should implement exports, invariance tests, and callback interfaces referenced here.
