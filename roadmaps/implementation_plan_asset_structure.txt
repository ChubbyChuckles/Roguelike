# Asset Structure & Management Implementation Plan
# Roguelike Game Engine - Asset Organization & Validation System
# Version: 1.0
# Date: September 7, 2025
# Author: Christian "ChubbyChuckles" Rickert

## Phase 1: Core Directory Structure Design
[x] Design hierarchical asset directory structure with category-based organization (see "Proposed Directory Structure" below; scaffold aligns with current assets/ layout; minor additions for fonts/, shaders/, meta/ subtrees documented)
[x] Create asset type classification system (graphics, audio, data, shaders, fonts) – implemented as `src/util/asset_classification.{h,c}` with deterministic, pure string heuristics (no filesystem I/O) and unit test `test_asset_classification`.
[x] Establish naming conventions for assets and directories – documented in README (Assets section) and summarized below (snake_case, singular category, semantic grouping: e.g. graphics/sprites/characters/player/player_idle.png, audio/sfx/ui/click.wav, data/configs/gameplay/*.json).
[x] Define version control strategy for asset files – binary (PNG/OGG/MP3) tracked directly (no Git LFS yet due to small size); generated manifests/checksums emitted under meta/ excluded via .gitignore (planned). Large raw source (e.g., PSD) explicitly excluded until a /source_art policy phase. Strategy note added to README.
[x] Create documentation templates for asset contributors – initial contributor guidelines section appended to README (naming, formats, dimensions, audio normalization, localization file layout). Future expansion: separate docs/asset_guidelines.md.

## Phase 2: JSON Schema & Metadata System
[x] Design core JSON schema for asset metadata (RogueSchema-based internal builders)
[x] Create specialized schemas for different asset types:
  [x] Sprite sheet definitions (frame coordinates, animations) — implemented in `schema_sprites.{h,c}`
  [x] Audio clip metadata (loop points, volume, format) — implemented in `schema_audio.{h,c}`
  [x] Level/map data structures
  [x] UI theme and layout definitions (Implemented schema_theme.{h,c} + test_ui_theme_schema)
[x] Implement JSON validation utilities (reused existing `rogue_schema_validate_json`; added new test `test_sprite_audio_schema`)
[x] Create schema versioning system for backward compatibility (implemented: optional `schema_version` field (1..1024) added to all content schemas; linear migration registry + unit test `test_schema_versioning_basic`; future enhancement: automatic invocation wrapper)

### Phase 2 In-Progress / Next Steps (Completed)
- [x] Implement placeholder asset enforcement: ensure `assets/placeholder.png` exists; unit test `test_asset_placeholder_enforcement`.
- [x] Add Level/Map schema (`schema_levels.{h,c}` + unit test `test_level_schema`).
- [x] Add UI Theme/Layout schema (`schema_theme.{h,c}` + `test_ui_theme_schema`).
- [x] Add shared optional `schema_version` field + migration registry (see above). Phase 2 core scope now complete; proceeding to Phase 3.

### Phase 3 Progress Update (Initial Slice Implemented)
- [x] Implement asset loading abstraction layer scaffold (added `asset_manager.{h,c}`: fixed-cap texture records, ref counting, headless safe; deferred real SDL texture creation to later slice).
- [x] Create cross-platform path resolution helpers (added `path_utils.{h,c}` with normalize/join/is_absolute; SDL_GetBasePath integration deferred).
- [x] Design texture/surface caching mechanism (simple deterministic array + duplicate suppression by derived id/basename).
- [x] Implement resource lifetime management (acquire bumps ref_count, release compacts array; unit test `test_asset_manager_basic`).
- [ ] Add support for different image formats (PNG, BMP, JPG via SDL_image) (planned – current slice records logical entries only).
- [ ] Create audio loading system (SDL_mixer integration) (planned – will extend asset manager with sound registry & format probing).

Next Phase 3 steps: integrate SDL_image when available, add width/height probe (surface->w/h), introduce negative caching for failed loads, and wire a lightweight hot-reload polling hook.

### Phase 2 Progress Update
- Sprite & audio schema builders implemented and validated (see new unit test `test_sprite_audio_schema`).
- Classification header annotated with future enum expansion TODOs (non-breaking).
- Commit message updated; README pending augmentation for new schema usage examples.
- Placeholder asset enforcement completed (helper + unit test + stub asset). Next: level & UI theme schemas.


## Phase 3: SDL2 Integration & Cross-Platform Loading
[x] Implement asset loading abstraction layer for SDL2 (asset_manager.{h,c} with real IMG_LoadTexture when SDL_image present; headless safe)
[x] Create cross-platform path resolution system using SDL_GetBasePath (path_utils + rogue_find_asset_path now prepends SDL_GetBasePath ascents before legacy relative probes)
[x] Design texture/surface caching mechanism (fixed-cap array keyed by derived basename id, duplicate suppression, ref counting)
[x] Implement resource lifetime management (acquire/release compaction, SDL_DestroyTexture/Mix_FreeChunk on refcount zero & shutdown)
[x] Add support for different image formats (PNG, BMP, JPG via SDL_image) (implicit via IMG_LoadTexture loader; width/height captured via SDL_QueryTexture; negative caching of failures)
[x] Create audio loading system (SDL_mixer integration, Mix_LoadWAV with negative caching + hot-reload polling foundation)

## Phase 4: Asset Validation & Integrity System
[x] Implement JSON schema validation (basic hook: `rogue_asset_json_validate_basic` – future extension will auto-dispatch to schema modules)
[x] Create asset file integrity checking (file existence helper + CRC32 computation `rogue_asset_crc32_file`)
[x] Design missing asset fallback system (global fallback texture path; applied during `rogue_asset_manager_acquire_texture` when original missing)
[x] Implement asset dependency tracking (in-memory edge list with `rogue_asset_dep_add` / `rogue_asset_dep_get`)
[x] Create asset usage reporting tools (`rogue_asset_usage_stats` aggregates counts/failures/handles)
[x] Add checksum validation for critical assets (register + verify APIs)

## Phase 5: CI/CD Integration
[x] Extend GitHub Actions workflow for asset validation (asset_validate step after tests + manifest artifact)
[x] Create automated asset testing pipeline (CTest target asset_validation_cli exercises CLI)
[x] Implement cross-platform asset validation (portable C11 scanner + CRC32)
[x] Add asset packaging and distribution (manifest installed to meta/ via CPack)
[x] Create asset change detection and notification system (CLI --baseline diff reporting)
[x] Implement automated asset optimization (heuristic advisor: duplicates & large PNG hints)

## Phase 6: Performance Optimization
[x] Design asset preloading system (added preload APIs & batch: rogue_asset_manager_preload_texture/rogue_asset_manager_preload_textures)
[x] Implement memory-efficient asset streaming (incremental queue + stream_step batching)
[x] Create texture atlas generation tools (horizontal atlas builder API + UV output)
[x] Optimize asset loading for different platforms (compressed variant probing .ktx2/.ktx/.dds preference)
[x] Add profiling and benchmarking for asset operations (metrics struct + cumulative timing using SDL perf counters)
[x] Implement lazy loading for non-critical assets (toggle + ensure load API)
[*] Expose metrics via debug overlay panel (Asset Metrics panel: queue depth, timings, streaming toggles)

## Phase 7: Developer Tools & Utilities
[*] Create asset management CLI tools (baseline asset_validate already implemented Phase 5; extended CLI tooling pending separate asset_tool target)
[x] Implement asset hot-reloading for development (extended: usage reload tracking hook + poll integration)
[x] Design visual asset browser/manager (stub panel panels_asset_browser.c listing texture/audio entries + filter API)
[x] Create asset conversion and processing scripts (tools/atlas_generate.py scaffold for atlas manifest generation)
[x] Add asset usage analytics and reporting (RogueAssetUsageStats extended with peaks, reload counters, last reload tick; new test_asset_phase7_usage_stats)
[ ] Implement asset debugging and inspection tools (pending: future deep-dive panel with dependency graph & checksum diffs)

## Phase 8: Documentation & Maintenance
[ ] Create comprehensive asset system documentation
[ ] Write contributor guidelines for asset creation
[ ] Document JSON schema specifications
[ ] Create troubleshooting and FAQ guides
[ ] Establish asset review and approval process
[ ] Document performance best practices

---

# Detailed Implementation Specifications

## 1. Proposed Directory Structure

```
assets/
├── graphics/
│   ├── sprites/
│   │   ├── characters/
│   │   │   ├── player/
│   │   │   │   ├── player_idle.png
│   │   │   │   ├── player_walk.png
│   │   │   │   └── player.json
│   │   │   ├── enemies/
│   │   │   │   ├── goblin/
│   │   │   │   │   ├── goblin_idle.png
│   │   │   │   │   └── goblin.json
│   │   │   │   └── orc/
│   │   │   └── npcs/
│   │   ├── items/
│   │   │   ├── weapons/
│   │   │   ├── armor/
│   │   │   ├── consumables/
│   │   │   └── misc/
│   │   ├── environment/
│   │   │   ├── tiles/
│   │   │   ├── decorations/
│   │   │   └── backgrounds/
│   │   └── ui/
│   │       ├── buttons/
│   │       ├── icons/
│   │       ├── panels/
│   │       └── cursors/
│   ├── textures/
│   │   ├── materials/
│   │   └── effects/
│   └── fonts/
│       ├── ui/
│       └── dialogue/
├── audio/
│   ├── music/
│   │   ├── ambient/
│   │   ├── combat/
│   │   └── menu/
│   ├── sfx/
│   │   ├── combat/
│   │   ├── environment/
│   │   ├── ui/
│   │   └── character/
│   └── voice/
│       └── dialogue/
├── data/
│   ├── levels/
│   │   ├── dungeons/
│   │   └── overworld/
│   ├── configs/
│   │   ├── gameplay/
│   │   ├── graphics/
│   │   └── audio/
│   ├── localization/
│   │   ├── en/
│   │   ├── es/
│   │   └── fr/
│   └── schemas/
│       ├── sprite.schema.json
│       ├── audio.schema.json
│       └── level.schema.json
├── shaders/
│   ├── vertex/
│   ├── fragment/
│   └── compute/
└── meta/
    ├── manifests/
    ├── checksums/
    └── documentation/
```

## 2. JSON Schema Examples

### Sprite Sheet Definition (sprite.schema.json)
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Sprite Asset Definition",
  "type": "object",
  "required": ["version", "name", "texture", "sprites"],
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "name": {
      "type": "string",
      "description": "Human-readable name of the sprite asset"
    },
    "texture": {
      "type": "object",
      "required": ["file", "width", "height"],
      "properties": {
        "file": {
          "type": "string",
          "description": "Relative path to texture file"
        },
        "width": {"type": "integer", "minimum": 1},
        "height": {"type": "integer", "minimum": 1},
        "format": {
          "type": "string",
          "enum": ["PNG", "BMP", "JPG"],
          "default": "PNG"
        }
      }
    },
    "sprites": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "x", "y", "width", "height"],
        "properties": {
          "id": {"type": "string"},
          "x": {"type": "integer", "minimum": 0},
          "y": {"type": "integer", "minimum": 0},
          "width": {"type": "integer", "minimum": 1},
          "height": {"type": "integer", "minimum": 1},
          "pivot_x": {"type": "number", "default": 0.5},
          "pivot_y": {"type": "number", "default": 0.5}
        }
      }
    },
    "animations": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "frames"],
        "properties": {
          "name": {"type": "string"},
          "frames": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["sprite_id", "duration"],
              "properties": {
                "sprite_id": {"type": "string"},
                "duration": {"type": "number", "minimum": 0}
              }
            }
          },
          "loop": {"type": "boolean", "default": true}
        }
      }
    }
  }
}
```

### Example Player Character JSON (player.json)
```json
{
  "version": "1.0.0",
  "name": "Player Character",
  "texture": {
    "file": "player_spritesheet.png",
    "width": 256,
    "height": 128,
    "format": "PNG"
  },
  "sprites": [
    {
      "id": "idle_01",
      "x": 0,
      "y": 0,
      "width": 32,
      "height": 32,
      "pivot_x": 0.5,
      "pivot_y": 1.0
    },
    {
      "id": "idle_02",
      "x": 32,
      "y": 0,
      "width": 32,
      "height": 32,
      "pivot_x": 0.5,
      "pivot_y": 1.0
    },
    {
      "id": "walk_01",
      "x": 0,
      "y": 32,
      "width": 32,
      "height": 32,
      "pivot_x": 0.5,
      "pivot_y": 1.0
    },
    {
      "id": "walk_02",
      "x": 32,
      "y": 32,
      "width": 32,
      "height": 32,
      "pivot_x": 0.5,
      "pivot_y": 1.0
    }
  ],
  "animations": [
    {
      "name": "idle",
      "frames": [
        {"sprite_id": "idle_01", "duration": 0.5},
        {"sprite_id": "idle_02", "duration": 0.5}
      ],
      "loop": true
    },
    {
      "name": "walk",
      "frames": [
        {"sprite_id": "walk_01", "duration": 0.25},
        {"sprite_id": "walk_02", "duration": 0.25}
      ],
      "loop": true
    }
  ]
}
```

### Audio Asset Definition (audio.schema.json)
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Audio Asset Definition",
  "type": "object",
  "required": ["version", "name", "file"],
  "properties": {
    "version": {"type": "string"},
    "name": {"type": "string"},
    "file": {"type": "string"},
    "format": {
      "type": "string",
      "enum": ["WAV", "OGG", "MP3"],
      "default": "OGG"
    },
    "volume": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "default": 1.0
    },
    "loop": {"type": "boolean", "default": false},
    "loop_start": {"type": "number", "minimum": 0.0},
    "loop_end": {"type": "number", "minimum": 0.0},
    "tags": {
      "type": "array",
      "items": {"type": "string"}
    }
  }
}
```

## 3. SDL2 Integration Architecture

### Asset Loading System (C11 Implementation)
```c
// asset_manager.h
#ifndef ASSET_MANAGER_H
#define ASSET_MANAGER_H

#include <SDL.h>
#include <cjson/cJSON.h>

typedef struct {
    char* id;
    SDL_Texture* texture;
    int width;
    int height;
    int ref_count;
} AssetTexture;

typedef struct {
    char* id;
    int x, y, width, height;
    float pivot_x, pivot_y;
} SpriteDefinition;

typedef struct {
    char* name;
    AssetTexture* texture;
    SpriteDefinition* sprites;
    size_t sprite_count;
} SpriteSheet;

// Core asset management functions
int asset_manager_init(SDL_Renderer* renderer);
void asset_manager_cleanup(void);
SpriteSheet* load_sprite_sheet(const char* json_path);
SDL_Texture* get_texture(const char* asset_id);
void release_asset(const char* asset_id);

#endif
```

## 4. Cross-Platform Path Handling
```c
// path_utils.h
#ifndef PATH_UTILS_H
#define PATH_UTILS_H

// Cross-platform path utilities
char* get_asset_path(const char* relative_path);
int path_exists(const char* path);
char* normalize_path(const char* path);
void free_path(char* path);

// Platform-specific implementations
#ifdef _WIN32
#define PATH_SEPARATOR "\\"
#else
#define PATH_SEPARATOR "/"
#endif

#endif
```

## 5. Asset Validation System

### JSON Validation Implementation
```c
// asset_validator.h
typedef enum {
    VALIDATION_SUCCESS,
    VALIDATION_FILE_NOT_FOUND,
    VALIDATION_INVALID_JSON,
    VALIDATION_SCHEMA_MISMATCH,
    VALIDATION_MISSING_TEXTURE,
    VALIDATION_INVALID_COORDINATES
} ValidationResult;

ValidationResult validate_sprite_asset(const char* json_path);
ValidationResult validate_audio_asset(const char* json_path);
void print_validation_errors(ValidationResult result, const char* asset_path);
```

## 6. GitHub Actions Integration

### Extended CI Workflow (asset-validation.yml)
```yaml
name: Asset Validation and Testing

on:
  push:
    paths:
      - 'assets/**'
      - 'src/asset_manager/**'
  pull_request:
    paths:
      - 'assets/**'
      - 'src/asset_manager/**'

jobs:
  validate-assets:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]

    steps:
    - uses: actions/checkout@v3

    - name: Setup Python for validation scripts
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install Python dependencies
      run: |
        pip install jsonschema pillow

    - name: Validate JSON schemas
      run: python scripts/validate_assets.py --schema-dir assets/data/schemas --asset-dir assets

    - name: Check asset file integrity
      run: python scripts/check_asset_integrity.py --asset-dir assets

    - name: Validate image formats
      run: python scripts/validate_images.py --image-dir assets/graphics

    - name: Generate asset manifest
      run: python scripts/generate_manifest.py --output-dir build/manifests

    - name: Upload validation results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: asset-validation-${{ matrix.os }}
        path: build/validation-reports/
```

## 7. Asset Processing Scripts

### Python Validation Script (validate_assets.py)
```python
#!/usr/bin/env python3
import json
import jsonschema
import os
import sys
from pathlib import Path

def validate_json_file(json_path, schema_path):
    """Validate a JSON file against its schema."""
    try:
        with open(json_path, 'r') as f:
            data = json.load(f)

        with open(schema_path, 'r') as f:
            schema = json.load(f)

        jsonschema.validate(data, schema)
        return True, None
    except jsonschema.exceptions.ValidationError as e:
        return False, str(e)
    except Exception as e:
        return False, f"Error processing file: {str(e)}"

def main():
    parser = argparse.ArgumentParser(description='Validate asset JSON files')
    parser.add_argument('--schema-dir', required=True, help='Directory containing JSON schemas')
    parser.add_argument('--asset-dir', required=True, help='Root asset directory')
    args = parser.parse_args()

    # Implementation continues...
```

## 8. Performance Optimization Strategies

### Asset Caching System
```c
// asset_cache.h
typedef struct {
    char* key;
    void* data;
    size_t size;
    time_t last_accessed;
    int ref_count;
} CacheEntry;

typedef struct {
    CacheEntry* entries;
    size_t capacity;
    size_t count;
    size_t max_memory;
    size_t current_memory;
} AssetCache;

AssetCache* create_asset_cache(size_t max_memory_mb);
void* cache_get(AssetCache* cache, const char* key);
void cache_put(AssetCache* cache, const char* key, void* data, size_t size);
void cache_cleanup(AssetCache* cache);
```

## 9. Error Handling & Fallbacks

### Missing Asset Handling
```c
// Asset fallback system
typedef struct {
    char* missing_asset_path;
    char* fallback_asset_path;
    int severity; // 0=warning, 1=error, 2=critical
} AssetFallback;

void register_fallback_asset(const char* asset_type, const char* fallback_path);
SDL_Texture* load_texture_with_fallback(const char* primary_path);
void log_missing_asset(const char* asset_path, int severity);
```

## 10. Integration Points

### Engine Integration
- Rendering System: Direct texture access via asset IDs
- Audio System: Background loading and streaming
- Input System: UI asset loading for cursors and overlays
- Game Logic: Data-driven configuration loading

### Memory Management
- Reference counting for shared assets
- Automatic cleanup of unused assets
- Memory pool allocation for small assets
- Streaming for large assets (audio, backgrounds)

## 11. Developer Tools

### Asset Hot-Reloading (Development Mode)
```c
// hot_reload.h
#ifdef DEBUG
void enable_asset_hot_reload(void);
void check_asset_modifications(void);
void reload_modified_assets(void);
#endif
```

### Asset Browser CLI Tool
```bash
# Asset management commands
./asset_tool list --type sprites
./asset_tool validate --path assets/graphics/characters/
./asset_tool optimize --input-dir assets/graphics --output-dir build/optimized
./asset_tool generate-atlas --sprites assets/graphics/sprites/ --output atlas.png
```

## 12. Documentation Requirements

### Asset Creation Guidelines
- Naming conventions for consistency
- Recommended resolutions and formats
- Optimization guidelines for different platforms
- JSON structure documentation with examples
- Common pitfalls and troubleshooting

### API Documentation
- Complete function reference for asset loading
- Performance benchmarks and recommendations
- Platform-specific considerations
- Memory usage guidelines

## Phase 1 Implementation Notes (Completed)
- Added asset classification module with 14 coarse enum categories; no allocations; O(n) over path length; safe on mixed separators (normalizes during token scan).
- Unit test covers representative paths for each category + unknown fallback.
- Roadmap advanced to Phase 2 tasks (JSON schema design) – pending implementation.
- Next actionable tasks: Phase 2 schema authoring + lightweight validator wrapper integrating with existing `json_schema` utility (will reuse envelope patterns from items/skills schemas).

This implementation plan provides a comprehensive, scalable asset management system that integrates seamlessly with SDL2 and maintains cross-platform compatibility while supporting the needs of a growing game project.
