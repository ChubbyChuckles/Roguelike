DEBUG OVERLAY UI/UX IMPROVEMENTS PLAN (Authoring-first, Modern UX)

Context and constraints
- Runtime: SDL2 renderer/input, immediate-mode UI in C, headless-safe APIs, MSVC toolchain (no VLAs; compile-time constants only).
- Existing: Panels for System, Skills, Items, Entities, Map, Audio/VFX; Content Graph visualization with exports; validation manager; JSON schemas and round-trips; live reload.
- Primary goal: Make adding/editing all asset types fast, safe, and pleasant while keeping overlay performance high and headless tests stable.

Guiding principles
- Authoring-first: Minimize clicks and modality. Offer smart defaults, templates, and fix-it actions.
- Always in context: Inline validation, previews, and references near the editor controls.
- Keyboard-first: Every action has a shortcut; full focus traversal with clear affordances.
- Non-blocking: Background I/O with progress; errors surface as toasts, not modal stops.
- Style consistency: Single theme system controls spacing, colors, typography, and DPI.

Milestone 0 — Foundations refresh (pre-work)
0.1 Overlay theme (overlay_theme.{h,c})
  - Centralize colors, spacing, radii, typography scale, and icon palette.
  - Provide 3 presets: Dark (default), Light, High-Contrast; runtime switch + persisted choice.
  - DPI scaling factor and font size ladder. Acceptance: All widgets render with theme constants; no hard-coded colors left in panels.
  - Status: Runtime setters for preset/DPI/font size implemented and exposed in System panel (persisted in overlay_theme.json). Core widgets migrated: Combo label text, Tree node (bg/border/text accent), and Color swatch border. Panels migrated to theme colors: System, Items, Skills (Testing + Effects Node Graph canvas/nodes/edges/labels), Content Graph previews, Map (brush ghost, collider outlines, pathfinding heatmap, autotiling hints), Audio/VFX (falloff ring gizmo), and Entities (world‑space selection gizmo ring/rect+crosshair around the selected entity). Remaining minor widgets/gizmos will be audited as we touch them.
0.2 Layout primitives
  - Resizable columns/splitter, collapsible sections, and tab bar. Persist per-panel layout.
  - Status: Splitter implemented in widgets and wired to overlay_prefs persistence. Items panel refactored earlier; Skills and Map panels now adopt persistent split layouts (left navigation/controls, right content/preview placeholder). Sizes persist across restarts.
  - Acceptance: Items/Skills/Map panels adopt split layouts; sizes persist across restarts. (Met)
0.3 Notification/toast system
  - Non-modal info/warn/error toasts with auto-dismiss and “Details…” link.
  - Acceptance: Save/Load, Auto-reload, and Validation surface through toasts.
  - Status: Implemented. Overlay now renders a non-blocking toast lane with info/warn/error kinds and per-toast TTL. Wired across:
    • Skills panel (Save/Load Overrides, Auto-Reload toggles and activity summaries)
    • Validation panel (run start/completion with summarized results, severity-based color)
    • Command Palette actions (Validation: Run Now, Skills: Save/Load)
    • Core render loop calls overlay_toast_render() and is headless-safe (no SDL calls when no renderer present)
  Verification: Debug SDL2 build with -j12; full CTest suite all green (594/594). Latest pass after Map + Audio/VFX + Entities selection gizmo theming also 100% green.

Milestone 1 — Navigation and discoverability
1.1 Global search (Ctrl+K) and fuzzy finder
  - Search assets across all registries by id/name/tags; jump-to panel + selection.
  - Recent assets and Favorites (star/pin) sections. Acceptance: <200ms results on 10k items via prefix index or substring scan with time budget + pagination.
  - Status: Initial Global Search implemented. Ctrl+K toggles a themed popup with an input field and a results list covering Items and Skills (case-insensitive substring). Keyboard Up/Down move selection; Enter triggers jump-to via overlay_nav helpers which open the target panel and programmatically select the entry. Items/Skills panels now expose deferred selection setters that scroll the virtualized table to bring the row into view. Build integration added (overlay_search.c compiled into overlay). Verification: Debug SDL2 full suite passed 100% (594/594) with -j12; manual smoke validated jump-to behavior.
1.2 Command Palette (Ctrl+Shift+P)
  - Fuzzy action picker for overlay commands (e.g., “Create Item”, “Export Subgraph JSON”, “Run Validation”).
  - Status: Implemented minimal Command Palette with default commands and a simple substring filter.
    Toggle with Ctrl+Shift+P. Default commands include: Validation run/show panel, Skills save/load overrides,
    Content Graph DOT export, and open Items/Skills/Map panels. Actions are callable headless via overlay_invoke_action.
  - Acceptance: Extensible registry; actions callable headless via overlay_invoke_action (Met for stub milestone).
1.3 Breadcrumbs and history
  - Breadcrumb trail per panel (e.g., Items > Swords > iron_sabre). Back/Forward history with Alt+Left/Right.
  - Status: Implemented. Overlay now maintains a bounded navigation history (ring buffer, 64 entries) with Back/Forward via Alt+Left/Right. A breadcrumb header renders at the top of supporting panels. Items and Skills panels record selection changes using a lightweight `OverlayNavState` (panel id, selected index, and up to three crumb segments) via `overlay_nav_set_current`. Programmatic navigation (e.g., Global Search Enter) uses `overlay_nav_open_items_and_select` / `overlay_nav_open_skills_and_select`, which push history after opening + selecting. Deferred selection is honored and rows auto-scroll into view in virtualized tables. Input capture gained an Alt modifier flag in the per-frame input snapshot.
  - Acceptance: History accurately replays panel + selection across Back/Forward; breadcrumb updates live; Global Search jump-to entries appear in history. Met.
  - Verification: Built Debug (SDL2) with parallelism (-j12) and ran the full CTest suite: 100% PASS (594/594). Manual smoke validated Back/Forward and breadcrumb updates in Items/Skills.

Milestone 2 — Authoring accelerators (all asset types)
2.1 Creation wizards (guided forms)
  - Items, Skills, Entities, Tilesets/Maps, Audio/VFX, Dialogue, Vendors, Loot Tables.
  - Smart defaults from templates; required-field gating; inline validation badges.
  - Acceptance: “New …” flow completes without touching JSON; invalid fields show error text; Save disabled until valid.
  - Status: Items panel implements a schema-backed "Create New Item" wizard with required fields, clamping, and inline messages. “Duplicate From Selected” prefills from the current row and suggests a unique id. Other asset types pending.
2.2 Templates and presets
  - Per-type template store (JSON fragments) + “Save as Template”.
  - Quick-pick presets from recent and curated sets. Acceptance: Duplicate from template in one step.
  - Status: Implemented for Items panel. Curated presets exposed in-panel plus file-backed templates with Save/Load/Apply. Templates are stored under `build/templates/items/*.json`; filenames are sanitized. Persistence uses json_io atomic writes and a cross‑platform ensure-dir helper. UI surfaces success/failure via headless‑safe toasts.
  - Verification: Debug (SDL2) build and full CTest run from the build dir passed 100% (594/594) with `-j12`.
2.3 Duplicate-as-template
  - One-click “Duplicate” opens wizard with fields pre-filled and a new id suggestion.
  - Status: Implemented for Items panel (prefill + unique-id suggestion), Skills panel (prefill from current selection; name gets a _Copy suffix; max-rank/passive/timing carried over; optional “Copy Coeffs” to duplicate coefficients), and Entities panel (duplicate selected enemy with configurable dx/dy offset via a headless‑safe API `rogue_entity_debug_duplicate`).
  - Verification: Debug (SDL2) build and full CTest run from the build dir passed 100% (594/594) with -j12 after Entities duplicate wiring.
2.4 Multi-asset batch creator
  - CSV/TSV import or pattern-based expansion (e.g., name_{1..5}). Acceptance: Preview table before commit; validates all rows.
  - Status: Pattern-based batch creators implemented for Items and Skills with a live preview table and per‑row validation (invalid format, bounds, and duplicate checks). Items: ID/Name printf‑style formats with Start/Count; Skills: Name printf‑style format with Start/Count plus Passive/Max‑Rank/timing fields. Entities ring‑spawn slice remains for spawn testing. All paths are headless‑safe and surface results via toasts/summary.
  - Verification: Debug (SDL2) build with -j12 and full CTest run passed 100% (594/594) on Windows/MSVC after these changes.
  - Next: Add CSV/TSV import pipeline and a generic cross‑type preview/validation framework; extend Duplicate‑as‑Template to VFX.

Milestone 3 — Inline editing and validation
3.1 Schema-aware editors
  - Enum dropdowns, range sliders with min/max from schema, tag pickers with autocomplete, file pickers with existence check.
  - Acceptance: No raw numbers for enums/ranges in the main editor; helpers fall back to InputText when schema absent.
3.2 Side-by-side JSON and Form view
  - Two-way binding; dirty markers per-field; diff viewer before save.
  - Acceptance: Changing JSON updates form within one frame; form edits serialize to JSON preview.
3.3 Cross-reference validation and fix-its
  - Show missing/invalid refs inline; offer quick “Create missing asset” or “Replace with closest match”.
  - Acceptance: Clicking fix-it resolves the validation and marks doc dirty.
3.4 Auto-save drafts and recovery
  - Per-asset draft file with debounce (e.g., 1.5s after last edit). “Restore draft?” toast on reopen. Acceptance: No lost edits on crash.

Milestone 4 — Live previews and inspectors
4.1 Items preview
  - Icon/sprite swatch; rarity color; sockets UI; vendor price impact; equip simulation snapshot.
4.2 Skills preview
  - Timeline (cast/channel/cooldown), damage tick graph, effect spec list; quick “Simulate” with deterministic seed.
4.3 Audio/VFX preview
  - Spatial audition at cursor; gizmo toggles; quick attenuation curve view.
4.4 Map/editor preview upgrades
  - Brush ghost, colliders overlay, pathfinding heatmap, tile autotiling hints. Undo/redo for map edits (local ring buffer).
4.5 Dialogue preview
  - Branch graph mini-view and rich text renderer with styles; test conversation runner.
Acceptance: Previews update in real time when fields change; 60 FPS budget maintained; heavy calcs done off-thread with snapshot handoff.

Status (M4 progress)
- 4.1 Items preview: Implemented headless-safe textual preview in Items Create wizard. Renders Category, Rarity, Level, Base Stats (Damage dmin–dmax or Armor), Sockets (min..max), and an informational Est. Value heuristic. Placed after socket sliders, before Duplicate/Create. Verification: Debug (SDL2) build and full CTest passed 100% (594/594) with -j12.
- 4.2 Skills preview: Added headless-safe textual “Skills Live Preview” to the Testing tab. Shows Skill name/type, Timeline (Cast, Cooldown), Coeff summary, Primary Effect kind/magnitude, Effect Nodes with delays/repeats, and a rough estimated total tick count derived from repeats/periods. This complements the existing real-time sprite preview controls. Verification: Debug (SDL2) build and full CTest suite remains all‑green (594/594) with -j12.
- 4.3 Audio/VFX preview: Implemented headless‑safe textual and lightweight visual previews in the Audio/VFX panel.
  • Controls: “Enable Positional Audio,” “Listener follows player,” and “Falloff Radius (tiles).”
  • Action: “Play Sound @ Cursor” auditions via the FX bus (rogue_fx_emit) at the cursor’s world position using screen→world mapping (camera/tile size).
  • Textual: ASCII “Attenuation Preview” samples rogue_audio_debug_effective_gain at 0/25/50/75/100% of the falloff radius.
  • Visual: Optional falloff ring gizmo near the listener/player; SDL‑guarded to keep headless tests stable.
  Verification: Debug (SDL2) build and full CTest run passed 100% (594/594) with -j12. No regressions.

- 4.4 Map/editor preview upgrades: Implemented brush ghost, colliders overlay, pathfinding heatmap, tile autotiling hints, and a local undo/redo ring buffer for map edits.
  • Brush Ghost: shows the current brush footprint under the cursor (square/rect), SDL‑guarded.
  • Colliders Overlay: togglable overlay rendering solid tiles/collision bounds for quick triage.
  • Pathfinding Heatmap: samples nav costs/blocked tiles and visualizes them as a heatmap (headless‑safe textual fallbacks when SDL not present).
  • Autotiling Hints: lightweight hints highlight tiles affected by autotile rules for the current placement.
  • Undo/Redo: 64‑entry ring buffer with Clear; edits invalidate the tile sprite LUT for immediate refresh. Bound to Ctrl+Z / Ctrl+Y in the panel with buttons as well.
  Verification: Built Debug (SDL2) with -j12 and ran full CTest from the build dir: 100% PASS (594/594). No regressions.

- 4.5 Dialogue preview: Added a new headless‑safe Dialogue panel with a textual branch mini‑graph, a simple conversation runner, and a style inspector.
  • Panel: registered as "dialogue" and wired into the overlay panels aggregator and CMake overlay target.
  • Conversation runner: Start/Advance/Reset controls drive `rogue_dialogue_start/advance`; the current line text displays live via `rogue_dialogue_current_text`.
  • Branch mini‑graph: lists lines with an ASCII chain (e.g., "0 -> 1 -> 2") and highlights the current line; updates on each change.
  • Style inspector: optional editor surfaces key style properties via `rogue_dialogue_style_get/set` (text/color/size knobs kept lightweight for MSVC C mode) and applies immediately.
  • Sample script: panel auto‑registers a built‑in sample (id 101) via `rogue_dialogue_register_from_buffer` when no dialogue exists so the preview works out‑of‑the‑box.
  Headless safety: all visuals are textual with SDL guards; no renderer calls from tests.
  Verification: Debug SDL2 build with -j12 succeeded; full CTest suite passed 100% (594/594) from the build directory with `--output-on-failure`.

Milestone 5 — Content Graph and diagnostics polish
5.1 Pan/zoom and focus tools
  - Mouse wheel zoom, right-drag pan, fit-to-selection, isolate subgraph, pin nodes.
5.2 Layout and highlighting
  - Layered/force-directed hybrid; emphasize cycles and long dependency chains; group halos; path “explain why” breadcrumbs.
5.3 Finders
  - Orphans (no inbound), missing providers (dangling), large hubs, and path conflicts. Batch action: open editors or export reports.
5.4 Exports and diffing
  - Compare snapshots; color edges added/removed; export focused diffs. Acceptance: JSON diff includes node degree deltas and cycle list.

Status (M5 progress)
- 5.1 Pan/zoom and focus tools: Implemented core interactions in the Content Graph SDL mini‑view and list.
  • Interactions: Right‑mouse drag to pan, mouse wheel zoom centered on cursor (clamped 0.5×–2.0×), and Fit‑to‑Selection via button or F key.
  • Isolate subgraph: Optional filter limits the list to nodes reachable from the current selection (BFS up to the current Preview Depth). Selection persists across frames and clicking nodes in the mini‑view drills selection and updates a breadcrumb trail.
  • Hints: On‑screen hint text renders inside the mini‑view (RMB drag=pan, Wheel=zoom, F=fit). Drawing paths are SDL‑guarded for headless tests.
  • Node pinning/manual repositioning: Implemented with Shift+LMB drag to pin/move nodes and “Clear Pins” command to reset. Persistence is session-only.
  Verification: Debug build (SDL2) with parallelism (-j12) and full CTest run passed 100% (594/594) from the build directory.

Milestone 6 — Style, accessibility, and micro-interactions
6.1 Visual language
  - Consistent spacing, hover/pressed/focus states, checkbox/radio sizes, accessible contrast. Tooltip system with rich content.
6.2 Icons
  - Bitmap icon atlas or icon font subset; use on tabs, headers, and buttons. Acceptance: No text-only controls for frequent actions.
6.3 Keyboard coverage and cheat sheet
  - F1 toggles overlay; ? opens “Shortcuts” panel; Alt+number to switch tabs; Ctrl+S save; Ctrl+Z/Y undo/redo; Esc clears focus.
6.4 Colorblind-safe palette
  - Optional mode remaps accent colors; ensure graph and heatmaps are distinguishable.

Status (M6 progress)
- 6.1 Visual language: Implemented micro-interactions across core widgets with accessible focus states.
  • Widgets updated: Button, Checkbox, SliderInt/Float, InputText, Combo, and Tree Node.
  • Visuals: subtle hover shading, darker pressed state, and clear focus rings using theme accents (accent_1/accent_2). Uses a small shade helper to adjust brightness without altering the theme state; respects the Colorblind-safe palette.
  • Tree Node: provided a concrete implementation used by Map, Validation, and Content Graph panels. Renders a collapsible header row with an arrow indicator (▸/▾), accepts mouse clicks and Enter/Space, and shows a focus ring when navigated via keyboard.
  • Headless safety: all draw paths are SDL-guarded; logic remains testable in headless runs.
  • Tooltips: Added a minimal, headless-safe tooltip system. Panels can attach text to the next widget via overlay_set_next_tooltip(); on ~400ms hover the tooltip appears near the cursor with theme styling. Core widgets (Button, Checkbox, Sliders, InputText, Combo, Tree Node) ingest the next-tooltip automatically and register their hover rects each frame.
  Verification: Debug (SDL2) build with -j12 and full CTest from the build directory passed 100% (594/594).
 - 6.2 Icons: Introduced a small, themed icon system and adopted icon buttons for common actions.
  • Module: src/debug_overlay/overlay_icon.{h,c} defines an OverlayIcon enum (SAVE, UNDO, REDO, PLAY, SEARCH, …) and overlay_icon_draw(). Icons are 12×12 monochrome bitmaps tinted with the theme accent; all SDL calls are guarded for headless tests.
  • Widget: overlay_icon_button(label, icon) renders a standard button with a left-aligned icon, consistent paddings, hover/pressed/focus states, and built‑in tooltip consumption.
  • Integration: Map panel action rows now use icon buttons for Undo/Redo and Save/Load; behavior is unchanged and labels remain for clarity.
  • Build: overlay_icon.c added to the overlay target so the module is compiled in all builds.
  Verification: Built Debug (SDL2) with -j12 and ran the full CTest suite from the build directory: 100% PASS (594/594). No regressions.
- 6.3 Keyboard coverage and cheat sheet: Expanded coverage to include Esc clear focus, Ctrl+S save, and Ctrl+Z/Y in Map.
  • Input: per-frame input snapshot extended to capture '?', number keys 1..9 (with Alt), and S/Z/Y for Ctrl+S/Z/Y.
  • Shortcuts panel: updated cheat sheet now documents Esc, Ctrl+S (contextual), and Ctrl+Z/Y (Map editor).
  • Global shortcuts:
    - '?' opens the Shortcuts panel anywhere in the overlay.
    - Alt+1..9 open common panels in order: 1 System, 2 Items, 3 Skills, 4 Map, 5 Audio/VFX, 6 Entities, 7 Content Graph, 8 Validation, 9 Dialogue.
    - Esc clears widget focus across panels (resets g_ui.focus_index).
    - Ctrl+S triggers contextual saves: Skills panel saves overrides (and refreshes), Map panel saves to build/map.json.
    - Ctrl+Z / Ctrl+Y route to Map editor undo/redo when the Map panel is visible.
  • Registration/Build: panel registered in overlay_panels and compiled into the overlay target.
  • Next: generalize Ctrl+S plumbing for Items/Dialogue editors as save paths are added; extend Ctrl+Z/Y to other editors as they gain undo stacks.
  Verification: Debug (SDL2) build with -j12 succeeded; full CTest suite passed 100% (594/594) from the build directory. No regressions observed.

- 6.4 Colorblind-safe palette: Implemented an accessibility toggle that remaps accent colors and raises toast contrast for better distinguishability.
  • Theme state: added a persisted boolean (overlay_theme.colorblind_mode) with getters/setters `overlay_theme_set_colorblind(int)` and `overlay_theme_get_colorblind(void)`.
  • Persistence: `build/overlay_theme.json` now includes a `"colorblind"` field; on load we re-apply the current preset to ensure remapped accents/toast colors take effect immediately.
  • System panel UI: new checkbox "Colorblind-safe palette" reflects and updates the theme setting at runtime.
  • Visuals: when enabled, accents shift to a blue/orange pairing and warn/error toasts use higher-contrast tones in dark mode; SDL paths remain guarded for headless tests.
  Verification: Built Debug (SDL2) with -j12 and ran full CTest from the build dir: 100% PASS (594/594). No regressions.

Milestone 7 — Persistence, safety, and collaboration
7.1 Undo/redo (local session)
  - Command stack per panel with memory cap. Acceptance: 50 steps typical; integrates with map edits and JSON form changes.
7.2 Save pipeline safeguards
  - Pre-save validation gate; atomic write; preflight conflicts (id/path) with fix-it prompts.
7.3 Content pack import/export
  - Bundle selected assets as a pack; import with conflict resolution: keep, rename, or replace. Acceptance: Dry-run preview, counts per type.
7.4 VCS helpers (optional)
  - “Reveal in Explorer” and “Copy path”; optional post-save hook to run formatter or git add.

Milestone 8 — Performance and robustness
8.1 Virtualized lists/tables
  - Only render visible rows; smooth scroll. Acceptance: 100k-row table stays >60 FPS.
  - Status: Items panel now supports virtualized rows with multiple inputs:
    • Mouse wheel over table (hover-aware consumption)
    • Keyboard Up/Down and Home/End
    • Shift to accelerate scroll step
    • Row-offset slider (kept for headless determinism)
  • Visible vertical scrollbar with click-to-page (track) and drag-to-scroll (thumb); synchronized with virtualization and clamped to bounds.
  FPS metrics on 20k–100k sets: initial headless perf smoke added (tests/unit/test_overlay_table_perf_smoke.c) measuring avg ms/frame over 200 frames on a 50k-row synthetic table. Tuned defaults adopted based on results (row height 16px, padding 1px vs prior 18px/2px), yielding ~4–5% lower avg ms/frame in Debug SDL2 locally. Sliders allow per-user override and persist via overlay_prefs. Verification: full CTest run post-change passed 594/594 (Debug, -j12) with no regressions.
8.2 Background job system
  - Worker pool for validation, hash compute, heavy exports; progress HUD with cancel.
8.3 Memory discipline
  - Arena/pool allocators for UI; hot paths avoid heap churn; capture zero when overlay disabled.
8.4 Instrumentation
  - Overlay perf panel: widget timings, draw calls, input latency; redlines when budgets exceeded.

Milestone 9 — Extensibility and testing
9.1 Action API
  - overlay_invoke_action(name, args) to trigger commands programmatically (headless-safe). Acceptance: Used by tests to open panels, select assets, and save.
9.2 Test fakes and snapshots
  - In-memory FS and renderer shims; snapshot tests for panel states; keyboard navigation simulation.
9.3 Stability guard
  - Flake-resistant timers; deterministic seeds; timeouts with retries for file mtime on Windows.

Implementation notes and constraints
- MSVC: use compile-time caps (e.g., OVERLAY_*_MAX_*) for buffers; avoid VLAs. Ensure declaration-before-statement compliance in C sources; panel_skills_effects.c adjusted by moving typedefs/static state to the top of the relevant block.
- Input: prefer “clicked” over “pressed” for discrete actions; capture input on focus.
- Headless: keep logic separated from rendering; all commands callable without SDL.
- Filesystem: atomic writes; centralize save paths; per-test isolation for units.
- Style: no hard-coded literals in panels—only through overlay_theme.

Suggested sequencing and effort
- 0. Foundations (3–5 days)
- 1. Navigation (3–4 days)
- 2. Authoring (7–10 days)
- 3. Inline/validation (5–7 days)
- 4. Previews (7–12 days)
- 5. Graph polish (4–6 days)
- 6. Style/accessibility (3–5 days)
- 7. Persistence/collab (5–8 days)
- 8. Performance (3–5 days, ongoing)
- 9. Extensibility/tests (4–6 days)

Acceptance criteria summary
- All major asset types can be created/edited/saved without touching raw JSON.
- Global search/command palette navigate anywhere within 1–2 keystrokes on average.
- Validation is always visible and fixable in-place; no modal dead-ends.
- Overlay remains responsive (>60 FPS) on large content sets; heavy work off-thread.
- Theming and shortcuts are consistent; settings persist; headless tests cover core flows.

Risk and mitigations
- Large content sets: use virtualized lists and incremental search; paginate when needed.
- Windows mtime quirks: keep existing robust mtime helpers; add backoff on save/reload.
- Visual complexity: ship with sensible defaults and progressive disclosure (collapsibles).
- Tech debt: codify UI constants early (overlay_theme); avoid ad-hoc styles.

Near-term next steps (actionable)
1) Implement overlay_theme and migrate System + Items panels to use it.
2) Splitters done: Items/Skills/Map now split with persisted sizes; toasts are already available and used in Items stub.
3) DONE: Items “Create New” wizard powered by schema; Duplicate-as-template integrated.
4) DONE: Virtualized table in Items list with row-offset slider and smooth navigation via mouse wheel (hover-aware), Up/Down, Home/End, and Shift-accelerated steps, plus a visual scrollbar (drag to scroll, click track to page). Next: remaining theming migration across panels; consider pixel-level smoothing.
5) DONE: Items list filtering and sort toggles (id/name/category/rarity); expose “Create Item” via Command Palette args to open the wizard focused.
  - Case-insensitive substring filter on id/name; header clicks toggle ascending/descending sort.
  - Keyboard Up/Down scroll the virtualized list; selection stays in view. Command: “Items: Create New”.
6) Begin overlay_theme extraction and migrate System + Items to theme constants (colors/spacing/typography). Document quick preset switch and persistence.
  - Progress: System panel adds Theme Preset, DPI, and Font Size controls using overlay_theme setters; selections persist. Core widget colors migrated; additional panels now consume theme colors (Skills Testing, Skills Effects Node Graph, and Content Graph). Remaining panels queued.

Verification
- Build: Debug (SDL2), parallel -j12.
- Tests: Full CTest run passed 100% (594/594) on Windows/MSVC after theme control additions; no regressions observed.
