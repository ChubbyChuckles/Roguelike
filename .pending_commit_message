Overlay: Content Graph polish — group-based tinting in SDL preview; surfaced last registration rejection diagnostics (cycle/path_conflict) via new asset_dep APIs (rogue_asset_dep_get_last_reject/clear). Docs/roadmap updated.

Persistence tests: fix test_save_incremental_basic to honor per-test save directory by using rogue_build_slot_path(0) from save_paths; resolves size_mismatch flake under parallel ctest -j12.

Gates: Rebuilt Debug (SDL2) with -j12 and re-ran full suite — all green (581/581).Phase 13.3 – Content Graph overlay complete: advanced filters (id:/path:/group:/hash:/dep:/rev:), reverse deps, group summaries, path-collision markers, DOT export, and layered SDL multi-hop preview with depth slider. Added rogue_skills_set_skip_icon_loads() and refactored two slow tests to millisecond runtime. Gate enforced: Debug build with SDL2 and ctest -j12 all green (581/581).

Phase 13.3: Content Graph click-to-drill + breadcrumbs

- Overlay: Added interactive click-to-drill in the SDL layered Content Graph preview.
  - Click any node box to focus it; selection updates or filter pivots to id:<clicked>.
  - Breadcrumbs show the path from current root to the clicked node with Back navigation.
  - Built a lightweight parent map from the BFS edge set; no allocations.
- Docs: Updated roadmaps/debugging_overlay.txt to document click-to-drill and breadcrumbs.
- Discipline: Rebuild Debug (SDL2) with -j12 and run full tests.

Content Graph: add Export JSON action (writes build/content_graph.json) alongside DOT export; rebuilt Debug (SDL2) -j12 and ran full test suite — 100% green (581/581).

Content Graph: focused subgraph exports + cycle-edge indicator

- Overlay: Added “Export Subgraph DOT” (build/content_subgraph.dot) and “Export Subgraph JSON” (build/content_subgraph.json) limited to the currently selected root and the SDL preview depth via BFS.
- SDL preview: cycle visualization — when the last registration reject was a cycle and both endpoints are visible in the preview, draw the connecting edge in red for immediate diagnostics.
- Docs: Updated README and roadmap to document subgraph exports and the red cycle-edge.
- Gates: Rebuilt Debug (SDL2) with -j12 and ran full test suite — all green (581/581).
