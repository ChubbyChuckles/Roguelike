feat(hitbox/phase4.1-advisory): wire temporal predictor as metrics-only advisory stage + unit test; 717/717 green

- Integrate header-only temporal coherence predictor into `collision_pipeline` via `rogue_collision_stage_temporal_advisory` (metrics-only). The stage touches pairs against a primary id and records conservative predictions without mutating candidate sets or affecting behavior.
- Add helper APIs `rogue_collision_advisory_reset(sep_thresh_px)` and `rogue_collision_advisory_get_metrics(&predicts,&updates)`; context gains `advisory_enabled` and `advisory_primary_*` fields.
- New unit test `test_collision_temporal_advisory` validates invariance of `candidate_count` and growth of advisory metrics across runs.
- Roadmap updated (Milestone 4.1) to reflect advisory wiring + test; README documents API and metrics-only posture.
- Validation: Full Debug build with SDL2 and parallel ctests (-j12) passed 100% (717/717).
- ci(windows): fix configure failure from stale vcpkg toolchain

	- Windows job now purges cached CMake files (`build/CMakeCache.txt`, `build/CMakeFiles/**`) before configure to avoid inheriting `CMAKE_TOOLCHAIN_FILE` from a prior run.
	- Explicitly clears toolchain-related CMake cache vars in configure: `-DCMAKE_TOOLCHAIN_FILE= -DVCPKG_ROOT= -DVCPKG_TARGET_TRIPLET=`.
	- Keeps vendor-only SDL2 policy intact; Linux/macOS unchanged. This resolves errors like:
		`include could not find requested file: D:/a/_temp/vcpkg/scripts/buildsystems/vcpkg.cmake`.

ci(build/windows): enforce vendored SDL2 only; determinism + SIMD parity; Phase 8.3 worldgen hardening; Release 716/716

- Windows builds/tests now use only the vendored SDL2 (SDL2/SDL2_image/SDL2_mixer) under third_party; manual-root discovery with NO_DEFAULT_PATH prevents system leakage. vcpkg coupling fully removed from build/CI.
- Collision pipeline determinism strengthened: order-independent temporal cache (<=5px drift), AABB prefilter priority ordering with stable cap=128, conservative +0.5px broad-phase epsilon, and lightweight deterministic BV prepass.
- SIMD parity: SSE2 batching for hbroad and SSE2 sort-key path for AABB prefilter behind `rogue_collision_simd_set_enabled(0|1)`; new tests assert scalar vs SIMD identical ordering/counts.
- Worldgen Phase 8.3: hardened upgrade-guarantee placement with strict bounds/overlay guards and a safe fallback scan; override-aware smart-drop planning; targeted test passes 10× deterministically.
- Validation: Full Release suite on Windows ran with -j12 and passed 716/716; targeted upgrade test also stable. README and roadmap updated to reflect vendor-only SDL2 policy and worldgen stability notes.

fix(collision/temporal-cache): refresh snapshot after AABB prefilter to avoid order-sensitive misses; add priority ordering + temporal sweep; 715/715 green

- Temporal cache now snapshots after the AABB prefilter reorders/caps candidates, so the next frame’s validation compares against the exact downstream sequence. This prevents false cache misses when spatial compaction order differs.
- AABB prefilter gains deterministic priority ordering: on‑screen first, then squared distance to view center, with id tie‑break; cap enforced to 128.
- Hierarchical broad phase includes a small temporal AABB sweep (~16ms horizon) to retain fast movers and mitigate tunneling in broad-phase pruning.
- Tests: `test_collision_pipeline_phase2_1_temporal` now passes consistently; added `test_collision_pipeline_phase2_1_temporal_aabb_priority` to validate sweep + ordering.
- Verified on Debug (SDL2, -j12): full suite 100% green (715/715).

feat(collision-pipeline): enforce per-stage max_candidates and add unit test; 713/713 green (SDL2, -j12)

- Enforce `RogueCollisionStage.max_candidates` after each stage in `rogue_collision_pipeline_execute`.
- Metrics now reflect caps (`metrics.output_candidates`), and downstream stages receive the capped input.
- New unit test `test_collision_pipeline_phase2_1_stage_cap` validates cap behavior and metrics/input propagation.
- Verified with Debug SDL2 build and ctest -j12: 100% green (713/713).

feat(collision-temporal): opt-in honor mode + extended metrics; stabilize Debug ctests build (MSVC); 719/719 green

- Add `rogue_collision_advisory_set_honor_mode(1)` to allow honoring conservative skip suggestions from the temporal predictor; default OFF preserves existing behavior (metrics-only) unless explicitly enabled.
- Extend metrics via `rogue_collision_advisory_get_extended(&hit_rate, skip_hist[4], &min_cands, &max_cands, &avg_cands)`. Keep `rogue_collision_advisory_reset(sep_thresh_px)` and `rogue_collision_advisory_get_metrics` for simple counters.
- New unit `test_collision_temporal_advisory_parity` ensures determinism parity when advisory is enabled but honor mode is OFF (no candidate count/order change).
- CMake/MSVC multi-config stabilization: pre-create per-config test runtime dirs and add an aggregate target that builds all test executables by default to eliminate CTest “Not Run” cases in Debug.
- Validation: Full Debug run is 100% green with -j12 (719/719); Release remains green.
