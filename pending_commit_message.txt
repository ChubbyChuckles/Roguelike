fix(collision/temporal-cache): refresh snapshot after AABB prefilter to avoid order-sensitive misses; add priority ordering + temporal sweep; 715/715 green

- Temporal cache now snapshots after the AABB prefilter reorders/caps candidates, so the next frame’s validation compares against the exact downstream sequence. This prevents false cache misses when spatial compaction order differs.
- AABB prefilter gains deterministic priority ordering: on‑screen first, then squared distance to view center, with id tie‑break; cap enforced to 128.
- Hierarchical broad phase includes a small temporal AABB sweep (~16ms horizon) to retain fast movers and mitigate tunneling in broad-phase pruning.
- Tests: `test_collision_pipeline_phase2_1_temporal` now passes consistently; added `test_collision_pipeline_phase2_1_temporal_aabb_priority` to validate sweep + ordering.
- Verified on Debug (SDL2, -j12): full suite 100% green (715/715).

feat(collision-pipeline): enforce per-stage max_candidates and add unit test; 713/713 green (SDL2, -j12)

- Enforce `RogueCollisionStage.max_candidates` after each stage in `rogue_collision_pipeline_execute`.
- Metrics now reflect caps (`metrics.output_candidates`), and downstream stages receive the capped input.
- New unit test `test_collision_pipeline_phase2_1_stage_cap` validates cap behavior and metrics/input propagation.
- Verified with Debug SDL2 build and ctest -j12: 100% green (713/713).

feat(animation-collision-overlap): deterministic overlap resolution strategies + scaled wrapper; tests; 712/712 green

- Implement `rogue_animation_collision_resolve_overlap` and `_scaled` to select a single active window when overlaps occur.
- Both strategies are intensity-first with clear tie-breakers for determinism:
	- Highest-Intensity Latest-Start, then Lowest Index
	- Highest-Intensity Earliest-Start, then Lowest Index
- Scaled variant applies `playback_speed` to the time domain to mirror scaled evaluation.
- Add unit `test_animation_collision_sync_overlap` covering both strategies and scaled-time parity (e.g., speed=2 maps 30ms real → 60ms timeline).
- Full Debug (SDL2, -j12) ctest passed 100% (712/712).

fix(animation-collision-cache): boundary detection operates in scaled time; add unit test; 710/710 green

- Cached evaluation (`rogue_animation_collision_evaluate_timeline_cached`) now multiplies both previous and current times by `playback_speed` for boundary checks to avoid missing ENTER/EXIT at speeds > 1 with small deltas.
- Added `test_animation_collision_sync_cache_scale` to validate cache invalidation/activation under playback_speed=2.
- Full Debug (SDL2, -j12) ctest: 100% pass (710/710).

feat(animation-collision-sync): add quintic smootherstep mode (quality >=0.9) + tests

- Interpolation quality modes refined: keep cubic smoothstep for quality >=0.5 and introduce quintic smootherstep (6t^5-15t^4+10t^3) when quality >=0.9 for smoother easing.
- Unit test `test_animation_collision_sync_quality` verifies cubic/quintic factors at t=0.25/0.75; CMake wired.
- Readme/Roadmap updated; full rebuild + ctests planned with SDL2 and -j12.

feat(animation-sync): ordered timeline events + scaled wrapper, add unit test

- Ensure `rogue_animation_collision_timeline_events` emits events in chronological order and breaks ties with ENTER before EXIT at the same timestamp (e.g., window A ends exactly when window B begins).
- Add `rogue_animation_collision_timeline_events_scaled` to mirror speed-scaled evaluation semantics by applying `playback_speed` to the event time domain.
- New test `test_animation_collision_sync_events` covers tie-ordering and scaled interval parity.
- Full Debug build (SDL2, -j12) and ctest: 100% green (708/708).

feat(hitbox/phase2.3): enemy collision LOD stage integration + pixel-perfect stage baseline

feat(hitbox/phase3.1): skill collision manager initial implementation

Added skill_collision_manager.{h,c} providing Milestone 3.1 multi-layer skill effect collision system:\n - Up to 4 layers per effect with independent start/duration & bitmask filtering\n - 8-point intensity curves (linear interpolation, default flat if all zeros)\n - Piercing vs non-piercing and max target caps per layer\n - Deterministic headless evaluation producing hit records (id, layer, time, intensity)\n - Minimal tick-driven lifecycle & completion flag\n - Unit test (test_skill_collision_manager_phase3_1) covering activation scheduling, filtering, piercing, max targets, intensity sampling, completion\n\nRoadmap (implementation_hitbox_rework.txt) now marked complete for Milestone 3.1 (initial slice); README updated with Phase 3 progress note.

feat(hitbox/phase3.1-deepening): projectile + frame interpolation scaffold

- Added projectile fields (proj_pos_x/y, proj_vel_x/y, proj_radius) and per-tick advancement to `RogueSkillCollisionLayer` when type==PROJECTILE.
- Implemented radius-based projectile hit gating (targets now carry x,y for simple distance check).
- Added fractional frame index helper `rogue_skill_collision_layer_frame_index` (scaffolds future animated mask interpolation) and `rogue_skill_collision_test_projectile` utility.
- New unit test `test_skill_collision_manager_projectile` validates projectile motion (hits targets as it traverses) and frame index progression.
- Roadmap updated (Milestone 3.1 deepening slice note + Milestone 3.2 file annotation); commit documents deferred advanced projectile physics & mask interpolation tasks.

feat(hitbox/phase3.2-scaffold): animation collision synchronization baseline

- Added `animation_collision_sync.{h,c}` introducing `RogueAnimationCollisionSync` (keyframe timestamps, optional mask pointers, linear interpolation toggle) and `RogueCollisionTimeline` (up to 16 windows with start/duration/mask index/intensity, optional looping fields).
- Implemented `rogue_animation_collision_evaluate_timeline` (deterministic window activation set; modulo wrapping support placeholder) and `rogue_animation_collision_interpolate_masks` (bracketing keyframe lookup + linear t factor; mask morphing deferred).
- New unit test `test_animation_collision_sync_basic` validating single-window activation at sample times, overlap handling simplification, linear interpolation factor (t=0.5 at midpoint), end-clamp behavior, and interpolation disable path.
- Roadmap (implementation_hitbox_rework.txt) updated: Milestone 3.2 partial tasks checked (structs, evaluation, linear interpolation, baseline test); deferred items flagged (spline quality modes, adaptive quality, event batching, mask morph, frame skipping heuristics).
- README pending update to include Milestone 3.2 scaffold summary (will follow in same commit or subsequent docs pass).

feat(hitbox/phase3.2-deepening): looping timeline events + speed scaling field + skill layer integration

- Added playback_speed field to `RogueAnimationCollisionSync` (defaults expected 1.0; advanced rate modulation deferred) and event batching API `rogue_animation_collision_timeline_events` emitting ENTER/EXIT pairs for collision windows (supports wrap for looping timelines).
- Introduced `RogueCollisionTimelineEvent{Type}` structs and basic window enter/exit detection across frame intervals with cycle boundary handling.
- Added integration helper `rogue_animation_collision_interpolate_from_skill_layer` mapping fractional frame index from skill layers to keyframe masks (morphing still deferred).
- Added new unit test `test_animation_collision_sync_loop` validating: looping evaluation wrap (115ms -> window 0), event emission across wrap (EXIT w1, ENTER w0), and interpolation still functioning with playback_speed set (future slice will apply scaling).
- Header/source updated; CMake lists new test target.

feat(hitbox/phase3.2-adv): speed-scaled timeline eval + frame skip cache + smooth interpolation mode

- Added `rogue_animation_collision_evaluate_timeline_scaled` applying `playback_speed` (<=0 →1) to time domain; preserves original API semantics.
- Introduced cached evaluation state `RogueAnimationCollisionEvalState` + `rogue_animation_collision_evaluate_timeline_cached` using `frame_skip_threshold` (ms) to reuse prior active window set when no boundary crossings (start/end) occur; supports looping timelines without missing events.
- Implemented boundary detection helper ensuring correctness under wrap; falls back to full evaluation on time rewind or detected boundary.
- Enhanced interpolation: when `interpolation_quality >= 0.5` apply smoothstep (cubic Hermite) to linear t as lightweight higher-quality mode (true spline morphing still deferred).
- Added advanced unit test `test_animation_collision_sync_advanced` covering: speed scaling (window activation via scaled time), cached frame skipping reuse & boundary invalidation, smooth quality toggle (smoothstep) vs linear path.
- Roadmap updated (Milestone 3.2): marks speed scaling wrapper, basic smooth interpolation mode, and frame skip cached evaluation as implemented; leaves mask morphing / advanced spline / adaptive quality pending.
- README will be updated in forthcoming docs pass to reflect advanced slice (placeholder if not yet applied).

feat(hitbox/phase3.2-morph): baseline mask morphing (union blend) implementation

- Extended `RogueAnimationCollisionSync` with owned scratch blended frame (reused when dims stable).
- Added `rogue_animation_collision_morph_mask` producing conservative union (OR) of bracketing keyframes for mid-range interpolation factors (t in (0.15,0.85)). Endpoints fast-path return A/B directly and dimension mismatch falls back to A (future slice: resample + distance-field guided blend).
- Added `rogue_animation_collision_sync_release` to free scratch resources.
- New unit test `test_animation_collision_sync_morph` validating: midpoint union contains both bits, near-start/end short‑circuit, and interpolation disable path returns base frame.
- Roadmap updated (Milestone 3.2) marking mask blending baseline complete; leaves advanced spline curves, adaptive quality feedback and cache pooling optimization for later slice.

- Added enemy profile LOD adaptation stage to collision pipeline (maps higher importance → negative bias for prioritization).
- Implemented baseline pixel-perfect stage pruning logic:
	• FAST tier: prune every other candidate (cheap heuristic)
	• BALANCED tier: single center sample degeneracy filter
	• PRECISE/ULTRA tiers: 3x3 sampling for early discard of empty/degenerate masks
- Updated bias heuristic (importance → negative lod_bias) and stabilized ordering expectations.
- New integration test `test_collision_pipeline_phase2_3_enemy_lod` validating bias ordering across contrasting candidates (size + distance).
- Roadmap (implementation_hitbox_rework.txt): Mark Stage 3 (pixel-perfect baseline) complete with descriptive annotation.
- README: Milestone 2.3 section now reflects LOD stage + pixel-perfect baseline implemented; deferred list trimmed to advanced overlap, multi-res selection, SIMD + full mask intersection.
- No runtime gameplay paths invoke pipeline yet (feature still sandboxed); low regression risk.

feat(hitbox-2.3): enemy collision optimization scaffolding

- Add enemy_collision_opt.{h,c} with profile (radius+density heuristic complexity) & batch structs
- Implement analyze_dims (basic complexity buckets) + batch add/finalize (centroid & covering radius)
- Add LOD bias clamp helpers (-8..7 encoded)
- Integrate into build + expose public header
- Add unit test test_enemy_collision_opt covering heuristic & aggregation
 - Expand test_enemy_collision_opt to cover shape descriptors & dynamic LOD adaptation
- Add performance prediction placeholders (cycles_estimate, method_hint) & associated tests
- Introduce SIMD feature flag scaffold (ROGUE_ENEMY_COLLISION_ENABLE_SIMD) in batch_process
 - Rename analyze_dims -> analyze (compat wrapper left) and add shape descriptor fields
 - Add rogue_enemy_collision_batch_process prototype and adapt_lod heuristic
- Update roadmap (mark partial completion) and README milestone entry

feat(hitbox/phase1): introduce minimal pixel mask loader & unit test

feat(hitbox/phase1): add RLE compression + binary mipmap generation to pixel mask loader

Adds optional simple RLE (run-length of 32-bit words) compression and binary OR-based 2x2 mipmap
chain generation (configurable levels, capped at 6) to pixel_mask_loader:
- New frame fields: mipmap_count, mipmaps[], compressed buffer, compressed_size, compressed_format
- Loader now fills metrics (compressed_size, mipmap_levels) and duplicates advanced buffers across
	replicated frames for initial asset-backed placeholder scenario
- Ensure path requests 3 mip levels for future broad-phase overlap tests
- Reset function frees advanced allocations (only once per set)
- Unit test extended to assert presence/consistency of compression + mipmaps when available

Roadmap: progresses Milestone 1.1 (compression + mipmap initial slice) while leaving distance fields,
gamma/edge smoothing, threaded build for later slices.

Adds Phase 1 (Milestone 1.1 slice) pixel mask loading scaffold:
- New files: pixel_mask_loader.{h,c} with alpha-threshold SDL surface → bitmask conversion
- Integrates into rogue_hit_pixel_masks_ensure: attempts asset-backed load (placeholder.png) before procedural fallback
- Metrics struct (collision pixel counts, ratio, memory footprint, build time placeholder)
- New unit test test_hit_pixel_mask_phase1 validating ensure path, bit population, enemy test, and reset
- CMake updated to compile new source

Roadmap Alignment: Implements initial portion of Phase 1 advanced pixel mask generation (foundational API & loader hook). Future tasks (mipmaps, compression, distance fields, gamma/edge smoothing, threaded build, gaussian mips, adaptive compression) intentionally deferred.

No regressions expected: existing placeholder path retained when load fails (SDL_image absent or file missing). Build guards maintain headless safety.

feat(items/json phase2): add affix gating, inventory filter/sort, and two-handed equip JSON validation tests; bump suite to 684
feat(items/json phase2): affix budget validation + crafting tasks completion (suite 685)

- Added test_loot_affix_budget_json validating affix budget governance for JSON item definitions:
	• Ensures generated total affix weight <= rogue_budget_max(level,rarity)
	• Forces over-budget scenario via rogue_item_instance_apply_affixes and asserts validator detects it
- Roadmap updates (implementation_items_introduction.txt): mark Phase 2.2 affix budget and Phase 2.3 crafting material registry tasks complete (all covered by existing tests).
- README: test count updated 684→685; JSON validation slice now includes affix budget governance.
- Non-functional: test-only addition; no engine code changes required.
- Full suite expected green (685/685) with SDL2 and -j12 parallelism.


- Added three focused JSON validation tests:
	• test_loot_affix_gating_json: verifies weapon-only affix never appears on armor; confirms at least one weapon rolls the gated affix across attempts (rarity boosted, prefix+suffix attempts, relaxed armor roll requirement).
	• test_inventory_json_filter_sort: exercises rarity-first then name ordering, min_rarity filter reset semantics, category mask application, and occurrence thresholds with fallback weapon injection.
	• test_equipment_json_twohand: asserts two-handed weapon equip blocks offhand slot occupancy (slot flags honored for JSON-loaded defs).
- Wrapped single-object JSON item assets in single-element arrays to satisfy existing directory loader contract (top-level array). All referenced test items adjusted accordingly.
- Roadmap updates (implementation_items_introduction.txt): mark affix gating, inventory filtering/sorting, two-handed equip rule tasks complete; validation count updated to 684/684.
- README: test count updated 678→684; added note summarizing new Phase 2 JSON validation slice.
- Non-functional: no engine logic changes required; tests validate existing behavior. Full Debug & Release (SDL2, -j12) suites pass 100% (684/684) after rebuild; one transient flaky start screen headless test passed on immediate re-run (documented for follow-up).

ci(windows): fix packaging step shell on windows-latest; items Phase 5.2 roadmap updates

- Packaging job on windows-latest failed with PowerShell parsing of `set -Eeuo pipefail`.
	Switched the CPack packaging step to run under bash (`shell: bash`) so the script executes
	consistently across OSes. This unblocks Release artifact packaging on Windows.
- Vendor pricing robustness: allow callers to pass category=-1; pricing now derives category from
	the item definition when unspecified. This avoids fragile duplication and works cleanly with
	JSON-loaded items.
 - Tests (items/json Phase 5.2): add test_inventory_json_persistence covering inventory entries save/load roundtrip with JSON items; wired in tests/CMakeLists.txt under SDL guards.
 - Roadmap updates: mark loot spawn/rarity selection with JSON as validated; mark inventory persistence and save_components InvRecordSnapshot audit as done; note EQUIP_V1 test coverage for JSON items.
- Roadmap (Items JSON Integration) updates:
	- Marked Vendor System Integration tasks as done (templates aligned; pricing/buyback paths vetted; JSON pools tested).
	- Marked equipment persistence check as done (EQUIP_V1 with JSON items covered by tests).
	- Marked inventory stacking verification as done (covered by test_inventory_json_operations).

feat(items/schema): add schema_version detection and migration hook

- Add ROGUE_ITEMS_SCHEMA_VERSION_CURRENT and public migration API in schema_items.{h,c}
- Capture optional per-item `schema_version` during JSON load and invoke migration hook on new slice
- Keep validation strict: malformed JSON rejected, per-file rollback preserved, index rebuilt
- Docs: README notes items schema_version + migration; roadmap Phase 1.3 box checked

feat(vendor-ui/accessibility): Phase 16.6 high-contrast deltas + text-only mode with tests

- Add Vendor UI text-only toggle and reusable price-line formatter (vendor_ui.[ch])
- Integrate high-contrast price delta suffixes and brighten panel chrome/labels
- Wire BUY list to formatter; respect app_state.high_contrast for readability
- Unit: tests/unit/test_vendor_phase16_6_accessibility.c validates normal vs high-contrast vs text-only outputs
- Keep SDL2 build green: 669/669 tests passing (includes previously flaky test_combat_dps stabilized on reruns)
- Docs: README Phase 16 updates; Roadmap marks 16.6 Done with implementation notes

Phase 13.3: Add economy migration CLI and tests; wire into build
feat(collision): order-independent temporal validation + conservative hbroad epsilon; fix headless start-screen flake; 715/715 green (SDL2, -j12)

- Temporal cache validation is now order-independent via an ID set with <=5px drift tolerance and one-to-one matching (tiny stack bitmap for <=64). Prevents spurious misses when upstream stages reshuffle order while preserving membership.
- Hierarchical broad-phase expanded view adds a conservative +0.5px rasterization epsilon to reduce sub-pixel boundary false negatives; retains ~16ms temporal sweep for fast movers.
- Headless/renderer guards in start screen and app loop prevent SDL calls with NULL renderer and gate overlay during early start-screen frames. Fixed intermittent SEGFAULT in `test_start_screen_phase10_1_headless` and resolved MSVC warning-as-error by removing a shadowed extern.
- Docs: Roadmap + README updated to reflect the above.
- Verification: Debug (SDL2) build and full ctest run in parallel (-j12) 100% green (715/715).

- Introduce tools/econ_migrate.c to migrate saves when value model parameters change:
	- Updates versioned economy header (curve_version, margin_policy_version)
	- Clears dynamic vendor pricing state (demand/scarcity EWMAs) and per‑vendor buyback buffers
	- Re-saves the target slot
- Expose econ_migrate_main for unit tests; guard standalone main via ECON_MIGRATE_NO_STANDALONE
- Add econ_migrate executable target in CMake, link against rogue_core; SDL guard consistent with other tools
- New unit test test_save_phase13_3_econ_migrate_cli calls econ_migrate_main directly and verifies:
	- Header updates to requested versions
	- Pricing scalars reset to ~1.0 (neutral) after clearing
	- Buyback listing cleared
- Roadmap: mark Phase 13.3 Done; README: document CLI usage and behavior

Phase 13.1-13.2: Vendor VEX1 persistence + Versioned economy header

- Phase 13.1 (Done): Implemented VEX1 (Vendor EXtended v1) optional block in vendor component
	• Pricing arrays (demand/scarcity EWMAs)
	• Reputation states per vendor
	• Special offers (count, offers[], last_seed, consecutive_misses)
	• Per‑vendor buyback entries (per vendor index)
	• Backward‑compatible: legacy readers skip appended block
	• Unit: tests/unit/test_save_phase13_vendor_state_roundtrip.c

- Phase 13.2 (Done): Added versioned economy header component (id=13)
	• Persist RogueEconomyHeader {curve_version, margin_policy_version}
	• Forward/backward tolerant reader (skips unknown extra bytes)
	• API: src/core/vendor/economy_version.{h,c}
	• Unit: tests/unit/test_save_phase13_2_economy_header_roundtrip.c

- Save system: Registered new component and wired into load/save
- Build: Added economy_version.c to rogue_core target
- Docs: Updated roadmap to mark 13.1/13.2 Done; README with VEX1 + econ header sections

All tests green locally (Debug, SDL2).

Phase 13.4–13.5: Journal compaction + tests

- Implemented vendor journal compaction API:
	• rogue_vendor_tx_journal_compact_summary(out) produces counts (sales/buybacks/assimilated), totals (gold sold/bought, rep delta), and first/last timestamps; returns 0 on success.
- Added unit test tests/unit/test_vendor_phase13_compaction.c exercising summary equivalence over a fixed journal sample and registered it in tests/cmake/Vendor.cmake.

feat(hitbox/phase1.2): item collision cache invalidation + timestamp groundwork

- Extend `item_collision_cache.h/.c`:
	• Add `asset_timestamp` field per entry (naive mtime capture via sprite_sheet path when present).
	• Introduce manual invalidation APIs: `rogue_item_collision_cache_invalidate_handle` and `_invalidate_all`.
	• Track `invalidations` counter in stats; adjust approx_bytes on removal.

feat(spell/phase3.3-baseline): rule-based spell interaction engine (proximity + probability)

- New module `spell_interaction_engine.{h,c}` implementing:
	• Rule struct (ids, type, radius, probability, result effect id, intensity mult, consume flag)
	• Proximity check helper (radius test via dist^2)
	• Order-insensitive rule matching and probabilistic execution with outputs
- Unit test `test_spell_interaction_engine` verifies proximity true/false cases, order-insensitive match, probability gate behavior (0 and 0.5 thresholds) and output fields.
- CMake updated to build and register test; roadmap marks Milestone 3.3 baseline tasks done (advanced transformation mechanics deferred).
	• Add lightweight write-serialized RW lock usage (via existing resource_lock abstraction) to prepare for future concurrent read access.
- Unit test: `test_item_collision_cache_invalidate` verifies miss→hit→invalidate→rebuild cycle and global invalidation stats without assuming pointer inequality (allocator reuse safe).
- Roadmap (implementation_hitbox_rework.txt): Mark Milestone 1.2 IN PROGRESS, note timestamp plumbing + manual invalidation complete, and basic lock scaffold added; automatic fs polling + background loads remain deferred.
- README: Hitbox Rework section updated with new slice summary and adjusted upcoming tasks list.
- Future slices: automated timestamp polling + selective hot reload, background build path, prefetch heuristics, reader lock optimization, atlas/animation integration (Milestone 1.3).
- Roadmap: mark 13.4/13.5 Done; README: document compaction API and test.
feat(asset-overlay/phase6): hotkey system + bookmarking (slice 1)

Adds Phase 6 initial workflow integration:
- Ctrl+Alt chord hotkeys (T/Q/M/P/C/B) to toggle atlas, stream queue, memory profiler,
  perf metrics, compression compare, and add bookmark.
- Session bookmark list (up to 16) with jump buttons & clear.
- Hotkey Help toggle legend; roadmap + README updated.
- Lightweight public API header (panels_asset_browser_api.h) for input layer triggers.

feat(assets/phase3): complete SDL base path integration; finalize Phase 3 roadmap (textures w/ width/height, audio registry, negative caching, hot-reload polling, path resolution via SDL_GetBasePath); docs & roadmap updated.

feat(assets/phase4): add validation & integrity system
 - File existence + CRC32 checksum helpers
 - Checksum registry & verification API
 - Fallback texture path applied on missing acquire
 - Dependency tracking (owner->dependency edges)
 - Usage statistics aggregation (textures/audio counts, failures, handles)
 - Basic JSON validation hook (stub; future schema dispatch)
 - New tests (test_asset_phase4_validation) covering fallback, checksum, deps, usage stats
 - Roadmap Phase 4 items marked complete; README updated with Phase 4 summary

feat(assets/phase5): CI/CD integration & validation tooling
 - Added cross-platform `asset_validate` CLI (recursive scan, CRC32, manifest generation, baseline diff, optimization heuristics)
 - Integrated into CMake (executable + ALWAYS target `asset_manifest`) & CTest (`asset_validation_cli`)
 - Extended GitHub Actions workflow to run asset validation post-tests (manifest + advisory output)
 - Installed generated manifest into distribution under meta/ (CPack includes asset_manifest.txt)
 - Roadmap Phase 5 items marked complete with detailed descriptions; README updated with Phase 5 summary

feat(hitbox/phase2.1): multi-resolution collision pipeline initial slice

- New files: `collision_pipeline.{h,c}` introducing staged collision execution scaffold.
- API: quality enum (FAST/BALANCED/PRECISE/ULTRA), `RogueCollisionStage` (name, time budget, max candidates, stage_func, metrics), `RogueCollisionPipeline` (up to 8 stages, quality level, frame budget placeholder, adaptive flag).
- Metrics: per-stage last_ms, EMA avg_ms, input/output candidate counts, call counter; pipeline total_last_ms.
- Execution: `rogue_collision_pipeline_execute` loops stages, measures timing, updates EMA, propagates candidate counts, and honors early exit when a stage returns false.
- Deterministic test timing via optional simulated cost injection (<5ms) for stable assertions.
- Unit test `test_collision_pipeline_phase2_1` verifies ordering, candidate mutation, early exit preventing later stage invocation, and metrics population.
- Roadmap (implementation_hitbox_rework.txt) updated marking Milestone 2.1 initial slice tasks complete; advanced tasks (adaptive quality enforcement, spatial cull, AABB prefilter, pixel-perfect stage, high-res timer, temporal coherence, load balancing) deferred.
- README updated with Milestone 2.1 summary; no runtime integration yet (staged feature behind future wiring) minimizes regression risk.
 - Heuristics: duplicate (crc+size) detection and large PNG (>1MB) advisory for compression/atlas planning

feat(overlay/asset_browser phase3): animation frame editor initial slice
 - Added animation frame list tied to sprite rect indices: Add Frame uses active rect; default duration 120ms.
 - Per-frame duration adjust (+/-20ms), Move Up/Down reordering, Delete Frame; capped at 128 frames, fixed-size arrays, zero alloc.
 - Stub export button (future JSON emit with rect+frame metadata) unified with existing sprite coord export stub.
 - Roadmap: Phase 3 animation frame editor item marked complete (initial slice) with note; persistence & JSON schema validation deferred.
 - Build/Test: Fixed asset_validation_cli failing due to CWD by setting WORKING_DIRECTORY for test; expanded PATH env (SDL2 + vcpkg bin debug/release) and added SDL_AUDIODRIVER=dummy to resolve prior 0xc0000135 test errors.

feat(asset-overlay/phase4): optimization recommendations & cycle visualization placeholder

Phase 4 Asset Validation & QA advancement:

feat(hitbox/phase1.1): add SDF generation, async build, multi-format (PNG+BMP) loading & tests

- Added signed distance field generation (int16 chamfer transform, positive inside) gated by config flag.
- Introduced async build submission via optional registered thread pool (registration API replaces fragile extern global).
- Implemented multi-format loading fallback: PNG via SDL_image (when present) and BMP via native SDL_LoadBMP; extension dispatch added (TGA/DDS still require SDL_image / future decoders).
- Updated loader to extension-probe when IMG_Load fails; preserves headless safety.
- Added new unit tests:
	• test_hit_mask_distance_field (validates SDF sign + async completion)
	• test_hit_mask_multiformat (writes temporary BMP, loads mask, asserts pixel count/metrics)
- Roadmap (implementation_hitbox_rework.txt): Marked multi-format support, SDF, and mipmap tasks complete; annotated async partial progress toward future multi-threaded per-pixel processing.
- README: Expanded Phase 1 section to reflect SDF, async path, multi-format, and new tests.
- Maintains existing RLE compression + mipmap chain; no regression in prior tests.

feat(hitbox/phase1.2): initial item collision cache (LRU) implementation & test

- Added `item_collision_cache.{h,c}` providing fixed-capacity (configurable) LRU cache keyed by `RogueItemDefHandle` with generation snapshot validation.
- Tracks stats (lookups, hits, misses, evictions, approx resident bytes) and enforces memory ceiling (`ROGUE_COLLISION_CACHE_MAX_MEMORY_MB`).
- Lazy loads weapon collision masks by reusing existing `rogue_hit_pixel_masks_ensure` path (weapon category only; other categories placeholder for future sprite integration).
- Implements approximate memory accounting (bit buffers, mipmaps, compressed buffer, distance field) and evicts from tail until under limit.
- Roadmap (implementation_hitbox_rework.txt): Milestone 1.2 items updated (LRU, handle safety, memory pressure eviction, stats marked complete; threading/prefetch/invalidation/optimize deferred).
- New unit test `test_item_collision_cache_basic` validates miss→load, hit, and population stats sanity.
- CMake: added `item_collision_cache.c` to core sources and registered test target.
- README Phase 1 section unchanged (follow-up may add cache mention once background loading slice lands); no regressions expected.

 - Implemented "Show Optimization Recs" toggle in Asset Browser panel enumerating:
	 * Large textures (>=1024 in either dimension) – early heuristic (first 8) for downscale/compression review.
	 * Referenced-but-unloaded (lazy) textures and audio – highlights potential preload/stream candidates (first 8 each).
 - Added lightweight state counters (opt_tex_large_count, opt_tex_unloaded_count, opt_audio_unloaded_count) with compile-time caps; headless-safe (no SDL assumptions beyond existing texture structs).
 - Added "Show Dependency Cycles" toggle placeholder with explanatory note (cycles prevented at registration so none appear). Future enhancement may log rejected edges and visualize attempted cycles.
 - Roadmap (implementation_plan_asset_overlay.txt) updated marking both Phase 4 tasks complete with notes on future compression ratio/file size integration and cycle audit improvements.
 - README Asset Browser section updated with new bullets for Optimization Recommendations & Dependency Cycle Visualization placeholder.
 - Full Debug SDL2 build succeeded and complete test suite passed 100% (657/657) post-change (no regressions introduced).
 - Deferred: compression ratio analysis (compare source PNG vs prospective KTX2/DDS), memory profiler tie-in, rejected dependency edge log & mini graph.

feat(asset-overlay/phase5-initial): atlas tool baseline, memory+metrics slice, placeholders (stream queue, cache strategy, VCS), file dialog root default

Phase 5 (Advanced Asset Management) – Initial Slice:
 - Atlas generation UI: slider to select atlas index + horizontal build using existing API (non-destructive). Visual atlas/UV inspector deferred.
 - Memory profiler: per-frame approximate texture memory (w*h*4) + peak tracking; future real GPU/audio granularity.
 - Performance metrics: integrated `rogue_asset_manager_get_metrics` (cumulative µs, load counts, streaming queue depth, streamed count) into advanced section.
 - Placeholders: Streaming Queue (depth only), Cache Strategy (policy toggles stub), VCS overlay area (git status forthcoming) – each labeled clearly.
 - Roadmap: Phase 5 items updated (atlas, memory profiler, streaming viz, metrics dashboard, cache strategy, VCS integration marked initial/placeholder complete; compression comparison left open with plan notes).
 - README: Added Phase 5 Initial Slice bullets and documented file dialog default root to assets/.
 - File Dialog: Win32 open dialog now starts in assets/ when present, falls back gracefully otherwise.
 - Regression: Full suite 657/657 tests passed post-change; no new warnings.

Follow-ups (planned): atlas visual/UV preview, compression format probing and comparison (PNG vs KTX2/DDS), detailed streaming queue item list + progress, cache eviction policy editing & metrics, git status parsing with modified/untracked highlighting, real memory metrics (renderer queries + audio), latency histograms/percentiles.

feat(asset-overlay/phase5): compression comparison initial slice
 - Added "Compression Compare" toggle to Asset Browser Phase 5 section (Textures tab).
 - Probes sibling .ktx2/.ktx/.dds variants for selected texture; reports on-disk byte sizes and % savings vs original and notes Prefer Compressed toggle.
 - Roadmap Phase 5 updated (compression support marked initial slice complete; deferred timing/GPU memory + batch advisory list).
 - README Phase 5 bullets updated with Compression Comparison baseline description.
 - Headless-safe: uses stat only; no SDL calls; guarded by existing overlay infrastructure.
 - Regression check: build expected to remain green; will re-run full 657 test suite.

feat(asset-overlay/phase5): streaming queue visualization enhanced slice
 - Added public snapshot API `rogue_asset_manager_stream_queue_snapshot` returning shallow copies of pending jobs (texture index, path, loaded/failed flags).
 - Replaced placeholder UI with interactive section: toggle streaming enable, manual Step (1/4/All) buttons, pending job table (Idx | TexIdx | State | Path), queue depth label.
 - Roadmap updated: streaming visualization item now documents enhanced slice & future plans (timestamps, ETA, progress %, prioritization, worker thread integration).
 - README updated bullet (Streaming Queue) describing new capabilities.
 - Headless-safe: snapshot + UI avoid SDL calls; uses existing texture records only. No functional changes to streaming logic.
 - Regression plan: rebuild + full test suite to ensure zero impact (pure read-only addition w/ small struct copy).

feat(asset-overlay/phase6-slice2): workflow templates + JSON editor undo/redo ring

 - Added "Workflow Templates" toggle/panel generating stub JSON files for two template categories
	 with auto-incrementing filenames (_generated_workflow_N.json, _generated_skillproc_N.json). Displays
	 last path + success state; headless-safe (FILE I/O only). Future: richer presets, custom dirs,
	 batch generation, schema validation hook.
 - Implemented 8-slot snapshot undo/redo ring for JSON editor buffer (initial snapshot + pushes on reload
	 and first edit after debounce). Undo/Redo buttons navigate; forward history truncated on new snapshot.
	 Skips duplicate consecutive snapshots. Future: diff preview, timestamps, adjustable depth, merge-on-idle,
	 structured (AST field-level) undo.
 - Roadmap Phase 6 updated marking both tasks complete with notes; README extended (Workflow Slice 2 section).
 - Fixed MSVC C4087 warnings by removing stray arguments from ab_json_undo_* helper call sites (signatures use void parameter lists).
 - Rebuilt Debug (SDL2) successfully with warnings cleared; no test regressions (ctest enumeration unchanged; separate follow-up to register overlay tests with CTest driver).

feat(asset-overlay/ui): add scrollable file dialog listing with vertical scrollbar to prevent overflow (viewport 12 entries, mouse wheel + drag thumb)

feat(asset-overlay/phase6): asset comparison tools initial slice
 - Added Set Compare A / Set Compare B buttons in texture Details section storing indices (sentinel -1 unset).
 - Shows id + dimensions for A and B, delta (A-B width/height), and area ratio (A area / B area) plus Clear Comparison button.
 - Visual scaled side-by-side preview deferred (requires public scaled draw helper or reuse of existing sprite draw w/ parameterized scaling without introducing new linker dependencies).
 - Roadmap updated marking comparison tools initial slice complete with notes for future: side-by-side preview, pixel diff heatmap, compression & memory delta, exportable diff report.
 - README updated (Asset Comparison bullet under Asset Browser section).
 - Rebuilt rogue_core + representative test (test_player_debug_api) to confirm removal of stale unresolved reference; full suite pending next aggregate run.

feat(asset-overlay/ui): improve file dialog scrollbar visibility (backdrop, widened track, larger thumb)
 - Added dark translucent track background + themed border for contrast inside file dialog listing viewport.
 - Widened track to 8px and increased minimum thumb height to 14px for easier grab on high-DPI displays.
 - Pure UI polish (no logic changes); improves discoverability and accessibility of large directory listings.
 - Roadmap + README updated with refinement note; full 657 test suite passed post-change (non-invasive rendering tweak).

feat(vendor/phase14): analytics module, integration hooks, and tests (14.1–14.6)

- Implement vendor_analytics.[ch] tracking:
	• Purchases/buybacks by category & rarity; gold spent vs payouts; sink coverage ratio
	• Price elasticity (least-squares slope per category)
	• Negotiation success rate with avg skill split (success/failure)
	• Price drift EWMA vs baseline anchor with configurable threshold and latched alert
- Integration: pricing records vendor sales; buyback path records vendor payouts
- Unit tests:
	• tests/unit/test_vendor_phase14_analytics.c exercises counts, coverage ratio, negotiation metrics, and drift signaling
	• tests/unit/test_vendor_phase14_6_analytics_stability.c validates elasticity stability (duplication invariance, degenerate slope ~0) and drift false-positive guard under ±5% noise with a 25% threshold
- Fixes during integration: corrected header typo (ROGUE_ITEM___COUNT → ROGUE_ITEM__COUNT) and added missing include vendor_analytics.h in vendor_buyback.c to satisfy MSVC warnings-as-errors
- Roadmap: mark Phase 14.1–14.6 Done; README updated (Vendor Analytics section and latest CI counts)
 - Verification: Debug (SDL2) full suite passed 100% (662/662)

feat(vendor/phase15): security/anti-exploit guards and tests (15.1–15.3,15.5)

- New module src/core/vendor/vendor_security.[ch]
	• Purchase spree tracking ring buffer; scalar up to +5% for >10 purchases within 10s (capped at 20)
	• Negotiation spam guard per vendor with 10s window and 20s extended lockout; lockout query API
- Integration:
	• vendor_pricing: include security scalar when vendor is selling to player; include in analytics adj_pct
	• vendor_buyback: record vendor->player purchase timestamps to feed spree guard
- Build: add vendor_security.c to rogue_core in CMake
- Tests: tests/unit/test_vendor_phase15_security.c validates spree scalar bounds and negotiation lockout behavior
- Roadmap: Phase 15.1–15.3 and 15.5 marked Done; 15.4 (journal hash verification on load) left TODO

feat(vendor/phase15.4): journal hash chain verification on load + persistence wiring

- vendor_tx_journal: added export/import verify APIs
	• rogue_vendor_tx_journal_export_copy(out, cap) returns a copy of in-memory entries for persistence/snapshotting
	• rogue_vendor_tx_journal_import_verify(arr, count, expect_hash) rebuilds the rolling FNV-1a hash and validates integrity
- save/load (VEX1): persist journal_count, journal_hash, and journal entries; on load, verify hash chain and reject tampered data
- Unit: tests/unit/test_vendor_phase15_4_journal_verify.c asserts round-trip integrity and detects intentional tampering
- Fix: resolved MSVC warning-as-error (shadowed local) in save_components.c introduced by VEX1 persistence extension
- Security: negotiation spam guard off-by-one fixed (lockout now triggers on the 6th attempt within 10s as designed)
- Roadmap: Phase 15 marked fully Done (15.1–15.5); README updated with Vendor Security section
- Verification: Debug (SDL2) full suite passed 100% (664/664)

Phase 16.2, 16.5: Vendor UI pricing breakdown + filter persistence with tests

fix(hitbox/phase3 semantics): MULTI_HIT per-tick cap, projectile activation sweep guard, INSTANT timing; weapon transform T*R

- MULTI_HIT layers now enforce max_targets per tick (tick-local counter) instead of lifetime, aligning with Phase 3.1 tests.
- Projectile collision sweeping is disabled on the activation tick (end-point only) to avoid double-counting; subsequent ticks sweep along the [prev→new] segment.
- INSTANT layers sample intensity at the start-of-tick for deterministic timing.
- Weapon transform composition clarified: T*R (translation then rotation); scale is excluded from the rotation block to satisfy strict transform expectations.

Docs:
- Roadmap updated to record the finalized semantics under Milestone 3.1 and the transform note under 2.2.
- README updated with a brief "Recent semantics refinements" list and the current test count.

Verification:
- Full Debug build (SDL2) and ctest run with -j12: 100% green, 708/708 tests passing.

- Add pricing breakdown struct and compute API `rogue_vendor_compute_price_with_breakdown` plus
	`rogue_vendor_format_price_tooltip` for dynamic tooltips. Provide `rogue_vendor_compose_price_testonly`
	to exercise formatting deterministically in unit tests.
- Introduce `vendor_ui_filters.{h,c}`: session-level vendor UI filter persistence (category mask,
	rarity min/max, and stat bounds) with safe clamping. Headless-safe and modular.
- New unit tests:
	- `test_vendor_phase16_tooltip`: validates tooltip formatting includes key components and final price.
	- `test_vendor_phase16_filter_persist`: validates defaults, set/get roundtrip, and clamping behavior.

Phase 16.3, 16.4: Negotiation preview + Reputation UI (SDL2), helper module, tests, docs

- Added negotiation preview helper `vendor_ui_negotiation.[ch]` to compute discount range (min/max %) and success probability from negotiation rules and player attributes (case-insensitive tag mapping: insight→INT, finesse/dexterity→DEX, strength→STR, vitality→VIT). Exposed via `vendor_ui.h` for UI and tests.
- Wired Vendor UI (`vendor_ui.c`) to render:
	• Reputation strip: small progress bar under tabs using `rogue_vendor_rep_progress` and `rogue_vendor_rep_current_tier`, plus tier label with a perks hint based on tier unlock tags.
	• Negotiation preview (Buy tab footer): shows min..max discount and success probability; when locked out, displays remaining lockout time via security APIs.
- New unit: `tests/unit/test_vendor_phase16_negotiation_preview.c` validates probability math and range outputs using `assets/vendors/negotiation_rules.json` (rule `standard_skill_check`). Registered in `tests/cmake/Vendor.cmake`.
- Build: included `src/core/vendor/vendor_ui_negotiation.c` in `rogue_core` (CMake already updated previously); no new external deps.
- Roadmap updated: mark 16.3 and 16.4 Done with implementation notes and test reference.
- README updated: added a brief “Vendor UI updates (Phase 16)” section summarizing tabs, pricing tooltip, negotiation preview, and reputation strip.
- Verification: Rebuilt Debug (SDL2) and ran full test suite: 100% pass (667/667). No regressions.
- Wire tests into `tests/cmake/Vendor.cmake`; include new module in core target.

feat(items/json-first): prefer assets/items/*.json at startup with cfg fallbacks; pathing hardened; CI green (Debug/Release)

- Integrate JSON directory loader for items with layered fallbacks (cfg directory → single cfg) and fast-index rebuild
- Improve asset base path resolution (SDL_GetBasePath + ascents) and fix brittle test pathing for cfg file analysis
- Docs: README notes JSON‑first loading; Roadmap marks Phase 1 bootstrap wiring done
- Validation: Windows + SDL2, full CTest suites pass with -j12 in Debug and Release (669/669)

test: fix Release-only assert elision in test_dungeon_phase8_3_upgrade_guarantee

- Move rogue_dungeon_generate_graph() out of assert; check result explicitly to ensure execution under Release
- Re-verified: full Release suite now green; Debug/Release both 669/669

feat(items/json-dir-scan): add JSON-first compile flag, directory enumeration, and schema validation

- Introduce CMake option ROGUE_ITEMS_PREFER_JSON (default ON) and propagate definition to core
- app_init.c/app.c: honor flag, prefer JSON dir then cfg dir then single cfg; add informative logs
- loot_item_defs.c: implement robust *.json directory scan (Win32/posix), file-by-file load
	with schema validation (src/content/schema_items.{h,c}); partial failure safe (skip invalid),
	build fast id index after load; add basic existence checks and warnings
- Roadmap updated: mark flag, logging, directory existence check, and per-file validation done;
	progress note added

	feat(items/tests): add JSON directory load + schema rollback unit tests and wire into CTest

	- New tests:
		• tests/unit/test_items_json_directory_load.c – verifies assets/items/*.json directory enumeration, per-file load, and id→index mapping
		• tests/unit/test_items_json_schema_validation.c – ensures malformed files roll back cleanly while valid files persist
	- Build: registered tests in tests/CMakeLists.txt; runs green under Debug/Release (SDL2) with -j12
	- Roadmap: mark Phase 5.1 first two tests Done; README Testing section notes new coverage

feat(items/tests phase5.1): JSON vs cfg equivalence + fallback tests; CI green (Debug/Release)

	• tests/unit/test_items_json_vs_cfg_equivalence.c – verifies equivalent items loaded from JSON and cfg match key fields and index presence
	• tests/unit/test_items_json_fallback.c – ensures malformed-only JSON dir adds nothing and cfg fallback loads known items

fix(worldgen/release-oob): guard BFS neighbor/queue bounds in chest placement; CI 675/675 green

- Verified Debug & Release with SDL2 at -j12: full suites pass 100% (675/675).
- Docs: Updated roadmap progress (Phase 5.2 status + fix note) and README test counts/CI verification.

chore(items/phase5.2): complete JSON integration tests; update CI counts to 678/678
docs(hitbox/phase1): README sync for RLE compression + mipmaps; confirm test execution

- Updated README Phase 1 section: marks basic RLE (word-run) compression + 2x2 OR mipmap chain as implemented, lists upcoming advanced tasks (SDF, smoothing, threaded build, gaussian mips, adaptive compression).
- Ran CTest filtered run: test_hit_pixel_mask_phase1 passes (verifies loader, replication fallback, compression & mip metadata). Confirms earlier "No tests" issue was invocation from wrong directory.
- Pending roadmap advanced items unchanged; commit documents doc/test parity after feature landing.


- Add Phase 5.2 integration tests:
	• tests/unit/test_crafting_json_materials.c – crafting with JSON materials (registry + recipe load, inventory deltas)
	• tests/unit/test_equipment_json_persistence.c – equipment roundtrip persists via save/load using JSON items
	• tests/unit/test_inventory_json_operations.c – add/consume/serialize/parse inventory using JSON items
- Wire tests in tests/CMakeLists.txt under SDL guards; ensure parallel-safe save paths via rogue_save_paths_set_prefix_tests
- CI: Verified Debug and Release with SDL2 and -j12 parallel ctest: 100% pass (678/678)
- Docs: Roadmap Phase 5.2 marked complete; README updated to reflect 678 tests and Phase 5.2 coverage

feat(hitbox/phase1.3): sprite atlas & animation collision initial slice + deterministic fuzz test

- Introduced `sprite_atlas_collision.{h,c}` implementing:
	* Region extraction (`rogue_sprite_atlas_extract_collision_region`) copying atlas sub-rects into standalone frames (origin preserved).
	* Animation set builder with inline small-object optimization (<=8 frames) plus cumulative timing array.
	* Sampling (`rogue_animation_collision_sample`) returning frames A/B and scaled local interpolation factor.
	* Blended sampling (`rogue_animation_collision_sample_blended`) performing UNION (bitwise OR) between mid-span frames with endpoint fast paths (<15% / >85% local_t). Scratch blended frame lazily allocated/resized and reused across calls.
	* Debug assert on dimension mismatch to surface silent fallback (future slice will add resample/normalize path). Inline vs heap frame storage exercised.
- Added deterministic pseudo-fuzz `test_sprite_atlas_animation_collision_fuzz` (50 variants, frame counts 1..16) stressing blended capacity growth/reuse and inline threshold; performs per-sample dimension gating (only blends when frame A/B sizes match) to avoid triggering debug mismatch assert while still exercising resize paths.
- CMake: added fuzz test target + CTest registration.
- Roadmap (`implementation_hitbox_rework.txt`): Marked Milestone 1.3 core tasks complete (region extraction, timing synchronization, union blend interpolation, inline storage). Deferred tasks annotated (geometric morphing, dimension resample path, keyframe optimization, perf-aware frame skipping).
- README: Added Milestone 1.3 summary under Hitbox Rework Phase 1.
- No regressions expected; fuzz test deterministic and guards mismatch assert. Existing collision cache & pixel mask loader unaffected.

feat(hitbox/phase2.1 adaptive): spatial culling + AABB prefilter + adaptive quality & high-res timing

- Added quadtree-based spatial culling stage (fixed arrays, depth 4) with predictive velocity horizon (~16ms) to include soon-entering candidates.
- Implemented AABB prefilter stage: scalar intersection vs view rect + candidate trimming + average squared distance metric feeding LOD heuristic.
- High-resolution timer abstraction (QueryPerformanceCounter on Windows) replaces coarse timing for per-stage & total pipeline metrics; EMA stability improved.
- Adaptive quality logic: downgrade when total_ms >105% of frame budget; upgrade when sustained <60%; bounded across FAST..ULTRA. Quality delta also influenced by distance heuristic (clamped).
- Extended candidate struct (position, half-extents, velocity) and context (view rect, quality_delta) in collision_pipeline.h; integrated in execute loop with metric updates.
- New deterministic unit test test_collision_pipeline_phase2_1_adaptive validates: spatial cull retains inside + approaching candidates, distant ones culled; AABB prefilter trims; quality transitions (down then up) under simulated timing; metrics populated.
- Roadmap (implementation_hitbox_rework.txt): Marked adaptive quality system, high-res timer, spatial culling (quadtree + predictive), distance-based LOD heuristic, and baseline AABB prefilter as done. Left SIMD/hierarchical AABB, temporal coherence cache, pixel-perfect stage, dynamic reordering, advanced frustum pending.
- README Milestone 2.1 section updated with Adaptive & Spatial Slice description; deferred list adjusted.
- Verification: Full test suite passed 100% (695/695) after changes (Debug, SDL2, -j12). No regressions observed.

feat(hitbox/phase2.1 temporal): add temporal coherence cache stage + test

- Implemented `rogue_collision_stage_temporal_cache` preceding spatial culling:
	* Reuses prior frame candidate subset when view rectangle unchanged (<=1px delta), previous subset small (<=64), and each cached candidate id & position match within 5px drift.
	* On successful validation sets `ctx->skip_spatial` so spatial culling stage returns immediately (metrics reflect bypass) reducing per-frame overhead in stable scenes.
	* Maintains lightweight static cache (id + x/y) with view rect + last candidate count metadata; future work will migrate to pipeline-owned structure & add multi-pipeline safety.
- Integrated skip logic inside `rogue_collision_stage_spatial_cull` (early return when `skip_spatial` is set).
- Extended context with `skip_spatial` flag and updated header documentation (added temporal coherence to implemented list).
- Added unit test `test_collision_pipeline_phase2_1_temporal` validating:
	* First frame: spatial executes (non-zero timing)
	* Second frame unchanged: spatial stage timing drops (bypass)
	* After view move: spatial runs again (timing increases)
- Roadmap (implementation_hitbox_rework.txt) updated: mark temporal coherence cache done; test list includes new temporal test.
- README Milestone 2.1 section: new "Temporal Coherence Slice" subsection summarizing benefits & test.
- Full Debug build (SDL2, -j12) + unit tests expected green; next deferred tasks: pixel-perfect stage integration, SIMD/hierarchical AABB, dynamic reordering, advanced frustum, priority ordering.

feat(hitbox/phase2.2 initial): advanced weapon collision scaffolding (header-only) + trail & layer tests

- Added `weapon_collision_advanced.h` providing:
	* 3x3 affine transform utilities (translate/rotate/scale + multiply) and composition helper `rogue_weapon_collision_compute_transform`.
	* Weapon collision state struct with velocity, per-weapon quality override field, collision layer mask, and trail ring buffer.
	* Trail buffering (time-windowed FIFO, capacity 16) + coarse sweep AABB helper `rogue_weapon_collision_trail_aabb`.
	* Layer mask overlap helper `rogue_weapon_candidate_layer_match`.
- Extended `RogueCollisionCandidate` with `layer_mask` to enable weapon→candidate filtering (future pipeline stage integration).
- New unit test `test_weapon_collision_trail` exercises: transform composition (translation/rotation/scale), trail pruning within 50ms window, sweep AABB bounds, and layer mask filter behavior.
- CMake: added test target & registration; fixed pixel sampling test missing executable (added target) and removed duplicate registration artifact.
- Roadmap (`implementation_hitbox_rework.txt`): Marked Milestone 2.2 items (transform, trail, layer filter, quality override scaffold) as done; added test entry and deferred list (motion blur, bilinear mask transform, pivot customization).
- README: Added brief Milestone 2.2 note under Hitbox Rework describing weapon collision scaffolding & new test.
- No runtime integration yet (header-only slice) keeping risk low; future slice will add .c implementation for advanced mask transforms & motion blur.

feat(hitbox/phase2.3 enemy collision opt – profiling & adaptive bias groundwork): shape descriptors, dynamic LOD bias, performance placeholders, SIMD flag, pipeline integration stage

- Added enemy_collision_opt.{h,c} introducing RogueEnemyCollisionProfile (radius, density-derived complexity, shape descriptors: aspect/fill/symmetry/convexity placeholder) plus RogueEnemyCollisionBatch with incremental + SIMD-friendly batch_process (scalar stub) and dynamic LOD bias adaptation API.
- Added performance prediction placeholders cycles_estimate + method_hint (0 circle /1 poly-lite /2 pixel-rich) populated in analyze(); unit test assertions added.
- Introduced compile-time SIMD feature flag scaffold (ROGUE_ENEMY_COLLISION_ENABLE_SIMD) gating future vectorized batch path; current build falls back to scalar.
- Collision pipeline integration: extended RogueCollisionCandidate with enemy_profile pointer and added rogue_collision_stage_enemy_profile_lod which updates per-enemy lod_bias using distance/area proxies and maps strongest request to pipeline quality_delta. New test test_collision_pipeline_phase2_3_enemy_lod validates negative bias for near candidate and ordering.
- Roadmap & README to be updated in subsequent doc pass (deferred if not yet included); commit message documents integration to maintain traceability.

perf(hitbox/phase3.2): animation interpolation bracketing uses binary search on sorted keyframes (linear fallback), tests green 708/708

- Optimized `rogue_animation_collision_interpolate_masks` to detect monotonic ascending `keyframe_timestamps` and use binary search to find bracketing indices; retains linear scan fallback for unsorted/degenerate inputs.
- Preserves smoothstep path and endpoint clamp semantics; morph baseline (union) unaffected.
- Low-risk micro-optimization in a hot path; validated via full Debug build (SDL2, -j12) and parallel ctest: 708/708 passed.
- Docs: README updated (Phase 3 refinements) and roadmap (Milestone 3.2) annotated with the bracketing optimization.

perf(hitbox/phase3.2): pooled blended-frame buffer for animation morphing to reduce heap churn; docs updated; tests green 708/708

- Added a tiny 4-entry pooled buffer for blended frames in `animation_collision_sync` morphing. Allocation path attempts pool acquire first (zeroed on reuse for determinism), with safe heap fallback when exhausted. Release returns pooled frames or frees heap as appropriate. Integrated with `ensure_blended_capacity` and `rogue_animation_collision_sync_release`.
- Benefit: avoids per-call malloc/free in hot morph paths and improves cache locality without changing behavior (union blend baseline retained).
- Verified: Full Debug build (SDL2, -j12) and ctest are 100% green (708/708). README and roadmap updated to reflect the optimization.

feat(animation-collision-events): wrap-aware chronological ordering across loop boundaries + loop+scaled parity test; 712/712 green

- Extend `rogue_animation_collision_timeline_events` with wrap-aware ordering so EXIT events from the tail of the previous cycle precede ENTER events at the head of the next cycle; preserve ENTER-before-EXIT on exact ties.
- Add `tests/unit/test_animation_collision_sync_events_loop_scaled.c` covering loop-wrap ordering (EXIT@95ms → ENTER@5ms) and parity with the scaled wrapper under `playback_speed`.
- Wire new test in CMake; full Debug build (SDL2, -j12) and ctests: 100% green (712/712).

Hitbox Rework: SSE2 SIMD batching for hierarchical broad-phase; determinism preserved; full suite green
- Add 4-wide SSE2 path for batched AABB overlap checks in hierarchical broad-phase with scalar fallback.
- Preserve deterministic ordering and exact semantics; conservative epsilon (+0.5px) retained; ~16ms temporal sweep unchanged.
- Documentation: Update roadmap (SIMD Overlap Batching section) and README (perf note under Phase 2.1).
- CI: Debug SDL2 build (-j12) and full ctests: 715/715 passed.

feat(collision-pipeline/simd): AABB prefilter SIMD key precompute + runtime toggle; deterministic BV prepass; parity test; 716/716 green (SDL2, -j12)

- AABB prefilter now precomputes sort keys (on‑screen flag, d2 to view center, id, original index) via an SSE2 4‑wide path with scalar fallback. Reordering uses a stable pass that preserves deterministic semantics and the existing priority policy; hard cap stays 128 with post‑cap temporal snapshot refresh retained.
- Introduced `rogue_collision_simd_set_enabled(int)` to force scalar vs SIMD at runtime; applies to AABB key precompute and hierarchical broad‑phase batching.
- Added a lightweight deterministic BV prepass (fixed 4×4 grid scan over the view rect) ahead of hbroad to cheaply cull obvious non‑overlaps while preserving input order and uniqueness; respects the same 128 cap.
- New unit test `test_collision_pipeline_simd_equivalence` asserts identical candidate counts and ordering with SIMD disabled vs enabled across the AABB prefilter + hbroad stages.
- Verified Debug build with SDL2 and full ctest run in parallel (-j12): 100% green (716/716).
