chore(worldgen/dungeon phase4): expand budgets (ΔL + critical-path), modifier smoothing; add ΔL precision tests; suite green (605/605)

- Phase 4: Extend encounter planner with ΔL- and critical-path-weighted budgets and modifier smoothing to reduce affix/mod clustering. Keep deterministic micro-RNG streams.
- Tests: add `tests/unit/test_dungeon_phase4_delta_mapping.c` for ΔL monotonic/precision and budget trend; expand encounter invariants.
- Stabilization: persistence integration bridge re-validated after a one-off flaky run; full Debug (SDL2) suite consistently green.

Quality gates:
- Build: Debug (SDL2) with -j12 OK.
- Tests: ctest from build dir with -C Debug -j12 --output-on-failure → 100% PASS (605/605).

feat(dungeon/phase3.5,4): statistical tests for biome envelopes + Phase 4 encounter planner slice; stabilize rare-variant probability; gates green (604/604)

- Phase 3.5: Add biome distribution envelope and rare-biome probability bound tests (`tests/unit/test_dungeon_phase3_5_biome_distribution.c`).
- Stabilize rare biome substitution by switching to a dedicated micro-RNG stream and basis-point thresholding with a clamped depth-scaled probability.
- Phase 4: Introduce an encounter planner with BFS room depths, depth-scaled budgets, elite/miniboss spacing constraints, and rare nemesis injection. Unit `tests/unit/test_dungeon_phase4_encounters.c` validates counts, budgets, type distribution, and nemesis frequency.
- Docs: Roadmap marks 3.5 Done and records Phase 4 slice progress; README notes new tests and planner slice; Latest CI verification updated to 604/604.

Quality gates:
- Build: Debug (SDL2) with parallelism (-j12) completed.
- Tests: ctest from build dir with -C Debug -j12 --output-on-failure → 100% PASS (604/604).

feat(dungeon/phase3): biome & theme layering module with deterministic selection, theme profile, and hazard palette; tests green (602/602)

- Implement biome selection per depth band with RNG governance and rare biome substitution.
- Add theme profile (fog density, ambient color, ambient SFX) derived from biome tags.
- Compute hazard palette with depth scaling and tag adjustments (movement penalties, DoT, trap weights).
- Wire into build; add unit test `test_dungeon_phase3_theme` validating determinism, ranges, and depth scaling.
- CI: Debug (SDL2) full suite passed 100% with -j12 (602/602).

feat(dungeon/phase2.4): add environmental deco overlay layer + safety invariant; tests green (601/601)

- Implement overlay_deco buffer with transform-aware stamping and kind→code mapping (pillar blocks nav; banner/brazier decorative)
- Gate overlay usage through RogueTileMap.overlay_magic (0xDEC00EAU) to prevent uninitialized overlay access in tests/runtime
- JSON room template loader: key-order independent; explicit exits/doors/deco arrays override ASCII markers; fuzz test included
- Stamp path respects overlay-aware navigation blocking; base tile stamping unchanged
- Hardened getters/setters for overlay get/set with invariant; free/init set/clear the magic
- All tests pass under Debug (SDL2) with ctest -C Debug -j12 (601/601)

Docs:
- Roadmap: mark Phase 2.4 Environmental deco layering as Done; note overlay_magic guard and updated test count
- README: bump test count to 601/601; add Dungeon Phase 2 note on deco overlay + safety invariant
feat(skills/audio): Add Sound ID Browser to Skills → Audio tab

Adds a filterable Sound ID Browser sourced from assets/sounds.cfg with an Assign-To selector (Cast/Impact/Loop). Clicking a row assigns the sound id to the selected target and saves via overrides. Headless-safe and non-SDL dependent.

Docs: Updated roadmap (2.3 Enhanced UI Widgets) and README (Skills → Audio helpers).

Quality gates:
- Build: Debug (SDL2) with -j12 OK
- Tests: ctest -C Debug -j12 → 100% passed (594/594)

Touched:
- src/debug_overlay/panel_skills/panel_skills_audio.c
- roadmaps/implementation_plan_skill_creation_suite.txt
- README.md
feat(skills/visuals UX): convert AoE Shape and Trajectory Type to enum dropdowns; docs/roadmap; gates green

- Visuals tab: replace discrete integer sliders with labeled combos
	• AoE Shape: None | Circle | Cone | Line
	• Trajectory Type: Linear | Arc | Homing | Scatter
- Rationale: clearer authoring and fewer mistakes when editing discrete params.
- Roadmap: mark Enum Dropdowns started and list current conversions under 2.4.
- README: add note under Skills Visuals about the new enum dropdowns.

Verification
- Build: Debug (SDL2) with -j12 completed.
- Tests: ctest from build dir with -C Debug -j12 --output-on-failure → 100% PASS (594/594).

Touched
- src/debug_overlay/panel_skills/panel_skills_visuals.c (combos)
- roadmaps/implementation_plan_skill_creation_suite.txt
- README.md

feat(skills/testing): Vector2 input widget + cast/target position support; runtime sim JSON accepts cast_pos/target_pos; tests green

- Widgets: add overlay_input_vec2 (X/Y) with themed visuals, proper focus/hover behavior.
- Skills panel (Testing): new "Cast Pos" and "Target Pos" controls wired via overlay_input_vec2.
- Runtime: skill_simulate_rotation now parses optional cast_pos/target_pos from profile JSON and seeds RogueSkillCtx accordingly (back‑compatible when omitted).
- Roadmap: mark Vector2D Input Widget DONE; note Testing tab wiring and JSON keys.
- README: document Testing positions and simulation JSON keys.

Verification
- Build: Debug (SDL2) with -j12 succeeds.
- Tests: ctest from build dir with -C Debug -j12 → 100% PASS (594/594).

Touched
- src/debug_overlay/widgets/overlay_widgets.h/.c (overlay_input_vec2)
- src/debug_overlay/panel_skills/panel_skills_testing.c (controls + JSON)
- src/core/skills/skills_runtime.c (vec2 parse + ctx)
- roadmaps/implementation_plan_skill_creation_suite.txt
- README.md

feat(overlay): Esc clears focus; Ctrl+S save; Ctrl+Z/Y undo/redo; Shortcuts updated (M6.3)

- Input: capture S/Z/Y to support Ctrl+S and Ctrl+Z/Y. Kept headless-safe; resets per frame.
- Esc now clears widget focus globally (g_ui.focus_index = -1) for fast keyboard exit.
- Ctrl+S contextual save:
	• Skills panel: Save Overrides JSON and refresh (toast confirms).
	• Map panel: Save to build/map.json via debug API (toast confirms).
- Ctrl+Z / Ctrl+Y: route to Map editor undo/redo when the Map panel is visible.
- Shortcuts panel: cheat sheet includes Esc, Ctrl+S, and Ctrl+Z/Y.
- Roadmap (improvements_debug_overlay.txt): Milestone 6.3 updated with the above; next steps noted (generalize save and undo stacks across panels).

Verification
- Build: Debug (SDL2) with parallelism (-j12) succeeded.
- Tests: Full CTest suite from build dir passed 100% (594/594) with --output-on-failure.

Touched
- src/debug_overlay/overlay_input.{h,c} (new key flags for S/Z/Y)
- src/debug_overlay/overlay_core.c (global Esc/Ctrl+S/Z/Y handlers)
- src/debug_overlay/panel_shortcuts/panels_shortcuts.c (updated tips)
- roadmaps/improvements_debug_overlay.txt (status update)

debug(overlay): centralize panel gizmo colors via OverlayTheme in Map & Audio/VFX; build/tests green

- Map panel: replaced hard-coded SDL colors with theme accents for brush ghost (fill/border),
  collider outlines, pathfinding heatmap, and autotiling hints. Uses overlay_theme_get() and
  accent_1/accent_2 with tuned alpha.
- Audio/VFX panel: themed falloff ring gizmo (accent_2) with safe fallback when theme unavailable.
- Roadmap: mark theming migration complete for these panels; note verification.
- README: document theme controls and recent overlay theming adoption across panels.

Verification:
- Built Debug (SDL2) with parallelism (-j12) successfully.
- Ran full CTest from build dir: 100% PASS (594/594), no regressions.

Touched files:
- src/debug_overlay/panel_map/panels_map.c
- src/debug_overlay/panel_audio_vfx/panels_audiovfx.c
- roadmaps/improvements_debug_overlay.txt
- README.md (docs)

Debug Overlay: Theme controls (preset/DPI/font size) + widget theming migration; docs/roadmap; tests green

- Theme API: add `overlay_theme_set_font_size(int)` with clamping and persistence; existing preset/DPI setters wired to the System panel.
- System panel: new Theme controls for Preset (Dark/Light/High Contrast), DPI scale, and Font Size; selections persist to overlay_theme.json.
- Widget theming: migrate common controls to overlay_theme colors (combo label text, tree node bg/border/text accent, color swatch border). Items/System panels now consistently themed.
- README: add a Theme & Accessibility tip under Debug Overlay (how to switch presets, DPI, font size; persistence path).
- Roadmap: mark progress under Milestone 0.1 (overlay_theme) and note remaining panel migrations.
- Verification: Debug SDL2 build and full CTest run passed 100% (594/594) with -j12 on Windows/MSVC.

debug overlay(items): adopt tuned row density defaults; add perf notes

- Items panel: switch default row height from 18→16 and padding from 2→1 via overlay_table_set_row_style; persists user override.
- Rationale: new headless perf smoke (test_overlay_table_perf_smoke) shows ~4–5% avg ms/frame improvement on 50k-row synthetic table.
- README: document the tuned defaults and how to adjust; Roadmap: mark metrics collected and defaults chosen under 8.1.
- Verification: Debug build and full CTest run passed 594/594 (Windows/MSVC, -j12). No regressions.

Debug Overlay: add persistent split views to Skills and Map; doc updates

- Widgets: Adopt the new splitter (overlay_splitter_begin/end) in Skills and Map panels.
	• Skills: left pane for Tab + Skill selection; right pane hosts the content with a sticky
		validation banner. Width persists via overlay_prefs (key: skills.split).
	• Map: left pane retains editing controls; right pane is a preview/inspector placeholder
		for upcoming mini-map/tile palette. Width persists (key: map.split).
- Roadmap: mark Milestone 0.2 Layout primitives as Met; update near-term notes to reflect
	splitter adoption and persistence.
- README: add a short note on split views and overlay preferences persistence
	(overlay_prefs.json) and list current adopting panels.
- Build/tests: no code path changes beyond UI layout; prior full suite run was green (593/593).

Add HEAL EffectSpec kind (enum/parser/loader/runtime), introduce RogueSkillExecState + getter, add per-skill profiling timestamps; Debug SDL2 build -j12; ctest 587/587 PASS

Debug Overlay: Command Palette (Ctrl+Shift+P) with default commands; docs/roadmap
Debug Overlay: Items panel authoring + virtualization; compile fix; docs/roadmap

- Items: add schema-backed "Create New Item" wizard (ID/Name/Category/Level/Rarity/Stack Max/Base Value/Dmg/Armor/Sockets).
	• Validates and sanitizes inputs; suggests unique id; calls rogue_item_debug_create.
- Duplicate-as-template: one-click prefill from selected row into the wizard with a new id suggestion.
- List virtualization (prototype): introduce a row-offset slider and fixed visible_rows=20 for stable headless rendering of large registries; selection persists; clamped bounds.
- Compile fix: remove references to internal UI globals from panels_items.c; rely on public overlay/widget APIs.
- Persistence: splitter widths per panel via overlay_prefs (keys: items.split, skills.split, map.split).
- Docs: README adds Items tips (duplicate-as-template + virtualization); roadmap marks Milestone 2.1 (Items wizard) and 8.1 (virtualization prototype) as progressed; near-term next steps updated.
- Tests: Debug SDL2 build and full ctest suite all green (593/593).

Debug Overlay: Items list search + header sort toggles; command action; MSVC fix; tests green

- Items panel: add case-insensitive search on id/name and header-based sorting (asc/desc) for id/name/category/rarity; keyboard Up/Down scroll keeps selection in view; works with the virtualized list window.
- Commands: register “Items: Create New” in the Command Palette to open the wizard focused.
- MSVC portability: remove a nested static function from panels_items.c (not supported by MSVC) by lifting it to file scope and wiring through registration.
- Roadmap: mark near-term step 5 DONE; README tips updated.
- Build/Tests: Debug SDL2 build (-j12) and full ctest run pass cleanly (593/593).


- Commands: minimal palette UI with substring filter and keyboard navigation; toggle with Ctrl+Shift+P.
	• Registered defaults: Validation (Run Now, Show Panel), Skills (Save/Load Overrides),
		Content Graph (Export DOT), Panels (Open Items/Skills/Map).
- API: overlay_command_register, overlay_invoke_action, overlay_register_default_commands.
- Init: seed defaults on overlay_init; headless-safe command invocations.
- Docs: README tip added; roadmap 1.2 marked stub Met.

Build: Fix vcpkg manifest error in MSBuild tasks

- Tasks: Add /p:VcpkgEnableManifest=true to MSBuild commands in tasks.json for thread tests and sync tests.
- Build: Verified Debug SDL2 build with -j12 completes successfully.
- Tests: Full ctest suite passes (593/593) with no regressions.

Debug Overlay: Items table smooth scrolling (wheel + Home/End + Shift), hover-aware; docs/roadmap; tests green

- Input: Add per-frame mouse wheel capture to overlay input (SDL_MOUSEWHEEL → OverlayInputState.mouse_wheel_y). Resets each frame.
- Widgets/Table: Track table hover (headers/rows) and expose overlay_table_hover_wheel(&dy) to consumers.
- Items panel: When the table is hovered, wheel scrolling adjusts the virtualized row offset; added Home/End jumps and Shift-accelerated step for Up/Down. Offset clamped to bounds; slider retained for headless determinism.
- Readme: Updated Items tips to mention wheel/Home/End/Shift controls.
- Roadmap: Mark virtualization + wheel/keyboard smooth navigation as DONE; note scrollbar rendering/drag as next.
- Build/Tests: Debug SDL2 build (-j12) succeeded; ctest full suite all green (593/593).

Debug Overlay: Items table vertical scrollbar (click-to-page + drag), integrated with virtualization; docs/roadmap; tests green

- Widgets/Table: New overlay_table_scrollbar(total_rows, visible_rows, &row_offset) draws a right-aligned vertical scrollbar with theme colors (track uses table_border; thumb uses accent_1/hover accent_2). Click track to page, drag thumb to scroll; clamps row_offset.
- Items panel: Scrollbar wired in before table end; stays in sync with wheel/keyboard/slider paths; Home/End continue to jump to bounds.
- Readme: Added tip about the new scrollbar interactions.
- Roadmap: Mark scrollbar complete under Milestone 8.1; keep FPS metrics as a next step.
- Build/Tests: Full ctest suite from build dir passed 100% (593/593) on Debug (SDL2, -j12).

Debug Overlay: Theme migration sweep (Skills Effects Node Graph + Content Graph), MSVC decl-order fix; docs/roadmap; tests green

- Theming: Skills Effects Node Graph visuals (canvas/nodes/edges/labels) and Content Graph SDL preview/diagnostics now consume overlay_theme colors and accents for consistent contrast across presets.
- MSVC: Fixed declaration-before-statement issues in Skills Effects by moving typedefs/static state to the top of the block (C mode under MSVC).
- Roadmap: Updated 0.1 status to list migrated panels; added MSVC note under Implementation constraints; near-term next steps reflect additional theme consumers.
- README: Added notes about themed visuals for Skills Effects and Content Graph plus the MSVC compatibility reminder.
- Verification: Debug SDL2 full rebuild and full CTest run green (594/594) with -j12 on Windows/MSVC.

Debug Overlay: Global Search (Ctrl+K) with jump-to Items/Skills; selection setters; docs/roadmap; tests green

- Feature: Add Global Search overlay (Ctrl+K) with a themed popup and substring matching over Items and Skills. Up/Down navigate; Enter jumps to the target panel and selects the entry.
- Navigation: Implement panel-level deferred selection setters (rogue_overlay_items_set_selected_index / _skills_) that apply on the next frame and scroll the view to keep the row visible in the virtualized table.
- Core wiring: overlay_core toggles and renders search; overlay_nav helpers open panels and call selection setters.
- Build: Include overlay_search.c in the overlay target so the module compiles/links on Windows/MSVC.
- Docs: README adds a Global Search tip; roadmap Milestone 1.1 updated with scope/status and verification.
- Verification: Debug (SDL2) full suite passed 100% (594/594) with -j12; manual smoke confirms jump-to selects and scrolls correctly. No regressions.

debug overlay: non-modal toast notifications wired across core flows; tests green

- Add overlay_toast lane rendering in overlay core; headless-safe guards
- Skills panel: toasts on Save/Load overrides and Auto-Reload activity
- Validation panel: start/complete toasts with summarized results & severity
- Command Palette: toasts for Validation: Run Now and Skills save/load actions
- Input/state untouched for gameplay; notifications are non-blocking with TTL
- Docs: README tips updated; roadmap M0.3 marked implemented
- Build/Test: Debug (SDL2) -j12; full CTest 100% (594/594)

Debug Overlay (Items): Templates & Presets (M2.2) with JSON-backed storage; docs/roadmap; tests green

- Items panel: add curated Presets and user Templates to the Create wizard. Save/Load/Apply templates stored under `build/templates/items/*.json`; filenames sanitized.
- Persistence: uses json_io atomic writes and an ensure-dir helper for cross-platform safety; errors surface via overlay toasts. Headless-safe throughout.
- README: add a tip explaining Templates & Presets usage and the storage path.
- Roadmap: mark Milestone 2.2 Templates & Presets as implemented with verification notes.
- Verification: Debug SDL2 build and full ctest run from build dir passed 100% (594/594) with -j12.

Debug Overlay (Skills): "Duplicate From Selected" prefill in Overview (M2.3 partial); docs/roadmap; tests green

- Skills panel: add "Duplicate From Selected" button that opens the Create wizard prefilled from the current selection.
	• New name uses a _Copy suffix; carries over max rank, passive flag, and timing fields (cast/cooldown/channel).
	• "Copy Coeffs" default enabled for duplicates; Create flow clones coefficient params when checked.
- README: add a tip under Skills describing the duplicate flow and optional coeff cloning.
- Roadmap: update Milestone 2.3 status to note Skills coverage and verification.
- Verification: Debug build (SDL2) and full CTest run from build dir passed 100% (594/594) with -j12.

Debug Overlay (Entities): Duplicate-as-template + initial Batch Creator (M2.3/M2.4); docs/roadmap; tests green
Debug Overlay (M2.4): Batch Creator for Items & Skills with pattern preview/validation; docs/roadmap; tests green

- Items: Added "Batch Create (Items)" with printf-style Id/Name formats, Start/Count, and a live preview table that validates format errors, id length, and duplicate ids via registry lookup. Applies using current Create-wizard fields; toasts summarize results.
- Skills: Added "Batch Create (Skills)" with Name format + Start/Count and optional Passive/Max Rank/timing fields. Preview lists generated names and flags duplicates against the registry; summary printed after apply.
- Roadmap/README updated to document usage and mark Milestone 2.4 progress.
- Verification: Debug (SDL2) build with -j12 succeeded; full CTest passed 100% (594/594) on Windows/MSVC.


- Entities panel: add "Duplicate Enemy" with dx/dy offset using new headless-safe API `rogue_entity_debug_duplicate`.
- Batch Creator (Entities): add a lightweight "Spawn Ring Around Player" that places N duplicates at a chosen radius using an 8-directional pattern (repeating as needed).
- Roadmap: update Milestone 2.3 to include Entities duplicate; start Milestone 2.4 with Entities batch creator slice and next steps.
- README: add Entities tips for Duplicate and Batch Creator usage.
- Verification: Debug (SDL2) build with -j12 and full CTest run from build dir passed 100% (594/594). No regressions.

Debug Overlay (M4): Items Preview (textual) and Skills Live Preview (textual); docs/roadmap; tests green

- Debug Overlay (M4.3): Audio/VFX previews
	- Audio/VFX panel: positional audition toggles (Enable Positional, Listener follows player), Falloff Radius control, and "Play Sound @ Cursor" via FX bus at cursor world pos.
	- Textual ASCII attenuation preview sampling effective gain at 0/25/50/75/100% of falloff; optional falloff ring gizmo (SDL‑guarded).
	- Docs: README tips updated; Roadmap M4.3 section populated.
	- Verification: Debug build (SDL2) with -j12 succeeded; full ctest suite passed 100% (594/594). No regressions.

- Items (M4.1): Add headless-safe Preview section in the Create wizard showing Category, Rarity, Level, Base Stats (Damage dmin–dmax or Armor), Sockets, and an informational Est. Value heuristic. Renders as a small two-column table and updates live.
- Skills (M4.2): Add a headless-safe textual "Skills Live Preview" on the Testing tab summarizing Skill name/type, Cast/Cooldown timing, Coeffs, Primary Effect and Effect Nodes (with delays/repeats), and an estimated total tick count derived from repeats/periods. Complements the existing sprite preview.
- README: Document both previews in Debug overlay tips.
- Roadmap: Mark 4.1 implemented and 4.2 textual preview added with verification notes.
- Verification: Debug build (SDL2) -j12; full CTest suite remains 100% green (594/594).

Debug Overlay (M4.4): Map/editor preview upgrades + Undo/Redo; docs/roadmap; tests green

- Map panel: brush ghost, colliders overlay, pathfinding heatmap, autotiling hints (SDL‑guarded; headless‑safe fallbacks). Local 64‑entry Undo/Redo ring with Clear; Ctrl+Z / Ctrl+Y bindings. Edits invalidate tile sprite LUT for instant refresh.
- Roadmap/README updated to reflect M4.4 completion.
- Verification: Built Debug (SDL2) with -j12 and ran full ctest from build dir: 100% PASS (594/594).

feat(overlay M6.2): expand icon+label adoption across panels; Content Graph controls converted; docs updated; gates green

- Icon system: continue adoption of overlay_icon_button across frequent actions with consistent padding, hover/pressed states, focus ring, and tooltip ingestion.
- Panels updated:
	• Map: Undo/Redo; Save/Load JSON
	• Skills: Save/Load Overrides JSON
	• Audio/VFX: Play Sound / Spawn VFX @ Cursor
	• Dialogue: Start / Advance; Reset
	• Validation: Validate All
	• Content Graph: Compute All Hashes; Export DOT/JSON; Fit to Selection (F); Clear Pins; List Orphans; List Hubs (top 10); Export Subgraph DOT/JSON; Back
- Docs: README Debug Overlay tips updated to reflect broader icon adoption; Roadmap M6.2 Integration section lists the covered controls.
- Verification: Built Debug (SDL2) with parallelism (-j12) and ran full CTest from the build directory with -C Debug -j12 --output-on-failure → 100% PASS (594/594). No regressions.
- Notes: All draw paths remain SDL‑guarded for headless stability; MSVC C compliance maintained (declarations before statements; no VLAs).

Debug Overlay (M5.1): Content Graph pan/zoom + fit + isolate subgraph; docs/roadmap; tests green

- Content Graph mini‑view: Right‑mouse drag pan, mouse wheel zoom centered on cursor (clamped 0.5×–2.0×), and Fit‑to‑Selection via button or F key. Selection persists; clicking nodes drills selection and updates a breadcrumb path.
- Isolate subgraph: Optional filter limits the list to nodes reachable from the current selection up to the current Preview depth (BFS). On‑view hint text: "RMB drag=pan, Wheel=zoom, F=fit". SDL‑guarded drawing keeps headless tests stable.
- README and roadmap updated to document M5.1 and note pinning/manual node placement as pending.
- Verification: Debug (SDL2) build with -j12 succeeded; full CTest run from build dir 100% green (594/594).

Overlay: Content Graph pinning/manual repositioning + Clear Pins.
Worldgen: SIMD availability gating + simplified path; tests green.

debug overlay(entities): add themed world-space selection gizmo; tests green (594/594)

- Entities panel: draw a lightweight selection gizmo around the selected entity (rectangle + small crosshair) using OverlayTheme accent_1 with tuned alpha; SDL-guarded with a safe fallback color when theme or renderer is unavailable. Added a toggle to show/hide the gizmo in panel options.
- Docs: README tips updated to mention the Entities selection gizmo; Roadmap (M0.1) updated to include Entities under themed gizmos and verification notes.
- Verification: Debug build (SDL2) with -j12 succeeded; full CTest run from build directory passed 100% (594/594).
- Touched: src/debug_overlay/panel_entities/panels_entities.c, README.md, roadmaps/improvements_debug_overlay.txt

Debug Overlay (M6.3): Shortcuts panel ('?') + Alt+1..9 quick panel switching; docs/roadmap; tests green

- Input: extend OverlayInputState to capture '?' (via SDL_TEXTINPUT) and numeric keys 1..9 for Alt chord handling.
- Global shortcuts: Alt+1..9 open common panels: 1 System, 2 Items, 3 Skills, 4 Map, 5 Audio/VFX, 6 Entities, 7 Content Graph, 8 Validation, 9 Dialogue.
- Shortcuts panel: new "Shortcuts" panel listing core key bindings; toggle by pressing '?' anywhere in the overlay.
- Wiring: register the panel in overlay_panels; include new source in CMake overlay target.
- Docs: README adds a tip for '?' and Alt+1..9; Roadmap marks 6.3 slice implemented and notes remaining work (Esc clears focus, Ctrl+S, broader Ctrl+Z/Y coverage).
- Verification: Built Debug (SDL2) with -j12 and ran full CTest from build dir: 100% PASS (594/594). No regressions.
- Touched: src/debug_overlay/overlay_input.{h,c}, src/debug_overlay/overlay_core.c, src/debug_overlay/overlay_panels.c,
		   src/debug_overlay/panel_shortcuts/panels_shortcuts.c, CMakeLists.txt, README.md, roadmaps/improvements_debug_overlay.txt

Debug Overlay (M6.1): Micro-interactions for core widgets + Tree Node implementation; docs/roadmap; tests green

- Widgets: add accessible hover/pressed/focus states to Button, Checkbox, SliderInt/Float, InputText, and Combo. Focus rings use overlay theme accents (accent_1/accent_2) and respect the Colorblind-safe palette. Added a small shade helper to lighten/darken fills consistently.
- Tree Node: implement a collapsible header row with arrow indicator (▸/▾), keyboard toggle (Enter/Space), mouse click handling, and a focus ring. Panels (Map, Validation, Content Graph) now link against this implementation.
- Roadmap: update Milestone 6.1 status with details and verification; README gains short tips on micro-interactions and the Tree Node control.
- Headless-safe: SDL draw calls are guarded; logic remains testable without a renderer.

Verification
- Build: Debug (SDL2) with parallelism (-j12) succeeded.
- Tests: Full CTest suite from the build directory passed 100% (594/594) with --output-on-failure; no regressions.

Touched
- src/debug_overlay/widgets/overlay_widgets_controls.c (micro-interactions; overlay_tree_node)
- roadmaps/improvements_debug_overlay.txt (M6.1 status)
- README.md (tips)

Debug Overlay (M6.1): Tooltip system (hover-delay) + widget integration; docs; tests green

feat(overlay M5.2): Content Graph group halos + “Explain Path”; docs updated; regression gate green (594/594)

- Group halos: softly tint nodes that share the selected root’s top-level group in the SDL mini‑view.
- Explain Path: BFS within the current Preview Depth to compute a parent map, backtrack to form a breadcrumb from root→target, and highlight the nodes/edges along that path using accent colors. Target persists for ongoing highlight.
- Depth cue: subtle border emphasis for the outer preview layer.
- Compliance: MSVC C mode friendly (declarations before statements; no VLAs); SDL rendering guarded for headless safety.
- Docs: roadmap (M5.2) and README Content Graph section updated.

Verification
- Build: Debug (SDL2) with -j12 succeeded.
- Tests: Full CTest from build dir with -C Debug -j12 --output-on-failure → 100% PASS (594/594).

feat(overlay M5.3): Content Graph “List Missing Providers” finder; docs updated; regression gate green (594/594)

- Content Graph: new finder scans all declared dependencies and reports unresolved refs as
	`missing <dep> (referenced by <id>)`. Exposed as an icon+label button beside Orphans/Hubs.
- MSVC C compliance maintained (declarations before statements; no VLAs). Headless-safe path used for CI.
- Roadmap (M5.3) and README updated to document the finder and its usage.

Verification
- Build: Debug (SDL2) with -j12 succeeded.
- Tests: Full CTest from build dir with -C Debug -j12 passed 100% (594/594).

- API: overlay_set_next_tooltip(const char*) lets panels attach a tooltip to the next widget call.
- Engine: new overlay_tooltip.{h,c} tracks hover rectangles per widget id and, after ~400ms, renders a themed tooltip near the cursor; headless-safe (SDL-guarded).
- Widgets: Button, Checkbox, SliderInt/Float, InputText, Combo, and Tree Node consume the next tooltip and register their hover rects automatically.
- Docs: README usage note; roadmap updated under 6.1.

Verification
- Build: Debug (SDL2) with -j12 succeeded.
- Tests: Full CTest suite from build/ passed 100% (594/594) with --output-on-failure.

Touched
- src/debug_overlay/overlay_tooltip.{h,c}
- src/debug_overlay/overlay_core.c (render hook)
- src/debug_overlay/widgets/overlay_widgets_core.c (API)
- src/debug_overlay/widgets/overlay_widgets_controls.c (integration)
- CMakeLists.txt (compile unit)
- README.md, roadmaps/improvements_debug_overlay.txt

feat(overlay M5.4): Content Graph snapshots A/B, A→B diff overlay, and JSON diff export; docs/roadmap; gates green

- Snapshots: Capture subgraph snapshots (root + BFS up to Preview Depth) into fixed-size buffers (no VLAs; MSVC C-friendly) via UI buttons “Capture A” and “Capture B”.
- Diff compute: “Compute Diff A→B” produces Added and Removed edge sets and per-node degree deltas; simple cycle detection within the focused subgraph.
- Overlay: Optional “Show Diff Overlay” tints Added edges with accent_1 and Removed with a high-contrast error red; Explain Path highlights remain in text_accent. All SDL drawing is guarded for headless tests.
- Export: “Export Diff JSON” writes build/content_subgraph_diff.json including degree deltas and cycles for A→B.
- Docs: README Content Graph section now includes a Snapshots & Diff usage guide; roadmap Milestone 5.4 updated with details and verification.

Verification
- Build: Debug (SDL2) with -j12 completed successfully.
- Tests: ctest -C Debug -j12 --output-on-failure → 100% PASS (594/594).

Touched
- src/debug_overlay/panel_content_graph/panels_content_graph.c (UI/state + diff/export paths)
- README.md (Content Graph: Snapshots & Diff)
- roadmaps/improvements_debug_overlay.txt (M5.4 status)

feat(dungeon/phase0): taxonomy foundation (archetypes, id ranges, depth→ΔL, biome→theme tags) + unit test; docs updated; gates green

- New module: world_gen_dungeon_taxonomy.{h,c}
	• RogueDungeonArchetype enum + name accessor
	• Reserved id ranges + validators (templates/events/objectives)
	• Depth→target level delta mapping (monotonic, saturating)
	• Biome→theme tag utility for early theming hooks
- Tests: tests/unit/test_dungeon_phase0_taxonomy.c validates invariants
- Build: wired taxonomy module into rogue_core
- Roadmap: Phase 0 updated (0.2/0.3/0.4/0.5 Done; 0.1 Partial)
- README: notes updated; latest suite now 595/595

Quality gates
- Build: Debug (SDL2) with -j12 OK
- Tests: ctest -C Debug -j12 → 100% passed (595/595)

feat(dungeon/phase2): room templates scaffolding (ASCII→stamp), rotations/reflections, exits; unit test; docs updated; gates green

- Module: add room templates engine (ASCII parse → transforms (rotate/reflect) → exit extraction → stamp into RogueTileMap with bounds checks). Wired into rogue_core.
- Tests: new `tests/unit/test_dungeon_phase2_room_templates.c` validates bounds, exit mapping/alignment, and rotation determinism.
- Roadmap: mark 2.2 Partial and 2.5 Done; Status Delta updated with Phase 2 progress and latest test count.
- README: reflect latest full-suite count (598/598) and note the new dungeon test.

Verification
- Build: Debug (SDL2) with -j12 OK
- Tests: ctest --test-dir build -C Debug -j12 --output-on-failure → 100% PASS (598/598)

feat(dungeon/phase2): JSON room template loader + door metadata + deco markers; tests and docs; gates green

- Loader: minimal, dependency-free JSON parser for a single room template object with fields:
	id, cls, biome_tags, encounter_slots, hazard_slots, puzzle_slot, grid[], exits[], doors[] (type/key_id), deco[].
	Key-order independent; preserves previously parsed fields when parsing the ASCII grid.
- Door metadata: normal/locked/secret types with optional key_id; transforms and stamping preserve semantics.
- Deco markers: parsed and transformed; rendering/collision layering deferred.
- Tests: new unit `tests/unit/test_dungeon_phase2_room_templates_json.c` validates fields, doors/exits/deco, and transform alignment vs stamped tiles.
- Roadmap: mark 2.1 Done, 2.3 Done, 2.4 Partial; Status Delta updated.
- README: note the new JSON loader test and latest suite count.

Quality gates
- Build: Debug (SDL2) with -j12 OK
- Tests: ctest --test-dir build -C Debug -j12 --output-on-failure → 100% PASS (599/599)
