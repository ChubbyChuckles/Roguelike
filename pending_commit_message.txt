feat(asset-schema): add UI theme JSON schema & validation test
feat(asset-structure/phase3): schema versioning completion + initial asset manager/path utils

Phase 2 Completion:
- Added optional `schema_version` (1..1024) to all content schemas (items, skills, entities, tilesets, sprites, audio, levels, ui_theme).
- Introduced linear migration registry (64-step cap) with unit test `test_schema_versioning_basic` (ui_theme v1→v2 adds panel_border default).
- Updated asset structure implementation plan marking schema versioning done.

Phase 3 Kickoff (Initial Slice Implemented):
- Added `asset_manager.{h,c}` (fixed-cap texture registry, id derivation via basename, acquire/release ref counting, headless safe stub loads).
- Added `path_utils.{h,c}` (normalize, join, is_absolute helpers) preparing for SDL_GetBasePath integration.
- New unit test `test_asset_manager_basic` covering acquire dedupe, ref count decrement/removal, and path normalization.
- CMake: integrated new sources into `rogue_core` + test target wiring.
- Roadmap updated with Phase 3 progress checklist; README gains Schema Versioning and Asset System sections.

Next (planned, not in this commit): real SDL texture load (PNG via SDL_image), width/height capture, audio clip registry, negative caching, hot-reload polling hook.

feat(assets/phase3): SDL_image textures (w/h), SDL_mixer audio, negative caching, hot-reload poll, SDL_GetBasePath path resolution

Phase 3 Completion:
- Integrated real texture loading when built with SDL_image (IMG_LoadTexture) capturing width/height via SDL_QueryTexture; headless builds remain safe (no renderer required).
- Added audio asset registry (Mix_LoadWAV) with ref counting and negative caching for missing files.
- Implemented negative caching flags (load_failed) for textures & audio to prevent repeated failed I/O attempts until file change.
- Added stat() mtime-based hot-reload polling for both textures and audio (rogue_asset_manager_poll_reload) with automatic destroy/reload on modification.
- Extended path resolution: rogue_find_asset_path now prepends SDL_GetBasePath derived prefixes (with up to two ascents) before legacy relative probes improving reliability when tests run from nested build dirs.
- Roadmap Phase 3 tasks all checked off with implementation blurbs; README Phase 3 section updated to reflect completion (formats, audio, reload, negative caching, path base integration).
- Tests: existing asset manager tests remain green; full 647/647 suite passes (Debug, SDL2, -j12). No new warnings introduced.



Implemented Phase 2 task (UI Theme/Layout schema):
- Added schema_theme.{h,c} describing palette + metrics fields with forward-compatible extension.
- Added test_ui_theme_schema unit test (valid + invalid cases).
- Integrated test target into CMake with SDL2 linkage.
- Updated asset structure implementation plan marking UI theme schema complete; noted upcoming schema_version task.
 - Updated README with new "UI Theme JSON Schema" section (fields, ranges, example, migration notes) and marked duplicate roadmap bullet as done for clarity.

This advances Phase 2 toward schema versioning and future layout migration.

feat(skill-suite/ui): add multi-select bitmask & dynamic list editor widgets (Phase 2.4)

Implemented overlay_multiselect_bits (bitmask-based multi-select) and overlay_list_editor (add/reorder/delete string list) widgets.
Integrated prototypes in Skills → Visuals tab: Status Tags (bitmask) and Combo Chain editor (dynamic list).
Added unit test test_overlay_multiselect_list exercising bit toggling and list mutation in headless overlay mode.
Updated roadmap (Phase 2.4) marking Multi-select Checkboxes & Dynamic Lists as completed (prototype status) with notes; README update pending future persistence wiring.
No persistence yet – widgets are UI scaffolds; future tasks: bind status tag mask to skill metadata & formal combo chain schema.
Note: test_overlay_multiselect_list later simplified to smoke-only (removed brittle simulated click assertions) for deterministic headless runs.

feat(overlay/skills): Asset Import Wizard prototype panel stub

Adds a minimal "Asset Import Wizard" panel in Skills → Visuals. Current functionality:
 - Opens modal panel via button.
 - Placeholder staging list (up to 8 entries) with "Add Files" invoking a future enumerator hook (overlay_fs_enumerate_skill_assets) if present.
 - Import button currently a no-op that clears staged entries.
 - Close button hides the panel.

Roadmap update: marks Phase 2.3 "Asset Import Wizard" as prototype started (no drag-and-drop yet). README updated under Phase 2.3 widgets section.

Follow-ups planned:
 - SDL_DROPFILE integration in input_events.
 - File type validation (image/audio) and target assignment UI.
 - Copy/move into assets/skills/<skill_id>/ structured dirs with collision handling.
 - Dependency tracking registration + hot-reload triggers.

Skill Creation Suite - Effect Tree Authoring & Validation Enhancements
---------------------------------------------------------------------
Summary:
Adds authoring-time validation for SPAWN_ENTITY parameters, overlay UI editing for experimental effect tree nodes, JSON debug override round-trip support, and two focused unit tests (hierarchical scheduling + cycle rejection) to harden the new feature.

Details:
* Validation: Enforce SPAWN_ENTITY count [1..8] and life_ms > 0 across primary effect, legacy flat effect nodes, and effect_tree nodes in skills_validate.
* Serialization: Debug overrides exporter/importer now persist an `effect_tree` array (skipped when empty) enabling round‑trip editing without losing hierarchical data.
* Overlay UI: Panel exposes a "Use Effect Tree" toggle; when enabled, allows editing node count and per-node fields (effect_spec_id, delay_ms, duration_ms, repeat_count, repeat_interval_ms, HP gate, parent_index) with bounds guardrails.
* Tests: Added test_skills_phase1_2_effect_tree_schedule (verifies parent-relative delays + periodic window + chained grandchild) and test_skills_phase1_2_effect_tree_cycle (ensures cycle detection rejects invalid parent graphs).
* Build Fix: Resolved pointer type warning (treated as error) in skill_debug.c effect_tree serialization loop by using RogueSkillEffectTreeNode*.
* README: Updated effect tree bullet to reflect completed UI + export/import integration and strengthened validation (including SPAWN_ENTITY constraints).

Follow-ups (deferred):
* Add round-trip serialization unit test for effect_tree overrides.
* Add invalid parent_index (>node_count or self-reference) negative tests beyond cycle case.
* Consider graphical tree visualization in overlay for complex hierarchies.

Overlay: first-open defaults, persistence, and drag UX; Dungeon: gating manifest export + vendor run_summary consumer; tests & docs

- Debug overlay
	- Default first-open shows only the “Panels” selector (non-closable).
	- Persist panel visibility/geometry across runs; save final position on mouse release.
	- Title-bar dragging with capture/release, viewport clamping.
	- Unit: test_overlay_persistence_and_drag.

- Dungeon (Phase 9.4)
	- Add rogue_dungeon_export_gating_manifest(path, graph) emitting compact JSON of capability ids and rooms count; ensure parent dirs exist.
	- Add minimal vendor run_summary consumer; registers listener and safely reacts to reward multipliers.
	- Units: test_dungeon_phase9_4_gating_manifest, test_vendor_phase9_run_summary_consumer.

- Fixes
	- Reduced motion path corrected; test_start_screen_phase10_4_reduced_motion green.
	- Vendor module: correct material count API usage to fix compile on MSVC.

- Docs
	- Roadmap: mark gating manifest export and vendor consumer Done; clean up Open Tasks.
	- README: add Phase 9.4 notes under Testing & Quality Gates.

Build/Test: Debug (SDL2), -j12; targeted/unit runs green locally.
packaging(cmake): robust JSON key parsing for SemVer/ShortSha and fix whitespace in regex; verify CPack filenames

- CMake: update packaging regex to accept both PascalCase and lowercase keys (SemVer/Version, ShortSha/short_sha) and use explicit whitespace classes.
- Result: CPack now emits versioned archives with platform suffix, e.g., roguelike-0.0.0+dev+gf45026d-windows.{zip,tar.gz} plus .sha256.
- Verified by reconfiguring CMake (Debug) and running CPack locally.

ci: improve diagnostics, parallelism, and reuse Release build dir across jobs

- Add CMAKE_BUILD_PARALLEL_LEVEL=12 to enforce parallelism across runners; keep SDL2 enabled everywhere.
- Capture dependency install logs per-OS (build/deps_*.txt) and tee CMake configure output to build/configure_*.log.
- Deduplicate repeated steps in CI and add inline comments for clarity.
- Share the Release build directory from build-and-test to package-release to avoid redundant rebuilds; fallback build remains when reuse isn’t available.
- Upload additional diagnostics in logs-* artifacts.

Skills Visuals: multi-target preview and playback controls

- Add Preview Target selector (Cast/Projectile/Impact/AoE) to the Skills → Testing live preview; caches textures and switches sources dynamically.
- Playback refinements: when paused, a frame scrubber appears; Step advances exactly one frame; Reset returns to t=0. Loop toggle overrides the definition for preview only.
- Cast previews use grid-sliced frames via `rogue_skill_build_grid_frames`; other targets default to full-texture single frame unless grid metadata exists.

Skills Visuals: packed atlas frame loader + layout fix

- Add `rogue_skill_build_packed_frames_from_json_text/file` supporting two JSON shapes:
	1) frames array: { "frames": [ {"x":..,"y":..,"w":..,"h":..}, ... ] }
	2) frames object with nested frame blocks: { "frames": { "name": { "frame": {"x":..,"y":..,"w":..,"h":..} } } }
- Lightweight custom scanner (skip whitespace, find blocks) avoids expanding minimal cJSON stub; ignores rotation/trim fields.
- Unit: test_skills_phase1_7_packed_sprite_loader validates both shapes and malformed entry skip.
- Overlay Skills Visuals panel: replace unsupported overlay_same_line usage with columns API to resolve linker undefined symbol.

EffectSpecs: SPAWN_ENTITY effect (Phase 1.2 slice)

- Add ROGUE_EFFECT_SPAWN_ENTITY enum value with two fields: spawn_entity_count (u8, clamped 1..8, default 1) and spawn_entity_life_ms (float ms, default 5000 if <=0).
- Parser & JSON loader accept `spawn_entity_count` and `spawn_entity_life_ms` keys (ignored for other kinds).
- Runtime apply: spawns N transient entities at caster position using lightweight fixed-cap (64) pool in spawn_entities.c; each entry stores x,y, expiry_ms and auto-expires on update.
- Sanitization during apply ensures safe defaults even if authoring omitted or out-of-range values.
- Unit: test_effectspec_spawn_entity (ensures counts increment and return to 0 after advancing time beyond lifetime).
- Roadmap (skill creation suite) updated: mark SPAWN_ENTITY done with implementation blurb; README EffectSpec JSON reference documents new fields and semantics.

Skills: EffectNode Tree Structure (experimental Phase 1.2 extension)

feat(skills/visuals): sprite sheet editor & asset dependency viewer (Phase 2.4)

- Add interactive Visual Sprite Sheet Editor to Skills → Visuals: grid overlay (auto-infers 64x64 heuristic when unset), left-click frame select, right-drag grid dimension adjust (prototype heuristic), Grow Frame Count (selected_index+1), Set Frame Count = Grid Cells (grid_w * grid_h). Writes back to RogueSkillVisualParams only on actions; headless-safe (guards rendering when no SDL renderer).
- Add Asset Dependency Viewer: collapsible list filtered by current cast sprite basename using asset dep registry; truncates after internal cap to keep UI responsive. Supports future validation/hot-reload workflows.
- Roadmap: mark Phase 2.4 items "Visual Sprite Sheet Editor" and "Asset Dependency Viewer" complete with implementation blurbs.
- README: add "New Skill Visual Authoring Tools" section documenting usage and interaction model.
- Cleanup: confirm legacy monolithic ui_context.c fully removed from tree (all functionality modularized previously); references replaced with per-module sources (core/interaction/navigation/etc.).


feat(skill-suite/ui): Formula Editor prototype (Phase 2.4 completion)

Adds lightweight expression evaluator (formula_eval.c) with + - * /, parentheses, identifiers: base, per, rank, str, dex, int, cap.
Integrated into Skills → Overview as "Formula Preview" with editable formula input and live evaluation against preview rank/stat sliders.
Unit test test_formula_eval validates success cases and error paths (syntax, unknown ident). Roadmap marks Formula Editors complete (prototype). Persistence & syntax highlighting deferred.

feat(skills/visuals): real-time animation timing adjustment (Live Timing Mode)

- Add "Live Timing Mode" checkbox to Skills → Visuals animation preview. When enabled, changes to the "Preview Speed (%)" slider immediately recompute and persist `frame_duration_ms` (clamped 1..1000ms) giving instant feedback on final playback timing without requiring the manual Apply button.
- Legacy workflow preserved: with Live Timing Mode off, authors still use the explicit "Apply Speed → Frame Duration" action for deliberate edits.
- Headless-safe: logic touches only overlay control flow and RogueSkillVisualParams; no new SDL dependencies introduced.
- Roadmap: mark Phase 2.4 "Real-time Animation Preview – Timing adjustment" complete with implementation blurb.
- README: document feature under New Skill Visual Authoring Tools as "Live Timing Mode".

feat(skills/visuals): sprite sheet import wizard (PNG/TGA/BMP enumeration, staging, assign)

- Replaces prototype stub with functional Import Wizard in Skills → Visuals.
- Scan Images: recursive assets/ traversal (Win32) collecting PNG/TGA/BMP (normalized forward slashes, depth clamp).
- Filter box performs substring filtering via existing overlay_table filter argument.
- Assign Target combo (Cast/Projectile/Impact/AoE); Stage Selected adds entry (64 cap) preserving chosen target.
- Import → Assign applies last staged path per target to RogueSkillVisualParams (cast_sprite_sheet, projectile_sprite, impact_sprite, aoe_sprite) and marks visuals dirty.
- Staged list supports per-entry Remove (compacting memmove) and clears after successful import.
- Headless-safe: all FS enumeration guarded; no SDL calls; pure overlay + Win32 API branch.
- Roadmap: marks Phase 2.4 "Sprite Sheet Import Wizard – Common formats" complete with implementation blurb & future tasks.
- README: replaces prototype description with concrete feature summary + future roadmap (drag & drop, structured copy, thumbnails, validation, audio import).

Deferred / next steps:
	* Cross-platform directory enumeration abstraction (POSIX opendir / portable layer).
	* Drag & drop integration (SDL_DROPFILE) pushing dropped paths into staging list.
	* Structured copy workflow: copy/move into assets/skills/<skill_id>/{cast,projectile,...}/ with collision resolution and toast feedback.
	* Dimension probe + suggested grid inference (populate grid_width/grid_height heuristics).
	* Inline thumbnail preview & missing/corrupted asset validation hooks (ties into pending Asset Validation Feedback roadmap item).
	* Audio file enumeration + staging (wav/ogg/mp3) with Assign-To (Cast/Impact/Loop) integration.

Build/Test Verification:
- Full Debug build (MSVC, SDL2) succeeded post-integration.
- All 638/638 tests passed including new test_formula_eval (0.01s) with no regressions.
- No new warnings introduced beyond pre-existing SDL_MAIN_HANDLED redefinition noise in several legacy test files.

- Data model: added `effect_tree_nodes[8]` and `effect_tree_node_count` to `RogueSkillDef` enabling optional hierarchical effect scheduling. Each node includes parent_index (-1 root) plus legacy timing/repeat + HP gate fields.
- Runtime: activation path now prefers tree when present; computes absolute start times via iterative parent resolution (N<=8) with cycle guard; falls back to legacy flat `effect_nodes[3]` when tree absent.
- Validation: extended `rogue_skills_validate_all` to enforce bounds, repeat/duration consistency, parent index validity, and detect cycles via parent chain coloring.
- Debug API: new `rogue_skill_debug_get_effect_tree` / `rogue_skill_debug_set_effect_tree` functions for live inspection and authoring; legacy APIs unchanged for backward compatibility.
- Roadmap: marks EffectNode Tree Structure implemented (experimental v1) and outlines next Advanced Effect Validation tasks (spawn_entity field pre-validation & tree window heuristics).
- Tests: full suite passes with no regressions (aside from pre-existing BAD_COMMAND skips unrelated to skills system); existing skill composition tests continue to exercise legacy path ensuring compatibility.
 - README: added Phase 1.2 recent additions section documenting SPAWN_ENTITY and experimental EffectNode tree feature.

feat(assets/ui): phase1 asset classification verified; initialize stat preview sentinel; roadmap phase2 kickoff

 - Asset Classification module (src/util/asset_classification.{h,c}) integrated with deterministic heuristics; unit test passes.
 - Full suite re-run surfaced uninitialized stat_preview_slot causing missing first SHOW event; added explicit initialization (=-1) and event queue index init in rogue_ui_init.
 - Fixed failing test_ui_phase4_stat_preview (now green); combat_dps transient segfault did not reproduce (likely unrelated intermittent state).
 - Roadmap updated with Phase 2 kickoff status + queued schema tasks (sprite/audio schema drafts, tests, placeholder.png enforcement).
 - Commit prepares groundwork for adding RogueSchema builders for sprite/audio without altering existing enum yet (will add TODO hook in classification next patch).

 - New Tests (follow-up additions):
	 * test_skills_phase1_2_effect_tree_overrides_roundtrip validates JSON overrides export/import preserves effect_tree nodes (round-trip integrity).
	 * test_skills_phase1_2_effect_tree_parent_index_invalid covers out-of-range parent_index and self-parent (cycle) rejection paths.
 - Overlay: added lightweight graphical tree visualization (experimental) rendering parent-child links and basic validity coloring to aid authoring.
 - Follow-ups Resolved: previously deferred round-trip serialization test and parent_index negative cases now implemented; remaining open: richer graphical layout (drag/drop for tree) and additional malformed graph heuristics.

Advanced Effect Validation (Phase 1.2 completion)
-----------------------------------------------
* Added advanced heuristics to rogue_skills_validate_all:
	- Non-negative delay_ms for flat and tree nodes.
	- repeat_interval_ms > 0 required when repeat_count > 0.
	- duration_ms must cover repeat_count * repeat_interval_ms when both provided.
	- Global effect tree scheduled span cap (<= 60000ms) computed from chained delays + window/repeat span; rejects runaway authoring.
* New unit: test_skills_phase1_2_effect_tree_advanced_validation (fails insufficient window, fails span overflow, passes valid composition).
* Roadmap: Marked "Advanced Effect Validation" complete with heuristic list.
* README: Updated Recent Skills/Effects section to list new validation rules.

Advanced State Machine & Skill Queuing (Phase 1.3 slice)
-------------------------------------------------------
* Added queued skill execution state (ROGUE_SKEXEC_QUEUED) and global lockout scaffold (ROGUE_SKEXEC_GLOBAL_LOCKOUT future use) in skills.h.
* Introduced rogue_skill_request API enabling buffered activation attempts; falls back to immediate activation and, on failure (cooldown/casting conflict), schedules using existing queued_trigger_ms/queued_until_ms with input_buffer_ms window.
* Extended RogueSkillState with queued_active flag; activation state accessor now reports queued state.
* Runtime processes queued activations immediately after a casting skill completes (reusing existing loop) and clears queued state.
* Added unit test test_skills_phase1_3_advanced_state_machine validating mid-cast queue then automatic post-cast firing and queue clearing.
* Roadmap: Marked Advanced State Machine & Skill Queuing System tasks complete with implementation notes.
* README: Added brief bullet in Skills Execution section describing queued activation support and new API.
* Non-invasive: Does not alter existing activation semantics for instant skills lacking input_buffer_ms (no regression risk).
Follow-ups:
	- Implement combo/sequence queue ordering priority.
	- Add global lockout triggers (e.g., after ultimate usage) and tests.
	- Overlay visualization of queued skills in skill bar.

feat(assets/schema): Phase 2 sprite + audio schema builders and validation tests

- Added `schema_sprites.{h,c}` implementing RogueSchema for sprite sheets (name, texture dims, sprites array, optional animations with frames + loop).
- Added `schema_audio.{h,c}` for audio clip metadata (name, file, optional format/volume/loop/tags).
- Inserted TODO enum placeholders in `asset_classification.h` for future sprite/audio metadata classification without altering runtime yet.
- New unit test `test_sprite_audio_schema` exercises valid + invalid JSON for both schemas (in-memory JSON objects, no filesystem dependency).
- CMakeLists updated to build & register new test target.
- Builds clean locally; ready for placeholder.png enforcement next.
 - Fixed test to use boolean JSON for `loop` (schema expects boolean; initial integer caused validation error).

feat(assets): placeholder asset enforcement helper + unit test
feat(assets/schema): add Level/Map schema (Phase 2) + unit test

- Added `schema_levels.{h,c}` defining Level/Map JSON schema: required id, w, h, tiles (RLE string), optional tile_size, spawns[] (type/x/y/faction), environment_tags[].
- Allows additional forward-compatible fields; cross-field semantic checks (e.g., spawn bounds) deferred to runtime validators.
- New unit test `test_level_schema` validates positive + negative JSON construction (in-memory; no filesystem I/O).
- Wired into `rogue_content_json` object library & CTest.
- Roadmap Phase 2 updated: Level/Map schema task checked off; next target is UI Theme/Layout schema + schema versioning system.


- Added asset_placeholder.{h,c} with `rogue_asset_placeholder_exists()` scanning fallback prefixes for `assets/placeholder.png`.
- Added unit test `test_asset_placeholder_enforcement` (asserts positive path + negative misspelled path).
- Committed stub placeholder PNG (tiny sentinel) to guarantee availability in CI/test runs.
- Roadmap Phase 2 updated: task checked off; next focus shifts to Level/Map & UI Theme schemas and schema versioning system.


Phase 1.4: PNG Sequence Loader
------------------------------
* Added API `rogue_skill_load_png_sequence(directory, prefix, out_frames, max_out)` scanning for prefix_001.png..prefix_999.png (zero-padded) with fallback to prefix_1.png style.
* Loads each PNG into its own RogueTexture and builds a full-image RogueSprite (no atlas packing yet).
* Roadmap: Marked PNG Sequence Loader complete with implementation note; follow-ups listed for texture lifetime mgmt & atlas generation.
* Unit: Added smoke test `test_skills_phase1_4_png_sequence_loader` asserting absence of frames when sample sequence not present (deterministic until assets added).
Follow-ups:
	- Centralize texture ownership & add unload routine.

feat(skills/visuals): Asset Validation Feedback panel (thumbnails + diagnostics)
-------------------------------------------------------------------------------
* Adds new panel in Skills → Visuals providing per-target (Cast/Projectile/Impact/AoE) thumbnail preview and status line.
* Detects and flags:
	- MISSING (file absent at path)
	- CORRUPT/UNSUPPORTED (texture load failure)
	- DIM ERR (w/h <=0 or >4096)
* Auto Refresh toggle forces per-frame reload (useful while externally editing assets), plus manual Refresh button.
* Headless-safe: only draws thumbnails when SDL present; logic compiles unconditionally to satisfy linker when overlay disabled.
* Roadmap: marks "Asset Validation Feedback" complete with implementation blurb.
* Future: integrate deeper validation (format heuristics, grid inference suggestions, hot-reload hooks) and batch operations.
	- Added validation helpers (rogue_skill_asset_validate, rogue_visuals_infer_grid) + unit test.
	- Panel now shows EXT WARN and grid suggestion with Apply, plus Normalize Paths batch op.
	- Add positive path test with curated sample sequence assets.
	- Integrate sequence playback into Skills → Visuals preview (frame stepping via existing anim sampler).
	- Atlas generation & dedup of identical frame dimensions.

Phase 1.4: Sprite Atlas Generation & Sequence Frame Free Helper
----------------------------------------------------------------
* Added `rogue_skill_pack_frames_horizontal(frames, count, out_atlas)` to consolidate PNG sequence frame textures into a single horizontal atlas (reduces future draw calls & texture binds).
* Rewrites frame `sx/sy` offsets in-place and frees/destroys the per-frame textures on success.
* Added `rogue_skill_free_sequence_frames(frames, count)` to explicitly release un-packed sequence textures (prevents accumulating leaks when author iterates without packing).
* Negative-path unit `test_skills_phase1_4_sprite_atlas_pack` validates deterministic no-op (0 frames, 0 repacked) in absence of assets / headless renderer.
* Roadmap: Marked Sprite Atlas Generation complete (Phase 1.4 slice) with implementation notes; README bullet added under Recent Skills/Effects.
Follow-ups:
	- Add positive-path asset-backed packing test (provide 2–3 tiny PNGs under a test skill directory).
	- Introduce vertical & area-based bin packing (heuristic: sort by height desc, skyline or shelf algorithm) + fragmentation metrics.
	- Provide optional padding and power-of-two rounding for GPU-friendly atlases.
	- Integrate atlas reuse/dedup across multiple skills sharing identical sequences.

feat(assets): Phase 1 core directory structure completion + asset classification module
--------------------------------------------------------------------------------------
Summary:
Implements Phase 1 of the Asset Structure roadmap: directory taxonomy finalization, asset type classification system, naming/version-control guidelines, and contributor documentation scaffolding.

Details:
* New module: src/util/asset_classification.{h,c}
	- Enum RogueAssetType with 14 categories (sprites, textures, fonts, music, sfx, voice, level, config, localization, shader vertex/fragment/compute, meta schema/manifest).
	- Heuristic classification: pure string (no I/O), mixed separator normalization, directory token boundary matching (e.g., "/sprites/", "/music/").
	- Extension buckets (png/bmp/tga, ogg/mp3/wav, json/cfg, ttf/otf, vert/frag/comp) with directory disambiguation for sprite vs texture & music vs sfx vs voice.
	- Stable lowercase snake string helper rogue_asset_type_str.
* Unit test: tests/unit/test_asset_classification.c validates representative paths for each category and unknown fallback.
* CMake: added asset_classification.c to rogue_core sources and test target + ctest registration.
* Roadmap: Phase 1 tasks marked complete with implementation notes; added Phase 1 Implementation Notes section.
* README: Assets/Configuration section expanded summarizing naming conventions (snake_case, semantic grouping), binary vs generated artifact strategy, and guidance seeds for contributors.
* Version control strategy codified: binary runtime assets tracked; future large source art to adopt explicit /source_art policy; generated manifests/checksums to be gitignored in Phase 4.

Follow-ups (Phase 2+ planned):
* Author JSON schemas (sprite/audio/level/ui theme) leveraging existing json_schema utilities.
* Implement json validation wrapper + unit tests (positive + negative) integrated into validation panel.
* Generate asset manifest (meta/manifests/) enumerating classified assets w/ hash + size for integrity & packaging.
* Add placeholder.png presence assertion + positive-path validation test with real sample sprite.


Phase 1.4: Asset Dependency Tracking & Hot-Reload (Foundation)
-------------------------------------------------------------
* Added fixed-cap (256) dependency registry for skill visual assets (PNG / packed JSON) with ref counting and modification timestamp snapshotting.
* New APIs (skill_assets.h):
	- rogue_skill_asset_dep_reset()
	- rogue_skill_asset_dep_track(path)
	- rogue_skill_asset_dep_untrack(path)
	- rogue_skill_asset_dep_count()
	- rogue_skill_asset_dep_data() (read-only pointer)
	- rogue_skill_asset_dep_poll_changes(cb,user) (mtime-based change detection)
* Polling strategy: explicit stat() of tracked unique paths; on mtime delta callback invoked for higher-level texture/preview reload (deferred for next slice).
* Deterministic & test-friendly: no dynamic allocation beyond fixed array; silent ignore when capacity exceeded.
* Unit: test_skills_phase1_4_asset_dep_tracking creates temp file, tracks, modifies, polls, and validates lifecycle (track → poll → modify → poll change → untrack).
* Roadmap: Marks Asset Hot-Reload (foundation) and Asset Dependency Tracking complete with implementation notes and API list.
* README: Adds bullet summarizing new APIs and polling workflow.
Follow-ups:
	- Integrate automatic texture reload & atlas repack triggers on change events.
	- Add fallback placeholder injection for missing assets (currently TODO in roadmap later slice).
	- Expand registry to support hashing content for deep validation (checksum diff) and to collapse identical assets across skills.

Phase 1.3: Enhanced Execution Pathways
--------------------------------------
* Added optional runtime-selectable execution pathways for showcase skill (Fireball) implemented as a tiny dispatch wrapper in `skill_tree.c` (pathway_fireball_dispatch).
* Three pathways: 0 Default (original projectile), 1 Empowered (projectile + short rank‑scaled PowerStrike buff), 2 Utility (mini dash forward, replaces projectile).
* New APIs in `skill_tree.h`: `rogue_skill_pathway_set/get/last_exec` (clamped, fixed-cap arrays, zero alloc). Non‑wrapped skills ignore pathway changes (no regression risk).
* Deterministic: pathway only alters effect body; timing / cooldown / state machine untouched. All existing tests remain green.
* Unit: `test_skills_phase1_3_execution_pathways` verifies pathway selection, player displacement in Utility, and recorded last pathway id sequence.
* Roadmap: Marks "Enhanced Execution Pathways" complete with implementation note; README updated under Recent Skills/Effects.
Follow-ups (deferred): generalize into data-driven mapping; expose pathway selector in debug overlay; authoring-time validation for pathway compatibility with passive/channel skills.

Range Sliders (Phase 2.4)
-------------------------
* Added overlay_range_slider_int / overlay_range_slider_float widgets composing two ordered standard sliders (min then max) that enforce non-decreasing bounds (swaps/clamps when crossed).
* Integrated sample usage in Skills → Overview panel: "Cooldown Target Band" (float ms range) and "Design Rank Window" (int rank span) as authoring aides (non-persistent for now).
* Roadmap: Marked Range Sliders complete (Phase 2.4) with implementation note; establishes primitive for future persisted stat bands (e.g., damage_min/max, projectile range windows).
* README: Added bullet under overlay feature list documenting API and current usage; notes future persistence + possible dual-handle single-draw variant.
Follow-ups (deferred): unify with eventual histogram/range visualization, add persistence + JSON schema fields for common min/max authoring domains.

Curve Editor (Phase 2.3)
------------------------
* Implemented overlay_curve_editor: SDL polyline widget (click add point, drag to move, Add Mid, Remove) maintaining ascending X ordering; headless-safe (no-op when renderer absent).
* Added overlay_gradient_editor prototype widget: gradient bar with stop add/select/drag/remove (endpoints 0 and 1 locked). Each stop has RGBA edited via existing color picker. Stops kept in ascending order; new stop clones nearest color. Example integrated in Skills → Overview as "Trail Color Gradient" (non-persistent design helper). Roadmap Color Gradient Picker item marked complete; README updated. Future: persistence schema (e.g., particle color ramps, UI theme gradients), export to JSON, interpolation modes (linear vs. ease), alpha preview checkerboard.
* Integrated example in Skills → Overview beneath coefficient section: "Damage Scalar Curve" (rank 1..20 vs scalar 0..3) as design-only helper (non-persistent prototype).
* Roadmap: Marked Curve Editor complete with implementation note (v1 prototype, future persistence & schema integration planned for progression curves: damage, cooldown, efficiency ramps).
* README: Documented new widget purpose and current limitations; sets stage for persisted progression authoring and evaluation utilities.
Follow-ups (deferred): persistence format (JSON array or sampled knots), evaluation helper (linear / spline), optional tangent handles, multi-curve overlay, test coverage (point ordering, bounds constraints).
