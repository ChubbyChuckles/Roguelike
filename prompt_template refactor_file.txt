### Add documentation

I have a C file open in my editor. Please generate comprehensive Doxygen documentation for all functions, structs, enums, macros, and global variables in the file. Follow these guidelines:

1. Use Doxygen comment style (e.g., `/** ... */` for multi-line or `///` for single-line comments) and ensure compatibility with Doxygen's default settings.
2. Include the following for each element:
   - For **functions**: A detailed description of the function's purpose, parameters (`@param`), return value (`@return`), and any side effects or notes.
   - For **structs**: A description of the struct's purpose and a brief explanation of each member (`@param` or `@var`).
   - For **enums**: A description of the enum's purpose and a brief explanation of each enumerator.
   - For **macros**: A description of the macro's purpose, its parameters (if any, using `@param`), and usage notes.
   - For **global variables**: A description of their purpose and usage.
3. Preserve and integrate any existing comments in the file, ensuring they are incorporated into the Doxygen comments without losing information. If an existing comment already provides useful details, adapt it into the Doxygen format rather than replacing it.
4. Add `@brief` for a concise summary and a detailed description where applicable.
5. Include `@file` documentation at the top of the file, describing the file's overall purpose and including `@author` (use a placeholder like `[Your Name]`), `@date` (use current date: September 2025), and `@version` (use 1.0 unless specified otherwise).
6. Ensure proper formatting for readability, with clear separation between sections and consistent use of Doxygen tags.
7. Do not modify the actual C code (e.g., function implementations, struct definitions, etc.)—only add or update comments.
8. If any part of the code lacks sufficient context for documentation, infer reasonable details based on variable/function names and code structure, but note any assumptions with `@note`.

Please generate the Doxygen comments and insert them appropriately throughout the file.


### Useful commands

ctest -C Debug -j8 -R ".*test_(skill|skills).*" --output-on-failure # test all skills

powershell -NoLogo -NoProfile -Command "cmake --build . --config Debug --parallel 8" # fast build tests

ctest --test-dir build -C Debug -j8 --output-on-failure --timeout 10

pwsh -NoLogo -NoProfile -Command "Set-Location -LiteralPath 'c:\Users\Chuck\Desktop\CR_AI_Engineering\GameDev\Roguelike'; cmake --build build --config Debug --parallel 8"


### Compile Documentation

cmake -S . -B build -G "Visual Studio 17 2022" -DCMAKE_BUILD_TYPE=Debug

cmake -S .. -B . -G "Visual Studio 17 2022" -DCMAKE_BUILD_TYPE=Debug

cmake --build . --config Debug --target docs --parallel 8


### Refactor File

Refactor this large C source file to enhance maintainability by modularizing it into smaller, logically cohesive files.

**Refactoring Objectives:**
- Decompose the file into distinct modules based on separation of concerns.
- Ensure each module:
  - Uses header files with proper include guards to prevent multiple inclusions.
  - Includes only essential dependencies to minimize coupling.
  - Exports public interfaces (functions, types, or shared globals) via headers, keeping implementation details static where possible.
- Update the original file or create a new main.c to orchestrate the refactored modules, ensuring seamless integration.
- Preserve all original functionality, avoiding any logic modifications or bug introduction.
- Adhere to consistent naming conventions: lowercase_with_underscores for file names, function names, and variables, aligning with C best practices.
- Manage global variables by relocating them to appropriate headers for shared access or encapsulating them within relevant modules to reduce global scope pollution.

**Output Requirements:**
1. Provide a detailed file structure overview, listing each new file with a concise description of its purpose and contents.
2. Generate complete, syntactically correct code for each new .c and .h file, including necessary includes, guards, and function declarations/definitions.
3. Include a comprehensive explanation of the refactoring decisions, highlighting how the new structure improves maintainability, scalability, and readability while adhering to C programming best practices.


### Add Documentation

Read the implementation plan at "roadmaps/custom_documentation.txt". Get the next not implemented feature from the top and implement it. After implementing it go back to "roadmaps/custom_documentation.txt" and mark the feature as done by changing the [ ] in front of it into a [x]. Repeat this process until all features are implemented.


### Multi-Platform support

The code currently runs on Windows but needs to be compatible with Linux and macOS without modifying the codebase on my local PC. Create a sophisticated, step-by-step implementation plan for a GitHub Actions CI workflow that achieves multi-platform compatibility (Windows, Linux, macOS) by performing any necessary refactoring within the CI pipeline itself. Read the current CMake files to know how the build process on the local PC works because the current CI workflow in ".github\workflows\ci.yml" doesnt work and fails to build on Windows while trying to setup SDL2.

The plan must:

1. Ensure the codebase remains unchanged on my local PC, with all refactoring (e.g., handling platform-specific differences like file paths or SDL2 configurations) done in the CI workflow.
2. Support building and testing on `windows-latest`, `ubuntu-latest`, and `macos-latest` runners.
3. Use CMake as the build system and install SDL2 (version 2.32.4) consistently across platforms.
4. Include automated static analysis to detect non-portable code and apply fixes (e.g., using `Clang-Tidy` or `sed` for file path conversions) within the CI pipeline.
5. Run unit and integration tests to verify functionality on all platforms, assuming a test executable exists (e.g., `game_engine_test`).
6. Set up versioned releases (e.g., using semantic versioning like `v1.0.0`) with release artifacts for each platform uploaded to GitHub Releases.
7. Handle platform-specific SDL2 configurations (e.g., include paths, libraries) using CMake and conditional compilation where needed.
8. Provide detailed steps for each phase, including dependency installation, refactoring, building, testing, and release creation.
9. Ensure the workflow is efficient, reusable, and maintainable, with clear error handling and logging.
10. Include comments in the workflow YAML to explain each step.

The plan should result in a complete GitHub Actions workflow file (`.github/workflows/build-and-release.yml`). Avoid assumptions about specific code details beyond SDL2 usage, and focus on generic solutions for portability issues (e.g., file paths, compiler differences). Format the plan as a TODO-List with tasks that warent done yet marked with "[ ]" and tasks that are done as "[x]" and write it ro "roadmaps/implementation_multi_platform.txt"


### Kil Codebase

Go through all the .c-files in the "src\core\loot" directory and add extensive doxygen documentation. Make sure the file header is at the top of the file and if there is already an existing file healer, move it to the top of the file and enhance it. Use information provided in existing comments in the documentation.
The informations for the file-header are:

@author [Chrisian "ChubbyChuckles" Rickert]
@date [06/09/2025]
@version 1.0
