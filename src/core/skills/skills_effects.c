/* Shared skill effect helper stubs (placeholder for future expansion). */
#include "skills_internal.h"

/* In the original monolith there were no separate reusable helpers distinct
   from activation logic; this file is reserved for future extraction of any
   common damage / scaling computations to keep runtime lean. */
