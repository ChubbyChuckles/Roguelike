/* Rendering helpers for tile sprites (currently thin - future batching can go here). */
#include "../util/log.h"
#include "tile_sprites_internal.h"

/* No additional functions yet; keeping file to allow future batching/drawing utilities.
   Existing public API is fulfilled by lookup + load modules. */
