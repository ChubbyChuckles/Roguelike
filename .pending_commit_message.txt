AI Phase 9.4 & 9.5: Agent Pooling + 200-Agent Stress Test

Phase 9.4:
- Added ai/core/ai_agent_pool.{c,h} slab allocator for EnemyAIBlackboard blocks (reuse vs malloc/free churn)
- Integrated pool into enemy_ai_bt enable/disable (rogue_ai_agent_acquire/release)
- Diagnostic APIs: rogue_ai_agent_pool_{in_use,free,peak,reset_for_tests,slab_size}
- New unit test test_ai_phase9_agent_pool validates reuse across cycles, peak stability, no leaks

Phase 9.5:
- New stress test test_ai_phase9_stress spawning 200 enemies; two-phase scheduler + LOD validation
	* Phase 1 radius 30: near subset moves (moved_near>=25) far agents static
	* Phase 2 radius 400: previously far agents move (moved_far>=100)
	* Verifies agent pool peak == 200 and scheduler frame progression
- README & roadmap updated (Phase 9.5 Done)

AI Phase 10.1–10.5: Debug & Tooling Suite
- Added ai/core/ai_debug.{c,h} implementing:
	* rogue_ai_bt_visualize (ASCII behavior tree)
	* rogue_ai_blackboard_dump (key/value listing)
	* rogue_ai_perception_collect_debug (facing + LOS primitives for overlay)
	* rogue_ai_trace_export_json (chronological ring buffer export)
	* rogue_ai_determinism_verify (dual factory run path hash comparison)
- New unit test test_ai_phase10_debug validates all helpers.
- Trace export ordering fixed to chronological (old ring-forward order confusing).
- Roadmap Phase 10.1–10.5 marked Done; README updated with new section.

Notes:
- Maintains SDL-enabled build; warnings treated as errors handled.
- Provides foundation for future in-engine overlays & external tooling ingestion.
