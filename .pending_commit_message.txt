feat(itemsys/phase17.5): record-level inventory diff metrics (reused vs rewritten) in incremental save mode
feat(itemsys/phase18.1/18.2): loot analytics ring buffer + JSON export

Phase 17.5:
 - Inventory save now snapshots ordered active item records (stable iteration over instance array).
 - On incremental saves with unchanged count, compares each record to previous snapshot producing metrics via rogue_save_inventory_diff_metrics (reused vs rewritten).
 - Test test_loot_phase17_5_diff covers: initial all rewritten, unchanged reuse, single modification causing 1 rewritten / N-1 reused.
 - Roadmap marked 17.5 Done; README updated Phase 17 entry with metrics + test reference.

Phase 18.1 / 18.2 (previously implemented):
 - Loot analytics module (ring buffer 512 events) + JSON export summarizing total drops and rarity counts.
 - Tests validate wrap & export (test_loot_phase18_analytics).

Follow-up candidates:
 - Optimize Phase 17.5 by skipping serialization of reused records (would require per-record length encoding or secondary index) – deferred.
 - Implement Analytics Phases 18.3–18.5 (drift alerts, session overlay, heatmap) building atop existing ring buffer.
