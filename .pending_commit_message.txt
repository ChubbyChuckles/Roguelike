persistence: autosave ring, durability toggle, migration bump to v2, tests

- Added autosave ring API (rogue_save_manager_autosave) with N=ROGUE_AUTOSAVE_RING rotating slots.
- Implemented internal generic save path writer (atomic temp -> final) centralizing checksum + ordering.
- Added durable write toggle (fsync/_commit) via rogue_save_manager_set_durable.
- Bumped SAVE_FORMAT_VERSION to 2; added linear migration walker (no-op chain currently) with error codes.
- Added autosave ring unit test (test_save_autosave_ring) ensuring ring file count constraint.
- Refactored save code for reuse; roadmap updated (1.3, 2.2, 6.2 Done; durability note in 1.4; tests 10.1 Partial).
- README persistence section pending further per-section tests (future work).
