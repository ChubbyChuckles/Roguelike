feat(equipment phase11.1-11.4): analytics snapshot, histograms, MAD outlier detection
feat(equipment phase11.3): set & unique usage tracking
feat(equipment phase11.5-11.6): proc/DR oversaturation flags + A/B balance harness

Implemented Phase 11 analytics & balance enhancements:
 - JSON snapshot export (rogue_equipment_stats_export_json) for key aggregated stats.
 - Per rarity/slot DPS & EHP histograms with export (rogue_equipment_histograms_export_json) and recording API.
 - Median/MAD based DPS outlier detector (rogue_equipment_dps_outlier_flag) with rolling sample buffer.
 - Set & unique usage tracking (rogue_equipment_usage_record / _export_json).
 - Proc oversaturation & DR chain detection (rogue_equipment_analytics_record_proc_trigger, _record_dr_source, _analyze, _export_json).
 - A/B balance harness (rogue_balance_register, rogue_balance_select_deterministic, rogue_balance_current) enabling configurable MAD multiplier & thresholds.
 - Unit tests: test_equipment_phase11_analytics (snapshot, histograms, usage, outlier) and test_equipment_phase11_balance (proc oversat, DR chain, deterministic variant selection).
 - Roadmap updated (11.1–11.6 Done; 11.7 basic tests Done); README Phase 11 section expanded with new APIs.

feat(equipment phase12.1-12.7 initial): headless UI helpers (panel grouping, layered tooltip, deltas, proc preview, socket/transmog state, determinism hash)

Implemented initial slice of Phase 12 UI / Visualization:
 - Equipment panel refactor (rogue_equipment_panel_build) grouping Weapons/Armor/Jewelry/Charms + set progress counters.
 - Layered tooltip builder (rogue_item_tooltip_build_layered) composing base tooltip + implicit armor line + gem socket lines + set tag with stable ordering.
 - Comparative delta helper (rogue_equipment_compare_deltas) emitting damage delta vs equipped slot; integrated into layered tooltip when compare_slot>=0.
 - Proc preview (rogue_equipment_proc_preview_dps) naive expected DPS contribution (sum triggers_per_min/60) for early balance surfacing.
 - Socket drag/drop ephemeral selection + placement APIs (rogue_equipment_socket_select / _place_gem) – groundwork for interactive panel.
 - Transmog selection wrapper remembering last chosen visual (rogue_equipment_transmog_select / _last_selected).
 - Deterministic tooltip hash (rogue_item_tooltip_hash) FNV-1a for regression snapshot tests.
 - Unit test test_equipment_phase12_ui validates panel sections presence, tooltip hash determinism, and non-negative proc preview.
 - Roadmap Phase 12 entries updated (marking basics Done / Partial where deeper visuals deferred). README updated with Phase 12 summary.

