UI Phase 6.1: Implement data-driven HUD layout spec

- Added `hud_layout.h/.c` providing kv-config driven placement for health, mana, XP bars and level text.
- Integrated layout into `hud.c` (replaces hardcoded coordinates) with lazy default fallback.
- Added asset file `assets/hud_layout.cfg` (modifiable without recompilation).
- Added unit test `test_ui_phase6_hud_layout` validating parse & coordinate propagation.
- Updated `implementation_plan_uisystem.txt` marking 6.1 Done (kv config instead of JSON/TOML) and README changelog.
- CMakeLists updated to compile new source; roadmap & commit log updated.

