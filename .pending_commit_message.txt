Debug Overlay – Items Phase 10.3–10.4: live vendor repricing, creation wizard, tests

- Vendor repricing hooks (10.3):
	- Added vendor APIs `rogue_vendor_reprice_all` and `rogue_vendor_on_item_def_changed` and wired them into item_debug setters and JSON loader so vendor slot prices update immediately on item changes.
	- New unit `test_vendor_live_reprice` validates price increases after base_value edits without restock.

- Item Creation Wizard (10.4):
	- Overlay Items panel gained a “Create New Item” section (ID/Name/Category + core stats) that creates and selects the new item on success.
	- Added headless-safe API `rogue_item_debug_create(...)` and a registry helper `rogue_item_defs_add(...)` with validation/sanitation and fast-index rebuild.
	- New unit `test_item_creation_wizard` covers required fields, duplicate id rejection, and sanitation (stack/sockets).

- Docs & roadmap:
	- Roadmap marks 10.3 and 10.4 Done and documents tests.
	- README: notes live vendor repricing and the creation wizard in the Items panel section.


feat(content): add Items JSON schema module and tests; wire into build

- Add src/content/schema_items.{h,c} with a validator and schema builder
	- Required fields: id, name, category
	- Ranges: category [0..ROGUE_ITEM__COUNT-1], stack_max>=1, sprite_tw/th>=1, rarity [0..10], sockets [0..6]
	- Allows additional fields for forward-compatibility
- Add unit test tests/unit/test_items_schema.c
	- Validates two sane item defs and rejects an invalid category
- Wire schema_items.c into rogue_content_json and register the test target
- Full Debug (SDL2) build + ctest -j12 all green (575/575)
- Roadmap: mark Phase 3.1–3.2 Done; note follow-up for schema-backed roundtrip
- README: update test count and add brief Items schema note
