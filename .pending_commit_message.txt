feat(persistence): implement Phase 1 core save architecture (descriptor + components + atomic write)

- Add SaveManager implementation with descriptor (version, timestamp, component_mask, section_count, total_size, CRC32)
- Deterministic component ordering via qsort by id
- Atomic temp file write + rename for save slots (save_slot_N.sav)
- Implement Phase 1 components: player, world_meta, inventory (item instances + affixes), skills (rank + cooldown), buffs (active list), vendor (seed + timers)
- Integrate CRC32 integrity verification on load
- Update roadmap Phase 1 statuses (Done / Partial) and README changelog section
- Lay groundwork for Phase 2 (migration registry, SAVE_FORMAT_VERSION) & autosave ring (deferred)

Follow-ups: autosave ring + fsync durability option, migration registry, per-section hashing, compression.
