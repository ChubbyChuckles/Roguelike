Skills: Meta Skill Type selector UI + debug API; overrides roundtrip; roadmap/README updates

Summary:
- Added a Skill Type selector to the Skills overlay (Meta section) with labels for MELEE/RANGED/AOE_SPELL/BUFF/DEBUFF/HEAL/SUMMON/PASSIVE/ULTIMATE.
- Implemented headless-safe debug APIs: rogue_skill_debug_get_type / _set_type and wired them into overrides export/import JSON.
- Rebuilt (Debug, SDL2, -j12) and ran full test suite: 586/586 PASS.
- Updated roadmap (STATUS DELTA and Phase 2.1 marking selector as Partial with presets pending) and README (new note about the Skill Type selector).

Notes:
- Selector persists via overrides JSON and refreshes validation banner after changes.Extend skills with Visuals/Advanced fields end-to-end and surface in overlay

 - Add optional visuals/audio/animation/AoE/projectile fields to RogueSkillDef and JSON loader (deep-copy/free, schema export)
 - Implement headless-safe debug APIs to get/set visual params with clamps and frees
 - Validate asset existence and parameter bounds in skills validator; wire overlay save/create to show errors
 - Expose a Visuals / Advanced section in the Skills panel; persist via overrides JSON and support auto-reload
 - README: document Visuals/Advanced usage; Roadmap: mark Phase 1.1 partial completion
 - Build/tests: Debug SDL2 full suite remains 100% green (584/584)
feat(overlay/skills): add "Create New Skill" wizard + headless API and tests

- New API: rogue_skill_debug_create(name, max_rank, base_cd_ms, cd_red_ms_per_rank, cast_ms, is_passive)
  - Appends a new RogueSkillDef to the registry with sensible defaults
  - Validates inputs; rejects duplicates by name (-2)
- Skills panel: new "Create New Skill" section (name, max rank, base CD, CD/rank, cast ms, passive)
  - On success, selects the new skill and shows a status line
- Unit: tests/unit/test_skill_creation_wizard.c verifies active/passive creation and duplicate guard
- Docs: README debug overlay tips mention the skills creation wizard
- Roadmap: Phase 14.1 and 14.5 marked Partial for Skills wizard and tests

feat(overlay/skills): template-based creation (prefill + optional coeff copy) + tests

- Skills panel: creation wizard gains a template workflow
  - "Template Skill Id" slider + "Apply Template" prefill name (suffix _Copy), max_rank, passive, and timing from the selected template
  - "Copy Coeffs" option applies template RogueSkillCoeffParams to the preview selection and to the newly created skill on success
- API: new `rogue_skill_debug_get_meta(id, &max_rank, &is_passive)` to support template prefill
- Tests: add `tests/unit/test_skill_template_creation.c` covering active/passive templates and coeffs copy on/off
- Docs: README overlay tips mention the template workflow; test counts updated
- Roadmap: Phase 14.2 marked Partial with details of the template UI/API and test coverage

feat(overlay/skills): searchable template selector in creation wizard

- Skills panel: add a "Template Filter" input and a filterable table (ID, Name) of skills; clicking a row selects its id for the template apply flow
- UX: substring filter for quick narrowing; integrates with existing "Apply Template" and optional "Copy Coeffs"
- Docs: README updated to mention the searchable template picker; roadmap CI counts bumped to 584/584 and Phase 14.2 notes the new selector

feat(overlay/skills): validation pipeline integration (API + UI gating) + unit test

- API: add `rogue_skill_debug_validate(char* err, int err_cap)` delegating to the core validator for headless-safe checks.
- UI: Skills panel Save Overrides JSON runs validation first and blocks save on failure with an error message; Create runs validation and shows a warning on failure (warn-only to allow iteration).
- Tests: new `tests/unit/test_skills_validation_pipeline.c` covers invalid→fix→valid for offensive skills missing coefficients.
- Docs: README “Debug overlay tips” adds validation behavior and mentions the new API; Validation System section documents panel integration.
- Roadmap: Phase 14.3 marked Done with details; full Debug (SDL2) build and suite are green (584/584, -j12).

feat(overlay/skills): live sticky Validation Status banner in Skills panel

- UI: Add a prominent, sticky "Validation Status" banner at the top of the Skills panel that shows either OK or ERROR with the first failure reason.
- Live refresh: The banner updates immediately when editing Timing, Coefficients, or Visuals/Advanced fields, and after Create/Save/Load actions.
- Validation: Uses the shared skills validator; no new rules added, just surfaced inline for fast iteration.
- Docs: README “Debug overlay tips” notes the banner; roadmap status delta mentions the new overlay feedback.
- Build/tests: Debug SDL2 build succeeds and the full CTest suite remains 100% green (584/584, -j12).

feat(skills): introduce optional skill_type enum (schema+runtime+validator)

- Data model: Add RogueSkillType enum and `skill_type` field to `RogueSkillDef`; defaults to UNKNOWN for back-compat.
- Loader/Schema: JSON/CSV loaders accept `skill_type`; schema exports/validates with range 0..9.
- Validator: flag mismatches when `skill_type=PASSIVE` but `is_passive=0`; reject out-of-range values.
- Docs: README Debug overlay tips mention the optional `skill_type` and defaulting behavior.
