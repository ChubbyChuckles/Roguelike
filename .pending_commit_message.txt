Debug overlay + validation Roadmap Phase 13.3/Worldgen benchmark stability

- Content Graph panel (Phase 13.3): substring filter + top-level id prefix grouping; seeded core nodes (items leaves/parent, affixes, loot, loot_runtime).
- Fix MSVC pointer-type warning in overlay_panels.c (explicit const char* ternary cast for slash grouping).
- Stabilize worldgen optimization benchmark: adaptively repeat scalar workload until >=0.5 ms (cap 64x) and mirror repeats on SIMD path to keep ratios stable.
- Result: full ctest in Debug (SDL2) with -j12 is all green (581/581).
feat(overlay/content): initial Content Graph panel + asset_dep enumeration APIs; docs/roadmap

- Implemented simple "Content Graph" overlay panel to browse content dependency nodes and view direct deps (Phase 13.3 prototype).
- Extended asset_dep with enumeration helpers: rogue_asset_dep_count/get/get_deps; kept existing hashing/invalidate/cycle detection.
- Updated README with Content Graph section; bumped latest CI verification count.
- Roadmap: mark 13.3 Partial with concrete status; CI tally now 581/581.

Debug Overlay – Items Phase 10.3–10.4: live vendor repricing, creation wizard, tests

- Vendor repricing hooks (10.3):
	- Added vendor APIs `rogue_vendor_reprice_all` and `rogue_vendor_on_item_def_changed` and wired them into item_debug setters and JSON loader so vendor slot prices update immediately on item changes.
	- New unit `test_vendor_live_reprice` validates price increases after base_value edits without restock.

- Item Creation Wizard (10.4):
	- Overlay Items panel gained a “Create New Item” section (ID/Name/Category + core stats) that creates and selects the new item on success.
	- Added headless-safe API `rogue_item_debug_create(...)` and a registry helper `rogue_item_defs_add(...)` with validation/sanitation and fast-index rebuild.
	- New unit `test_item_creation_wizard` covers required fields, duplicate id rejection, and sanitation (stack/sockets).

- Docs & roadmap:
	- Roadmap marks 10.3 and 10.4 Done and documents tests.
	- README: notes live vendor repricing and the creation wizard in the Items panel section.


feat(content): add Items JSON schema module and tests; wire into build

- Add src/content/schema_items.{h,c} with a validator and schema builder
	- Required fields: id, name, category
	- Ranges: category [0..ROGUE_ITEM__COUNT-1], stack_max>=1, sprite_tw/th>=1, rarity [0..10], sockets [0..6]
	- Allows additional fields for forward-compatibility
- Add unit test tests/unit/test_items_schema.c
	- Validates two sane item defs and rejects an invalid category
- Wire schema_items.c into rogue_content_json and register the test target
- Full Debug (SDL2) build + ctest -j12 all green (575/575)
- Roadmap: mark Phase 3.1–3.2 Done; note follow-up for schema-backed roundtrip
- README: update test count and add brief Items schema note


feat(ci): stabilize Windows format-check via per-file clang-format runner

- Add cmake/RunClangFormat.cmake to run clang-format per file (MODE=check/-n -Werror and MODE=format/-i) across src/ and tests/.
- Update CMakeLists.txt to wire `format` and `format-check` targets to the new runner when clang-format is found; provide no-op targets when missing (local dev convenience).
- CI: Windows job already installs clang-format; the new per-file runner avoids command-line length limits on Windows. "Build step" failures caused by format checks should now be resolved.
- Docs: README mentions how to run `format`/`format-check`; roadmap notes the CI formatting gate stabilization.


chore(ci/windows): switch to Ninja generator to prevent MSBuild stalls

- Use MSVC developer environment with Ninja (`ilammy/msvc-dev-cmd@v1` + `choco install ninja`).
- Configure with `-G Ninja` and build with `cmake --build build --parallel 12` to improve throughput and avoid intermittent stalls when compiling hundreds of tiny unit test targets.
- README: document Ninja usage in Windows CI.

feat(items): add stable item definition handles and migration/export tool

- Add RogueItemDefHandle (index+generation) with helpers to make/query/get; bump generations on add/reset to invalidate stale handles.
- New CLI tool tools/items_migrate to export current registry as versioned JSON envelope (schema "items") via json_envelope and atomic write.
- New unit test test_items_roundtrip_schema validates export→envelope→schema→reload roundtrip.
- Roadmap Phase 3.3–3.5 marked Done; README to mention migration tool and handles.

fix(items+tests): accept JSON envelope in items loader; stabilize test_boot harness; all tests green (576/576)

- Loader: `rogue_item_defs_load_from_json(path)` now supports both raw JSON arrays and versioned envelopes with `$schema: "items"` and `entries`.
- Tests: `tests/unit/test_items_roundtrip_schema.c` uses an envelope for the write path; loader unwraps transparently and import succeeds.
- Integration: `tests/integration/test_boot.c` now chdir's to the repo root and uses the full `RogueAppConfig` initializer with logical dimensions and background color, fixing a headless/SDL segfault.
- CI: Full Debug (SDL2) build and ctest -j12 run are fully green (576/576).


feat(skills): add Skills JSON schema + envelope support; roundtrip test; full suite green (577/577)

- Content schema: introduce src/content/schema_skills.{h,c}
	- Defines skills.json with required fields and value ranges; permits additional fields for forward-compatibility
	- Validator APIs and a converter helper for tests/docs
- Loader: extend skills loader to support versioned JSON envelopes ("$schema": "skills") while preserving raw-array back-compat
- Tests: add tests/unit/test_skills_roundtrip_schema.c exercising export→envelope→validate→atomic write→reload path
	- Windows/SDL: define SDL_MAIN_HANDLED for the test target to avoid SDL_main linkage on MSVC
- CMake: wire schema sources and test target; link rogue_core and SDL2; add compile definition for SDL_MAIN_HANDLED
- CI/build: Debug (SDL2) -j12 build; ctest ran 577 tests, all passed (100%)
- Docs/Roadmap: README notes new skills schema and updates test count; roadmap marks Phase 4.1–4.3 Done and 4.5 Partial; CI validation section notes skills loader/schema status

feat(skills/overlay): mtime‑based auto‑reload for skills overrides; overlay toggle; new unit; full suite green (578/578)

- Skill debug API: add `rogue_skill_debug_autoreload_tick(const char* path)` that polls file mtime and applies overrides on change; default path `build/skills_overrides.json` when NULL. Returns applied count on change, 0 otherwise.
- Overlay: Skills panel gains “Auto‑Reload Overrides” checkbox; when enabled, calls the tick each frame and displays a brief status when changes are applied.
- Tests: new unit `tests/unit/test_skills_live_reload.c` writes a small overrides JSON, ticks once (expect change), then ticks again (expect no‑op on unchanged mtime).
- CMake: register test target and ensure Windows/SDL console linkage via SDL_MAIN_HANDLED.
- CI/build: Debug (SDL2) -j12 build; ctest ran 578 tests, all passed (100%).
- Docs/Roadmap: README notes the Auto‑Reload toggle; Roadmap marks Phase 4.4 Partial (overrides auto‑reload) and updates CI counts.

feat(skills): base skills JSON auto‑reload + overlay toggle; registry reload path; tests; full suite green (579/579)

- Core: introduce `rogue_skills_reload_from_cfg(path)` to clear and rebuild the skills registry from a JSON/envelope path. Add `rogue_skills_base_autoreload_tick(path)` that polls mtime and triggers a full reload when the base skills JSON changes.
- Overlay: Skills panel gained a separate “Auto‑Reload Base Skills JSON” checkbox and displays reload counts for both base and overrides paths.
- Tests: new unit `tests/unit/test_skills_base_autoreload.c` writes a temp JSON, enforces an mtime delta on Windows, asserts first‑change detection and no‑op on unchanged mtime, and cleans up. Existing overrides live‑reload test remains green.
- Build/CI: Rebuilt Debug (SDL2) with parallelism; CTest full suite passed 100% (579/579).
- Docs/Roadmap: Roadmap marks Phase 4.4 Done and updates CI validation notes. README mentions the new “Auto‑Reload Base Skills JSON” toggle and updates test counts.


feat(skills/tests): extend cross‑reference validation; docs updated; full suite green (580/580)

	• detects invalid proc.effect_spec_id references
	• allows passive (non‑offensive) skills without coeffs
	• verifies offensive skills with coeffs pass

 feat(validation): add cross-reference validation manager, overlay panel, CLI; fix save path test and go all-green (580/580)

 - Engine: add state validation manager (`src/core/integration/state_validation_manager.{h,c}`) with system registration, cross-rules, incremental skip via snapshot hashes, telemetry, and event ring buffer.
 - Wiring: `src/core/integration/validation_wiring.{h,c}` registers default validators (items/entities/tilesets/tag_registry) and cross-rule (skills); invoked from app_init; default interval=120 ticks.
 - Overlay: new “Validation” panel shows stats and recent events; Run Now, Force All, and interval slider controls; registered at overlay startup.
 - CLI: new `tools/validate_content.c` runs validations headlessly; returns 0 OK, 1 warnings (with --fail-on-warn), 2 corruption; supports --force-all and --quiet. Wired in CMake as `validate_content`.
 - Tests: add `tests/unit/test_validation_cli_smoke.c` and keep `test_state_validation_manager` to cover manager telemetry/repair paths.
 - Fix: persistence `test_save_v2_backward_load` now uses canonical save paths via `save_paths.h` to avoid cwd issues. Full Debug (SDL2) build + `ctest -C Debug -j12` are now 100% green (580/580).

chore(validation/tests): confirm validate_content wiring; bump suite to 581/581

- Add smoke test `tests/unit/test_validation_cli_smoke.c` to exercise the validation manager and default wiring via the headless API; ensures at least one run completes.
- Build: Debug (SDL2) with parallelism (-j12); ctest full suite now runs 581 tests, all passing (100%).
- Docs: README and roadmap updated to reflect the new test count (581/581).

build: Debug (SDL2) -j12 succeeded; ctest reports 581/581 passing after Content Graph panel additions.

chore(overlay/content): wire initial asset_dep registrations and add Content Graph filter/group

- App init resets asset_dep each run and registers core content nodes: items (per-category leaves + logical parent), affixes, loot tables, and a runtime loot node depending on items.
- Content Graph panel gains substring filter and top-level id prefix grouping for quick narrowing.
- Roadmap updated to reflect 13.3 wiring and filtering; README unchanged (no behavior change to documented counts).

feat(overlay/content): broaden Content Graph coverage + panel features; tests remain green (581/581)

- Registrations: seed additional domains at app init — skills/base→skills, tiles, sounds, enemies/types, dialogue/style+scripts→dialogue, ui/hud_layout, ui/theme, player/anim, player/sheets, projectiles, tag_registry, and world/{biomes,trees,plants,resource_nodes,mining_nodes}.
- Panel: show per-node file hash (hex), reverse dependencies, and per‑group size summaries; add “Compute All Hashes” to warm caches; retain substring filter and id‑prefix grouping.
- Build/CI: Debug (SDL2) with -j12; full ctest 581/581 passing.
- Docs: README Content Graph section expanded; roadmap marks 13.3 as Partial with new details.

feat(overlay/content): advanced filters + DOT export + on-panel edge preview

- Content Graph panel: add prefix filters id:/path:/group:/hash:/dep:X/rev:X alongside substring.
- Export Graphviz DOT to build/content_graph.dot; add textual group edge view and path collision markers.
- Minimal SDL edge preview inside the panel for the selected node with a toggle.
- Roadmap updated to reflect 13.3 progress; README gets a concise usage blurb.

feat(overlay/content): layered multi-hop SDL preview (BFS) with depth control; MSVC warnings resolved; gate green (581/581)

- Added multi-hop exploration via BFS and a layered SDL preview with a max-depth slider in the Content Graph panel.
- Fixed MSVC pointer/int mismatch warnings in overlay_panels.c by including missing standard headers (<string.h>, <stdio.h>) ensuring correct prototypes for string/IO functions.
- Rebuilt Debug (SDL2) with -j12 and re-ran the full test suite: 100% pass (581/581).
