feat(overlay/skills): EffectSpec palette category filters (Offensive/Defensive/Movement/Utility)

- Added category toggles to the Effects tab palette; combines with substring, Kind, and Debuff-only filters. Assign-To flow and persistence unchanged.
- Headless-safe guards preserved; no schema/runtime changes required.
- Roadmap updated (Phase 2.2 categories Done + STATUS DELTA/CHANGELOG), README tips mention category toggles.
- Build & tests: Debug (SDL2) with -j12; full ctest suite passed 100% (586/586).

Skills panel: Effect Node Graph connections, validation coloring, and derived delays

- Implemented explicit connection system in Effects tab Node Graph: link/unlink flow with cycle prevention, parent mapping, and edge rendering.
- Added per-node validation-based tinting (red invalid id, orange timing issues, green OK) and actions: Unlink Selected, Clear Selected Node.
- "Apply Connections to Delays" computes delay_ms from graph topology by accumulating parent spans (duration or repeats × interval). Kept "Chain Nodes" for X-order quick sequencing.
- Preserved headless-safe guards; no schema/runtime changes required.
- Updated roadmap (STATUS DELTA + Phase 2.2 status) and README with linking workflow and colors.
- Built Debug (SDL2) with parallelism (-j12) and ran full test suite: 100% pass (586/586).

Skills: Meta Skill Type selector UI + debug API; overrides roundtrip; roadmap/README updates

Summary:
- Added a Skill Type selector to the Skills overlay (Meta section) with labels for MELEE/RANGED/AOE_SPELL/BUFF/DEBUFF/HEAL/SUMMON/PASSIVE/ULTIMATE.
- Implemented headless-safe debug APIs: rogue_skill_debug_get_type / _set_type and wired them into overrides export/import JSON.
- Rebuilt (Debug, SDL2, -j12) and ran full test suite: 586/586 PASS.
- Updated roadmap (STATUS DELTA and Phase 2.1 marking selector as Partial with presets pending) and README (new note about the Skill Type selector).

Notes:
- Selector persists via overrides JSON and refreshes validation banner after changes.Extend skills with Visuals/Advanced fields end-to-end and surface in overlay

 - Add optional visuals/audio/animation/AoE/projectile fields to RogueSkillDef and JSON loader (deep-copy/free, schema export)
 - Implement headless-safe debug APIs to get/set visual params with clamps and frees
 - Validate asset existence and parameter bounds in skills validator; wire overlay save/create to show errors
 - Expose a Visuals / Advanced section in the Skills panel; persist via overrides JSON and support auto-reload
 - README: document Visuals/Advanced usage; Roadmap: mark Phase 1.1 partial completion
 - Build/tests: Debug SDL2 full suite remains 100% green (584/584)
feat(overlay/skills): add "Create New Skill" wizard + headless API and tests

- New API: rogue_skill_debug_create(name, max_rank, base_cd_ms, cd_red_ms_per_rank, cast_ms, is_passive)
  - Appends a new RogueSkillDef to the registry with sensible defaults
  - Validates inputs; rejects duplicates by name (-2)
- Skills panel: new "Create New Skill" section (name, max rank, base CD, CD/rank, cast ms, passive)
  - On success, selects the new skill and shows a status line
- Unit: tests/unit/test_skill_creation_wizard.c verifies active/passive creation and duplicate guard
- Docs: README debug overlay tips mention the skills creation wizard
- Roadmap: Phase 14.1 and 14.5 marked Partial for Skills wizard and tests

feat(overlay/skills): template-based creation (prefill + optional coeff copy) + tests

- Skills panel: creation wizard gains a template workflow
  - "Template Skill Id" slider + "Apply Template" prefill name (suffix _Copy), max_rank, passive, and timing from the selected template
  - "Copy Coeffs" option applies template RogueSkillCoeffParams to the preview selection and to the newly created skill on success
- API: new `rogue_skill_debug_get_meta(id, &max_rank, &is_passive)` to support template prefill
- Tests: add `tests/unit/test_skill_template_creation.c` covering active/passive templates and coeffs copy on/off
- Docs: README overlay tips mention the template workflow; test counts updated
- Roadmap: Phase 14.2 marked Partial with details of the template UI/API and test coverage

feat(overlay/skills): searchable template selector in creation wizard

- Skills panel: add a "Template Filter" input and a filterable table (ID, Name) of skills; clicking a row selects its id for the template apply flow
- UX: substring filter for quick narrowing; integrates with existing "Apply Template" and optional "Copy Coeffs"
- Docs: README updated to mention the searchable template picker; roadmap CI counts bumped to 584/584 and Phase 14.2 notes the new selector

feat(overlay/skills): validation pipeline integration (API + UI gating) + unit test

- API: add `rogue_skill_debug_validate(char* err, int err_cap)` delegating to the core validator for headless-safe checks.
- UI: Skills panel Save Overrides JSON runs validation first and blocks save on failure with an error message; Create runs validation and shows a warning on failure (warn-only to allow iteration).
- Tests: new `tests/unit/test_skills_validation_pipeline.c` covers invalid→fix→valid for offensive skills missing coefficients.
- Docs: README “Debug overlay tips” adds validation behavior and mentions the new API; Validation System section documents panel integration.
- Roadmap: Phase 14.3 marked Done with details; full Debug (SDL2) build and suite are green (584/584, -j12).

feat(overlay/skills): live sticky Validation Status banner in Skills panel

- UI: Add a prominent, sticky "Validation Status" banner at the top of the Skills panel that shows either OK or ERROR with the first failure reason.
- Live refresh: The banner updates immediately when editing Timing, Coefficients, or Visuals/Advanced fields, and after Create/Save/Load actions.
- Validation: Uses the shared skills validator; no new rules added, just surfaced inline for fast iteration.
- Docs: README “Debug overlay tips” notes the banner; roadmap status delta mentions the new overlay feedback.
- Build/tests: Debug SDL2 build succeeds and the full CTest suite remains 100% green (584/584, -j12).

feat(skills): introduce optional skill_type enum (schema+runtime+validator)

- Data model: Add RogueSkillType enum and `skill_type` field to `RogueSkillDef`; defaults to UNKNOWN for back-compat.
- Loader/Schema: JSON/CSV loaders accept `skill_type`; schema exports/validates with range 0..9.
- Validator: flag mismatches when `skill_type=PASSIVE` but `is_passive=0`; reject out-of-range values.
- Docs: README Debug overlay tips mention the optional `skill_type` and defaulting behavior.

 feat(skills overlay ui): Phase 2.1 tabs + type presets + context-sensitive panels; move simulation to Testing tab

 - Introduce top-level tabs in Skills panel: Overview, Effects, Visuals, Audio, Testing. Keeps authoring organized and uncluttered.
 - Add "Type Presets" per RogueSkillType (MELEE/RANGED/AOE_SPELL/BUFF/DEBUFF/HEAL/SUMMON/PASSIVE/ULTIMATE) applying safe baseline timing/coeff defaults.
 - Make property panels context-sensitive to selected skill_type (e.g., AoE fields only for AOE_SPELL; projectile fields only for RANGED; hide animation for PASSIVE).
 - Consolidate simulation controls into the Testing tab; Overview focuses on metadata and creation.
 - Preserve sticky Validation Status banner behavior and overrides Save/Load + Auto-Reload flows.
 - Build: Debug SDL2 with parallelism; Tests: full ctest suite all green (586/586).
 - Docs: README debug overlay tips updated for tabs, presets, context-sensitive panels, and Testing tab move.
 - Roadmap: STATUS DELTA updated; Phase 2.1 items (tabs, presets, context-sensitive panels) marked Done; preview window remains pending.

feat(overlay/skills): real-time preview in Testing tab (sprite-based, headless-safe)

- Add a real-time preview panel to the Skills Testing tab that renders cast/projectile/impact/AoE sprites with sprite-sheet animation.
- Controls: Enable Real-time Preview, Auto-animate, and Zoom; selects visuals based on skill_type and caches textures.
- Headless-safe guards prevent SDL calls when no renderer is present.
- Fix compile issues by moving a nested helper to file scope and adding <string.h> in panels_skills.c.
- Build: Debug SDL2 with -j12 succeeded; Tests: full suite passed (586/586).

fix(skills overrides): atomic import of primary effect + nodes; order-independent

- Loader now parses `effect_spec_id` and `effect_nodes[]` in any order and applies them together via `rogue_skill_debug_set_effects` once the skill id is known.
- Prevents intermediate partial state from earlier immediate-apply behavior; matches Effects tab export.
- Rebuilt Debug (SDL2, -j12); full ctest suite passed 100% (586/586).

feat(overlay/skills): add EffectSpec palette + registry enumeration helper

- Effects tab: toggleable palette lists all EffectSpecs with a substring filter, selection, and Assign-To (Primary or Node 0..2). Clicking Assign Selected updates the composition and marks it dirty for overrides save.
- Core: new public `rogue_effect_count()` exposes registry size to drive the palette safely; used alongside existing `rogue_effect_get`.
- Docs: README adds palette usage; Roadmap STATUS DELTA and CHANGELOG updated; Phase 2.2 marked Partial for the minimal palette with notes on pending graph editor/validation.
- Build/Tests: Debug (SDL2) full suite passes (586/586) with -j12.

Skill Creation Suite: Effects authoring UX polish (banner + inline validation + filters)

- Skills panel: added a sticky Validation Status banner at the top (live updates on edits & Create/Save/Load).
- Effects tab: inline validation for primary/node EffectSpec IDs, timing (duration, repeat_count/interval coupling), and HP gate limits; messages render in-row as you type.
- EffectSpec palette: added Kind filter and Debuff-only toggle; kept substring filter; added "Clear Nodes" action. Assignments persist via overrides.
- Rebuilt Debug (SDL2, -j12) and ran full test suite: 586/586 PASS.

Add basic Skills Node Graph editor with inline quick-fixes; keep tests green

- Skills panel (Effects tab):
  - Introduce a basic Node Graph editor (drag/move nodes, click select) with a compact parameter panel (EffectSpec ID, delay_ms, duration_ms, repeat_count, repeat_interval_ms, HP gate).
  - Add "Chain Nodes" action: orders nodes by X position and auto-derives sequential delays from node durations/repeats.
  - Inline quick‑fix buttons next to common validation errors: clear invalid IDs (primary/node), set duration 0, clamp repeat_count 0..32, set repeat_interval to 1000ms when repeats > 0 and interval is 0, clamp HP gate to 0..100%.
  - Rendering is headless‑safe (no SDL calls when no renderer is present).
- Palette: keep the EffectSpec palette (filters + Assign-To) and Clear Nodes; composition persists via overrides import/export.
- Build & tests: Debug build (SDL2, -j12) succeeded; full ctest suite passes 100% (586/586).
- Docs: update roadmap (STATUS DELTA + CHANGELOG) and README with Node Graph usage notes.

docs(effectspec): document SPAWN_PROJECTILE JSON fields & behavior; tests 100% green

- README External EffectSpec Config now lists SPAWN_PROJECTILE fields: proj_speed, proj_life_ms, proj_count, and explains damage-from-magnitude and facing-based direction. Notes multi-count emission.
- Tests: referenced new unit `tests/unit/test_effectspec_spawn_projectile.c`; re-verified full Debug (SDL2) suite 100% green (588/588, -j12).

refactor(overlay/skills): modularize Skills panel into per-tab modules + orchestrator; unify validation/save helpers

- Split legacy monolith into modules: panel_skills_overview.{h,c}, panel_skills_effects.{h,c}, panel_skills_visuals.{h,c}, panel_skills_audio.{h,c}, panel_skills_testing.{h,c}; added panel_skills_shared.{h,c} for validation cache, paths, and save+refresh.
- Orchestrator (panels_skills.c) delegates to tabs and renders the sticky Validation Status banner; headless-safe preview and guards preserved.
- Visuals/Audio/Testing logic migrated with unchanged behavior; Effects/Overview wired to shared helpers; persistence via overrides JSON verified.
- Build & tests: Debug (SDL2) with -j12; full suite passed 100% (587/587).
- Docs: roadmap STATUS DELTA/CHANGELOG updated; README notes modularization and bumps latest run to 587/587.

feat(overlay/skills): Effects tab authoring helpers (Repeat Mode + EffectSpec-based suggestions)

- Added a lightweight Repeat Mode helper (0 none / 1 count / 2 window) that maps cleanly to repeat_count, repeat_interval_ms, and duration_ms without guesswork.
- Contextual suggestions sourced from EffectSpec timing: one-click "Apply spec duration" and "Use spec pulse period" (optionally deriving repeat_count), plus extra fixes for interval/duration inconsistencies.
- UI-only changes; headless-safe. Roadmap STATUS DELTA/CHANGELOG and README updated.
- Build & tests: Debug (SDL2) with -j12; full ctest passed 100% (587/587).

feat(overlay/skills): Effects tab presets & batch helpers (Instant/Window/Counted + HP gates + normalize/fill/clear)

- Added Node Presets: Instant (one-shot), Periodic Window (uses EffectSpec pulse if present; defaults safe), and Counted Pulses (derives interval/count from spec duration/pulse with clamping).
- Added HP gate quick buttons (0%, 25%, 50%) and Batch Helpers: Fill empty nodes with Primary, Normalize window durations to whole pulses, Clear all HP gates.
- UI-only; headless-safe. Roadmap STATUS DELTA/Phase 2.2 marked Done; README notes new presets/helpers.
- Build & tests: Debug (SDL2) with -j12; full ctest suite 100% green (587/587).
