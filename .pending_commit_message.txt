Persistence Phase 4.5: Add optional signature abstraction (save format v9)

- Bump ROGUE_SAVE_FORMAT_VERSION to 9.
- Add RogueSaveSignatureProvider API (sign/verify callbacks + registration).
- Implement signature trailer layout: [uint16 sig_len]["SGN0"][sig bytes] appended after SHA256 footer (v7+) when provider registered.
- Descriptor checksum logic clarified: checksum over payload only (excludes SHA footer + optional signature).
- Signature covers payload + SHA footer; verification failure sets new tamper flag ROGUE_TAMPER_FLAG_SIGNATURE.
- Add migration v8->v9 (no-op structural).
- Update save_manager.c for consistent footer ordering (compute CRC then write SHA then signature).
- Update load_and_validate to detect & verify optional signature (bounds + magic + length checks, len clamp <4096).
- Add unit test test_save_v9_signature (dummy XOR signature) validating success path and tamper flag on corruption.
- Update roadmap (Phase 4.5 marked Done) and README (v9 description + signature details).
- Preserve prior integrity features (per-section CRC, SHA256, tamper flags, recovery) with passing builds.
